# Day 2 第六小時：Nginx 容器實戰

---

## 一、前情提要（2 分鐘）

前面學了一堆指令，現在來實戰。

這堂課用 Nginx 做一個完整的練習：
- 啟動 Nginx 容器
- Port Mapping
- 瀏覽器驗證
- 自訂首頁
- 設定檔修改
- 多個 Nginx 容器

---

## 二、認識 Nginx（5 分鐘）

### 2.1 Nginx 是什麼

Nginx 是一個高效能的 Web 伺服器，也常用作反向代理、負載均衡。

特點：
- 輕量、快速
- 高併發處理能力
- 設定簡單

Docker 官方映像檔是最常被下載的映像檔之一。

### 2.2 為什麼用 Nginx 練習

- 映像檔小（alpine 版約 40MB）
- 啟動快
- 有 Web 介面，方便驗證
- 實際工作常用

---

## 三、啟動第一個 Nginx 容器（10 分鐘）

### 3.1 拉取映像檔

```bash
docker pull nginx:alpine
```

alpine 版本比較小。

### 3.2 前景執行看看

```bash
docker run nginx:alpine
```

會看到 Nginx 的啟動日誌。

但這樣沒辦法訪問——沒有做 Port Mapping。

Ctrl+C 停止。

### 3.3 背景執行 + Port Mapping

```bash
docker run -d --name web -p 8080:80 nginx:alpine
```

- `-d`：背景執行
- `--name web`：容器名稱
- `-p 8080:80`：主機 8080 對應容器 80

### 3.4 驗證

```bash
# 確認容器在跑
docker ps

# 命令列測試
curl http://localhost:8080
```

或打開瀏覽器訪問 http://localhost:8080

會看到 Nginx 的歡迎頁面。

### 3.5 查看日誌

```bash
docker logs web
docker logs -f web    # 持續追蹤
```

用瀏覽器訪問幾次，可以看到 access log。

---

## 四、Port Mapping 深入（10 分鐘）

### 4.1 格式解析

```
-p [主機IP:]主機Port:容器Port[/協定]
```

**常見寫法**

```bash
-p 8080:80              # 主機 8080 → 容器 80
-p 127.0.0.1:8080:80    # 只綁定 localhost
-p 8080:80/tcp          # 指定 TCP（預設）
-p 8080:80/udp          # 指定 UDP
-p 80:80 -p 443:443     # 多個 port
```

### 4.2 綁定所有介面 vs 只綁定 localhost

```bash
# 所有介面（預設），外部可存取
docker run -d -p 8080:80 nginx

# 只綁定 localhost，外部無法存取
docker run -d -p 127.0.0.1:8080:80 nginx
```

生產環境考慮安全性，不要隨便開放所有介面。

### 4.3 使用隨機 Port

```bash
docker run -d -P nginx
```

大寫 `-P` 會把映像檔 EXPOSE 的所有 port 對應到隨機的高位 port。

查看對應：

```bash
docker port web
# 輸出：80/tcp -> 0.0.0.0:49153
```

### 4.4 查看 Port 使用

```bash
docker port web
docker ps --format "{{.Names}}: {{.Ports}}"
```

---

## 五、自訂網頁內容（15 分鐘）

### 5.1 Nginx 預設網頁目錄

Nginx 官方映像檔的網頁目錄：

```
/usr/share/nginx/html/
```

裡面有 index.html（歡迎頁）和 50x.html（錯誤頁）。

### 5.2 方法一：docker cp 複製檔案

先建立一個 HTML 檔案：

```bash
echo '<h1>Hello Docker!</h1>' > index.html
```

複製到容器：

```bash
docker cp index.html web:/usr/share/nginx/html/index.html
```

重新整理瀏覽器，看到變化。

### 5.3 方法二：Volume 掛載（推薦）

先準備一個目錄和檔案：

```bash
mkdir -p ~/docker-demo/html
echo '<h1>Hello from Volume!</h1>' > ~/docker-demo/html/index.html
```

啟動容器時掛載：

```bash
# 先刪除舊容器
docker rm -f web

# 用 volume 掛載
docker run -d --name web \
  -p 8080:80 \
  -v ~/docker-demo/html:/usr/share/nginx/html:ro \
  nginx:alpine
```

`:ro` 表示唯讀（read-only），容器不能修改這個目錄。

驗證：

```bash
curl http://localhost:8080
```

**修改主機上的檔案**

```bash
echo '<h1>Updated!</h1>' > ~/docker-demo/html/index.html
```

刷新瀏覽器，立刻看到變化。不需要重啟容器。

這就是 Volume 的好處——主機和容器共用檔案。

### 5.4 建立完整的靜態網站

```bash
mkdir -p ~/docker-demo/website
cat > ~/docker-demo/website/index.html << 'EOF'
<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>My Docker Website</title>
    <style>
        body { font-family: Arial; margin: 40px; }
        h1 { color: #0066cc; }
        .container { max-width: 800px; margin: 0 auto; }
    </style>
</head>
<body>
    <div class="container">
        <h1>Welcome to My Docker Website!</h1>
        <p>This website is running in a Docker container.</p>
        <ul>
            <li>Web Server: Nginx</li>
            <li>Container Engine: Docker</li>
        </ul>
    </div>
</body>
</html>
EOF
```

重新建立容器：

```bash
docker rm -f web
docker run -d --name web \
  -p 8080:80 \
  -v ~/docker-demo/website:/usr/share/nginx/html:ro \
  nginx:alpine
```

瀏覽器訪問看效果。

---

## 六、修改 Nginx 設定（10 分鐘）

### 6.1 查看預設設定

```bash
docker exec web cat /etc/nginx/nginx.conf
docker exec web cat /etc/nginx/conf.d/default.conf
```

### 6.2 提取設定檔

```bash
mkdir -p ~/docker-demo/nginx
docker cp web:/etc/nginx/conf.d/default.conf ~/docker-demo/nginx/
```

### 6.3 修改設定

編輯 ~/docker-demo/nginx/default.conf：

```nginx
server {
    listen       80;
    server_name  localhost;

    # 自訂 access log 格式
    access_log  /var/log/nginx/access.log;

    location / {
        root   /usr/share/nginx/html;
        index  index.html index.htm;
    }

    # 新增 /api 路徑
    location /api {
        return 200 '{"status": "ok", "message": "Hello from Docker Nginx!"}';
        add_header Content-Type application/json;
    }

    error_page   500 502 503 504  /50x.html;
    location = /50x.html {
        root   /usr/share/nginx/html;
    }
}
```

### 6.4 套用新設定

重新建立容器，掛載設定檔：

```bash
docker rm -f web
docker run -d --name web \
  -p 8080:80 \
  -v ~/docker-demo/website:/usr/share/nginx/html:ro \
  -v ~/docker-demo/nginx/default.conf:/etc/nginx/conf.d/default.conf:ro \
  nginx:alpine
```

驗證：

```bash
curl http://localhost:8080
curl http://localhost:8080/api
```

### 6.5 熱重載設定（不重啟容器）

如果只是改設定，不需要重建容器：

```bash
# 修改主機上的設定檔後
docker exec web nginx -s reload
```

Nginx 會重新載入設定。

---

## 七、運行多個 Nginx 容器（5 分鐘）

### 7.1 同時運行多個

```bash
# 網站 A
docker run -d --name site-a -p 8081:80 \
  -v ~/docker-demo/site-a:/usr/share/nginx/html:ro \
  nginx:alpine

# 網站 B
docker run -d --name site-b -p 8082:80 \
  -v ~/docker-demo/site-b:/usr/share/nginx/html:ro \
  nginx:alpine
```

準備不同的內容：

```bash
mkdir -p ~/docker-demo/site-a ~/docker-demo/site-b
echo '<h1>Site A</h1>' > ~/docker-demo/site-a/index.html
echo '<h1>Site B</h1>' > ~/docker-demo/site-b/index.html
```

驗證：

```bash
curl http://localhost:8081    # Site A
curl http://localhost:8082    # Site B
```

### 7.2 查看所有 Nginx 容器

```bash
docker ps -f ancestor=nginx:alpine
```

### 7.3 統一管理

```bash
# 停止所有
docker stop site-a site-b web

# 啟動所有
docker start site-a site-b web

# 刪除所有
docker rm -f site-a site-b web
```

---

## 八、練習題（3 分鐘）

**題目**

1. 啟動一個 Nginx 容器
   - 名稱：my-web
   - Port：本機 80 → 容器 80
   - 掛載本機目錄到 /usr/share/nginx/html
2. 在掛載的目錄中建立一個 about.html
3. 瀏覽器訪問 http://localhost/about.html

**參考答案**

```bash
mkdir -p ~/my-web
echo '<h1>About Page</h1>' > ~/my-web/about.html
echo '<h1>Home</h1><a href="about.html">About</a>' > ~/my-web/index.html

docker run -d --name my-web \
  -p 80:80 \
  -v ~/my-web:/usr/share/nginx/html:ro \
  nginx:alpine

curl http://localhost/
curl http://localhost/about.html
```

---

## 九、本堂課小結（2 分鐘）

這堂課用 Nginx 實戰練習了：

**核心技能**
- Port Mapping：`-p 主機Port:容器Port`
- Volume 掛載：`-v 主機路徑:容器路徑`
- 查看日誌：`docker logs`
- 進入容器：`docker exec`
- 複製檔案：`docker cp`

**學到的技巧**
- 掛載網頁目錄，主機修改立即生效
- 掛載設定檔，自訂 Nginx 行為
- 多容器運行在不同 port
- 熱重載設定：`nginx -s reload`

Day 2 的內容到這邊。下一堂是練習和複習。

---

## 板書 / PPT 建議

1. Nginx 容器目錄結構（/usr/share/nginx/html, /etc/nginx）
2. Port Mapping 示意圖
3. Volume 掛載示意圖
4. 完整的 docker run 命令解析

---

## 📺 補充教材：狂神說 Docker

> 以下內容來自【狂神說Java】Docker 教程系列，作為本節的補充參考資料。

### P14：作業一：部署 Nginx

<sub>[00:00-00:05]</sub>
我們來看一下，首先第一個作業，我想讓大家用 Docker 來安裝一個 Nginx。我們來帶大家做一遍，Nginx 我們想用它做負載均衡跟反向代理。

<sub>[00:06-00:16]</sub>
那怎麼樣去裝它呢？我們來回顧一下，首先第一步我們是不是要搜索鏡像，來，跟著我們的思路，學以致用 Docker search，比如說我先搜索一個 Nginx。

<sub>[00:17-00:29]</sub>
如果搜到了我們就可以下載，那當然的話你要看指定版本號的話，是不是要去我們的 Docker Hub 上去下載。要在這個地方搜，你往這個地方搜，比如搜 Nginx，在這個地方搜能看到詳細的信息，然後就教大家如何用。

<sub>[00:30-00:46]</sub>
第一步我們在這邊搜索 Nginx，搜索到了之後在這邊去點擊，點擊到了之後我們就可以往裡面看。在這裡面的話有一些版本的信息，你可以根據版本下載。那這種帶了什麼 Alpine 的，這個什麼東西呢，這就是瘦身版的，用它就非常非常小。

<sub>[00:46-01:05]</sub>
然後再往下的話就是一個告訴你怎麼樣去使用它，然後 What is Nginx，告訴你它什麼，然後下面就是 How to use 教你怎麼去使用。其實你把這個命令拿過來就能跑了，但是這樣的話你根本搞不懂它是為什麼，所以說我們還是從頭學。

<sub>[01:05-01:20]</sub>
那我們來第一步搜索鏡像，搜索到了 Nginx 沒有什麼問題，那現在我們幹嘛把它啟動一下。來，`docker pull`，我們可以把這個 Nginx 啟動起來，它現在就幹嘛去給我們把這個鏡像下載下來。

<sub>[01:20-01:42]</sub>
對不對，所以說我在這個地方給大家寫步驟，命令我就不寫了。我就把這個地方留成作業，讓大家去搜索鏡像。搜索鏡像，我建議大家去網站上搜索。第二步呢咱們這個地方要拉取鏡像、下載鏡像。pull 下載，上面搜索 search，不建議大家用 search，我建議大家去咱們的 Docker Hub 上搜索，這樣的話可以看到它的詳細信息和幫助文檔。

<sub>[01:54-02:11]</sub>
然後我們就把它下載下來。OK，現在的話就把它下載成功了，`docker pull` 下載完了，下載完之後我們來看一下我們這個裡面有沒有這個鏡像。`docker images` 可以發現我們這邊是不是把 Nginx 這個鏡像下載下來了。

<sub>[02:11-02:30]</sub>
那下載下來就是啟動嘛，我們來啟動，首先 `docker run` 進行啟動。`docker run` 啟動之後還是一樣的，首先我們想以後台的方式啟動 Nginx，然後這是我們給 Nginx 取個名字，這個 `--name` 的話不寫就是默認是它，我們如果要起多個就可以寫名字，比如說我寫一個 nginx01，我們還可以起 02、03。

<sub>[02:30-02:58]</sub>
對不對，那我現在的話 Nginx 我們要暴露一些端口號，比如說 Nginx 默認的端口號是 80，比如說我現在暴露一個 3344 吧，因為我的服務器 3344 是開放的。假設我放了一個這麼端口號，這是對於我什麼呢，這個是對於我服務器的外部的地址，然後這個是容器內部的地址。就是我們的 Nginx 默認是 80，那現在的話我們把它映射到外面 3344，也就是我們通過公網的 3344 可以訪問到 Docker 裡面的 80，等會給大家畫幅圖讓大家理解。

<sub>[02:58-03:17]</sub>
然後最後把這個 Nginx 這個鏡像啟動起來就是它，那現在的話這段命令完之後它就會啟動。那它就啟動了這麼一個容器，啟動起來之後 `docker ps` 查看一下這個容器，你看 Nginx 這個容器是不是正在啟動啊。

<sub>[03:17-03:40]</sub>
那現在的話我們可以來測試訪問一下，先本機測試，`curl` 就是我們去查看一個 URL 的狀況，發起個請求。那我們測一下本機的 3344，來回車。看到了嗎，Welcome to Nginx，所以說本機自測可以通過。

<sub>[03:40-04:06]</sub>
你看啟動環境多麼簡單。來，運行測試，查看一下當前的鏡像。那這個地方說一下它的一個參數，`-d` 是代表後台運行，然後 `--name` 這是咱們的起名字給容器命名。然後再往下的話，`-p` 是暴露端口，然後這個地方的兩個端口是一個咱們的外部訪問，就是咱們的服務器或者咱們的宿主機的端口，還有就是它的容器內部端口。

<sub>[04:06-04:29]</sub>
來，給大家畫個圖理解這麼一個概念。很多人不懂，什麼叫容器內端口，什麼叫宿主機端口。現在我們的環境好比是這麼樣子的，這是我們的一個大大的 Linux，這個 Linux 裡面有個東西叫做防火牆。

<sub>[04:29-05:08]</sub>
然後再往上的話，這裡面是我們 Docker 一個服務。那這個服務裡面有非常多的容器啟動，比如說我這邊來個容器，我現在寫了一個容器叫 nginx01，這是我啟動起來的。這個容器起來的話，它是因為下面有個鏡像啟動起來的，鏡像通過 `run` 啟動起來的容器。這個容器我們說了，它就好比是一個獨立的 Linux，所以在這個容器內部，它是一個單獨的小的 Linux 環境。

<sub>[05:08-05:36]</sub>
那在這裡面，我們是不是啟動了一個 Nginx 啊，是不是它開啟了 80 端口啊。但是這個是在 Docker 這個容器裡面，它跟外面是隔離的，我們說了容器是不是隔離的，我們之前說過。那外面怎麼去訪問它呢？就是我們真正的話，公網 IP 在外面。

<sub>[05:36-06:15]</sub>
我們外網首先要請求到 Linux 上，Linux 要開端口。如果你用的是阿里雲，這個中間還要攔一道阿里雲的安全組。比如說我們要在這個地方首先開放咱們的 3344，那在 Linux 防火牆你也得開啟這個 3344 端口。那這兩個開啟了，就是通過外網能夠訪問到 Linux 了，但是 Linux 還訪問不到 Docker。所以說我們要讓宿主機跟這個容器打通，就是我們要讓宿主機進入這個容器打通，所以說在這個地方我們要通過 `-p` 來暴露。

<sub>[06:15-06:44]</sub>
暴露就有兩個參數，第一個是我們外部的就是宿主機的，還有一個是容器裡面的。那外面的話就是我們的 3344，容器裡面是 80。那通過這麼一個操作，我們就可以通過外網訪問 3344 來訪問到我們的 80 了，這就好比是一個映射的關係，一定要理解這個端口暴露的一個概念。

<sub>[06:44-07:24]</sub>
OK，那現在的話我們在這個地方 3344 是能夠啟動成功的。假設如果我的防火牆都是開的，我在外網去訪問我的 3344 應該是能訪問到的。如果你能訪問到的話，這個防止就是公網了，你們也能去訪問，這個是我服務器的地址加 3344，這個就是我 Docker 裡面的，我現在部署的一個地址。

<sub>[07:24-08:06]</sub>
在停之前呢，我們先想收一下進去的感覺。來，我們來進入一下容器，首先 `docker ps` 看一下這邊啟動的容器，我們現在進去，`docker exec -it nginx01 /bin/bash`，進入交互模式。OK，那現在就進入 Nginx 裡面了。那在這裡面的話，你可以找一下 Nginx 配置文件，大家非常熟悉，Nginx 配置文件是不是就在 `/etc` 目錄下，然後咱們的 URL 是不是就在這裡面。

<sub>[08:06-08:23]</sub>
你們可以去看一下，我們的 Nginx configure 配置是不是就在這的。你們的 HTML 的頁面數據在這個 Nginx 下面，你現在可以動態的改。現在的話你們這個公網地址能訪問了，裡面的數據也能改了。

<sub>[08:23-09:03]</sub>
進入容器就是 `docker exec -it` 進入交互模式，然後在這裡面可以看那些文件。那我現在這個地方，再來看看留個思考題。我們現在去實戰部署了一個 Nginx，並且可以到公網訪問了，你們在這個地方是不是可以直接訪問了。我現在只需要把這個 Docker 容器停止了，`stop`，先退出容器，`docker ps`，`docker stop`。

<sub>[09:03-09:41]</sub>
我一旦 stop 之後，你們就訪問不了了嘛，其實我就已經部署了一個應用了。部署容器，Docker 運起來這麼簡單，我們把它停止了，我現在停止了，你們不可能訪問了，刷新報錯，因為服務器已經停了嘛。映射不過去了，那這是我們一個端口暴露概念，我們現在就可以把一個容器裡面的應用部署出來。

<sub>[09:41-10:46]</sub>
那這是咱們的第一個練習，Docker 部署 Nginx，並上線運行測試就這麼簡單。然後這個地方重點理解了 Docker 暴露端口的一個概念。但是我希望大家思考一個問題，就是我們每次改動我們的 Nginx 配置文件，都需要進入容器內部，是不是十分的麻煩。我要是可以在容器外部，提供一個映射路徑，然後在容器外部修改文件，容器內部就可以自動修改。那這個就是我們後面要講的一個技術——數據卷的技術，我們之後講到再說。

### P21：容器數據卷使用

<sub>[00:00-00:15]</sub>
OK 那我們來接著昨天的講。我們來先回顧一下，昨天的話我們是把咱們 Docker 基礎入門到咱們鏡像的講解都搞定了。那我之前的話給大家說了，這算 Docker 入門階段。而我們今天要學的這三個，就屬於 Docker 的一個精髓階段，就是 Docker 的容器數據卷、以及 Dockerfile、還有 Docker 的網絡。這三個相對於昨天的知識來說，會相對來說難一點。

<sub>[00:22-00:46]</sub>
但是你一定要把昨天的一些基本命令全部掌握，比如說基本的鏡像命令啊、容器命令啊、然後鏡像的一些基本的原理。搞懂這個之後，我們就可以去學咱們今天的內容。那咱們來看一下，第一個就是容器數據卷。首先的話，我們還是要搞懂一個詞，什麼是容器數據卷，那這是一個什麼樣的技術，我們為什麼要學它。

<sub>[00:46-01:05]</sub>
那首先我們說過 Docker 的理念，我們來回顧一下。Docker 的理念有什麼東西？一句話就是將我們的應用和咱們的一些運行的環境，打包成一個鏡像，然後發布之後啟動，然後運行，就變成一個容器了。

<sub>[01:05-01:22]</sub>
那現在的話，我們有一個小小的問題，比如說現在我舉個例子，我們很多時候，我們的程序它是需要保存數據的，那這個數據就不應該放在容器裡面。如果數據都在容器中，那麼我們一把容器刪除就沒了。

<sub>[01:22-02:05]</sub>
那麼我們容器一刪除，只要容器刪除了，那數據就會丟失。那假設數據丟失的話，我們這就是不希望的。所以說我們這個地方又提了一個新的需求，那就是我們有這麼一種需求，我希望數據是不是可以持久化。尤其是像數據庫這樣的，比如你安裝了一個 MySQL，你用 Docker 安裝 MySQL，然後你把這個 MySQL 的容器刪了，但是 MySQL 這裡面存的數據會跟著這個容器一塊丟。那如果你這樣的話，相當於什麼，就是刪庫跑路了，這個風險就非常非常大。

<sub>[02:05-02:45]</sub>
所以說在這種情況下，我們需要什麼呢？就是我們也有一個需求，就是我們需要 MySQL 的數據可以存儲在本地或者其他地方，反正只要不在這個容器裡面就好了。所以說我們就迫切的希望有這麼一個技術，就是容器之間可以有一個數據共享的技術。我們要學這個容器數據卷，是因為我們之前學的鏡像容器，它沒有這個功能。那它給我們提供了一個技術來解決這個問題，那就是一個容器數據卷。

<sub>[02:45-03:10]</sub>
那也就是什麼意思，就是在 Docker 容器中產生的數據，我會把它同步到本地。那假設同步到本地之後，它是不是就可以存出來，那我把容器刪了，但是這個數據在本地，所以說就不會丟失。那這就是咱們的一個卷技術。

<sub>[03:10-03:40]</sub>
那其實也比較簡單，說白了就是一個目錄的掛載。就是將我們容器內的目錄掛載到咱們的虛擬機上或者掛載到咱們的 Linux 上面。我們昨天畫過一幅圖，我們說了我們的 Linux 假設在這個地方，那這是我們的大系統，然後這裡面是我們的 Docker 服務，然後這裡面運行了很多很多的容器。

<sub>[03:40-04:15]</sub>
那假設這是我們的一個 Docker 服務，比如說 Docker Server。那這個 Docker 裡面，我們現在跑了非常多的容器，比如說我這個地方有個 MySQL 的容器，那這個容器裡面它是自成一個系統，在這裡面的話它的數據應該是存在自己的獨立的 Filesystem 裡面。就是在這個容器裡面我們還有一個小的文件系統，而這個跟外面是隔離的，那我們的 Linux 的文件系統在外面。

<sub>[04:15-05:09]</sub>
那這個時候的話，我們說的容器跟外面是互相隔離，那我們就需要做個映射。比如說把我們 MySQL 裡面的一個路徑能夠映射到我們的 Linux 文件系統裡面，就是 Linux 跟容器裡面掛載。比如說 MySQL 裡面有這麼一個路徑，比如說我寫一個 `/usr/mysql`，那這個路徑是在 MySQL 容器裡面的。那現在的話我們在外部比如說我想把它放到我的 home 目錄下，那就是把容器裡面這個目錄跟咱們真正服務器上的這個目錄同步。

<sub>[05:09-05:28]</sub>
那假設我們在容器內部寫的所有的數據，它會自動的同步到咱們的 Linux 上面的你指定的這個文件夾上。而這個就是一種卷的技術，非常非常簡單，它是一種同步機制。

<sub>[05:28-06:10]</sub>
所以說我們把它總結一句話，說白了為什麼要用卷？就是為了咱們的容器的持久化和同步操作。就是我們不希望所有數據都放在容器中，這個容器一刪肯定是不行的。除了這個之外，它還支持一個高級功能，就是容器間也是可以數據共享的，就是多個容器它可以用同一個目錄。

<sub>[06:10-07:08]</sub>
那我們來先試一下使用一下數據卷。方式一，我們直接使用命令來掛載。我們昨天說過一個命令叫 `-v`，就是 Volume。比如說 `docker run -it -v` 後面的參數就跟我們的 `-p` 類似，`-p` 是主機端口和容器內端口，這是一個端口映射。現在我們再做一個目錄映射，就是主機的目錄地址和容器內的目錄。我們要把主機內的目錄跟容器內的目錄做一個映射，就好比我們所說的端口做個映射。

<sub>[07:08-08:06]</sub>
那現在的話我們理解了 `-v` 之後就可以測試一下。比如說我還是來用一個 CentOS，讓咱們來看一下。比如說我現在想在我的 home 目錄下面去做一個測試。你看我這個 home 目錄下是沒有咱們的測試這樣一個目錄的。那我現在運行一個容器 `docker run -it -v`，我要在咱們的虛擬機下面去用一個測試目錄來掛載，然後這個跟容器裡面的東西綁定起來。就是我想把容器裡面的 home 目錄掛載到我們本地的虛擬機上的 home 下面的測試這個目錄，然後讓它們做一個映射。

<sub>[08:06-09:10]</sub>
啟動起來之後我們可以通過 `docker inspect` 去查看一下容器的詳細信息。我們來找一下 Mounts，看到這個地方有個 Mounts 的同學扣一，這邊是不是有一個掛載。Source 就是我們的目的地，這是咱們主機內的地址；下面的 Destination 就是 Docker 容器內的地址。

<sub>[09:10-10:00]</sub>
我們一旦做了這樣一個映射之後，它們以後的關係就是什麼——比如說我在 Docker 容器裡面加了一個東西，它在主機內就會同步過來。相當於它們兩個其實是一個地址，就好比做完映射之後，我們在容器內部放的文件會直接進這裡面，我們在外面新建的文件它也會在容器裡面出現。

<sub>[10:00-11:30]</sub>
那我們來看一下雙向綁定。現在我這邊是我的 Linux 虛擬機，這邊是我們的容器。我們在容器內部的 home 目錄下，touch 一個 test.java 文件，然後在 Linux 查看，發現是不是數據同步過來了。我們在容器內添加了文件，它會幫我們同步到容器外面，那這兩個目錄它就是一個同步的過程。你們可以把它理解為雙向綁定，就是一邊發生了變化，另外一邊也會隨之變化。

<sub>[11:30-13:25]</sub>
那我們再來測試，按照剛才的邏輯我們現在去把這個容器停了。停止容器之後，我們在宿主機上修改文件，比如說寫一個 hello。然後容器裡面現在是沒有動靜的，那我們再把容器啟動起來。我們通過 `docker start` 啟動，再用 `attach` 進入容器，`cd` 到 home 目錄下 `ls` 看一下，然後 `cat test.java`，你可以發現我們的數據是不是放進去了，完全沒有問題。所以說反向操作它也是可以的，我們在外邊修改了文件，在容器內部也會產生一個同步。所以說它是一個雙向的過程。

<sub>[13:25-14:50]</sub>
不管我們第一步先停止容器，第二步在宿主機上修改文件，修改完畢之後第三步再次啟動容器，你會發現容器內的數據依舊是同步的。這樣的話我們就能明白之前留的作業了，我們在 Nginx 那個作業裡面，每次需要修改配置文件都非常麻煩。那現在我們是不是可以通過一個 `-v` 命令給它掛載出來，比如說 `-v /usr/share/nginx:/usr/share/nginx`，把容器裡面這個目錄跟服務器上的目錄同步。以後我們在服務器上操作就好了，就不需要進入容器了。好處就是我們以後修改只需要在本地修改即可，容器內會自動同步。

### P22：實戰：MySQL 同步數據

<sub>[00:00-00:18]</sub>
我們再來做一個實戰測試，部署 MySQL。我們都知道 MySQL 的數據它不應該放在容器內，它肯定要把數據持久化出來。所以說我們就要思考一個問題，MySQL 這種要持久化數據的都需要思考數據持久化問題。

<sub>[00:18-00:44]</sub>
我們知道 MySQL 底下是不是有個 data 目錄，我們所有的數據都放在這個 data 目錄，那我是不是要把這個 data 給它取出去。容器刪除了外部的數據也就沒了嗎？不是的，容器刪除是刪除容器跟目錄有什麼關係。

<sub>[00:44-01:13]</sub>
那我們來看一下，還是一樣的，我們來回顧一下。首先 `docker search mysql`，我們昨天其實已經搜過 MySQL 了，所以這邊肯定是能搜到的。搜到之後確定可以用，然後按照版本去下載。那我們要拉取鏡像，`docker pull mysql:5.7`，我就下 5.7 版本了。

<sub>[01:13-02:08]</sub>
把它下載下來，這是我們的一個獲取鏡像。下載完了，`docker images`，我們的 MySQL 鏡像過來了。那我們來啟動一下，`docker run -d` 後台運行，`-p 3310:3306` 暴露端口，比如說 3310 對應容器內的 MySQL 默認的 3306。

<sub>[02:08-03:08]</sub>
那再往下，我想把它的數據掛載出來，`-v`，把它放在我們的 home 下面，我現在放一個 mysql，就是這是我們的真實路徑。我們這個 home 中現在應該是沒有 mysql 的，等會它就把 MySQL 的數據映射出來。MySQL 我們一般會改動一個 MySQL 的 config 就是配置文件，那這個是對應我們 MySQL `/etc/mysql` 的配置文件。我們把它的配置文件取出來，我們 MySQL 的配置文件對應容器裡面的路徑。

<sub>[03:08-03:54]</sub>
除了掛載一個，我們還可以同時掛載多個，就是可以映射多個目錄。有時候我們經常的話就會把好多目錄拿出來映射。這個是配置文件的，我們還有數據的。數據的話我也希望把它映射出來，比如說 `home/mysql/data` 對應容器內的 `/var/lib/mysql`。

<sub>[03:54-05:07]</sub>
然後還有一個問題，就是安裝啟動 MySQL 的時候，你們記不記得我們 MySQL 是需要配置密碼的。我們要去配一下 MySQL 的密碼，我還是建議大家在 Docker Hub 上找，因為所有的配置你們一定要懂去哪個地方能找到。在 Docker Hub 上搜 MySQL，進入 MySQL 這個鏡像，往下翻，可以看到它的啟動方式。我們一定要去設置密碼，`-e MYSQL_ROOT_PASSWORD=123456`。

<sub>[05:07-06:00]</sub>
那配完之後我們把這個 MySQL 給它起個名字，`--name mysql01`。你看到現在你們昨天學完之後，今天再看到這一段是不是就非常舒服了，就是慢慢的就能看懂越來越複雜的了。然後 MySQL 5.7 啟動。

<sub>[06:00-06:28]</sub>
`-d` 後台運行，`-p` 端口映射，而我們的 `-v` 就是我們今天寫的數據卷掛載。我們可以通過兩個 `-v` 來掛載兩個目錄，那你要掛三個也是同理。然後 `-e` 是環境配置，我們配置了 MySQL 的密碼。然後就是 `--name` 容器名字，最後把它啟動起來。

<sub>[06:28-07:14]</sub>
啟動成功之後，我們在本地使用 SQLyog 來連接測試一下。比如說我打開 SQLyog，如果這個 MySQL 數據庫現在是能用的話，它就能連上了。新建一個連接，Root 寫上，密碼就是我們剛才設置的密碼，暴露的端口 3310。點擊測試連接，看到連接成功了吧。

<sub>[07:14-08:10]</sub>
我們通過外部第三方工具從我們本地訪問到了 Linux 上的容器內部。首先我們本地的 SQLyog 通過遠程地址 host、帳號密碼、端口連上，連接到服務器的 3310，然後 3310 和容器內的 3306 映射。

<sub>[08:10-09:05]</sub>
那現在我們本地能連接上，說明我們 Docker 部署成功是沒有任何問題的。我在本地 home 目錄下 `ls` 看一下，MySQL 看到了，`cd mysql`，`ls` 看一下，conf 在這，data 也在這。`cd data`，`ls` 可以看到這邊是我們所有的數據，沒有任何問題。

<sub>[08:31-09:07]</sub>
那現在我只需要做一個操作，比如說在 SQLyog 裡面建一個 test 的數據庫，我點擊創建。然後再來 `ls` 看一下，看到了吧！就是我們在本地創建東西，在我們的 Linux 服務器上映射過來了，在 data 目錄裡也映射出來了。所以說之後的持久化是綁定到我們本地的了，就是你把這個容器幹掉了、停了、或者刪了，那我們的數據在本地，所以說數據是不會丟失的。

<sub>[09:07-10:02]</sub>
那在本地測試，創建一個數據庫，查看一下我們映射的路徑是否 OK。假設我們現在映射的路徑是 OK 的，我們在這邊創建的數據庫它的文件是不是就過來了，那就是完全沒有問題的。以後我們要改配置，可以在我們剛才同步出來掛載的 Linux config 下面去改。

<sub>[09:48-10:31]</sub>
剛才有同學問了，老師我把這個容器刪了它還能不能行。那我給你們演示，我現在就把這個 MySQL 彻底刪掉，刪掉之後這個容器沒了，但是數據一定還在的。`docker rm -f` 直接把這個 MySQL 容器刪掉。OK 現在這個容器就沒了，`docker ps` 沒有了，`docker ps -a` 你可以發現我們的 MySQL 是不是彻底沒了。

<sub>[10:31-11:07]</sub>
但是你回過頭來看這邊，`cd data`，`ls`，看到了吧，是不是完全沒有任何問題。就是我們哪怕在本地把這個 MySQL 的容器刪了，我們最終的數據依舊保存在本地，數據不會丟失。假設我們將容器刪除，發現我們掛載到本地的數據卷依舊沒有丟失，這就實現了我們的容器數據持久化功能。

### P23：具名掛載和匿名掛載

<sub>[00:00-00:14]</sub>
我們來說一下，剛才的話我們就測試完了，也理解了什麼是容器數據卷之後，來給大家說一些官方名詞。經常的話你們會看到這兩個詞，一個叫做具名掛載，還有個叫匿名掛載。

<sub>[00:14-00:52]</sub>
其實我們剛才用了兩種方式掛載。這個名字只是我們給當前的掛載方式取了兩個名字。首先是匿名掛載，什麼是匿名掛載呢？我們剛才是不是說過我們 `-v` 之後直接寫容器內路徑，這個樣子也是可以寫的，就是你不去寫我們主機內的路徑，它就會自動幫我們生成一個目錄。

<sub>[00:52-01:36]</sub>
比如說我們可以這樣去啟動一個，`docker run -d -P`，大 P 就是隨機映射端口。比如說我們來起個 nginx01，然後我 `-v` 直接寫 `/etc/nginx`，我把它掛載出來就可以了。我們來把它啟動來測試一下。

<sub>[01:36-02:10]</sub>
這個時候我們可以通過 `docker volume` 來看一下它的狀態。我們先 `help` 查看一下它的幫助文檔。這裡面有這麼幾個操作：第一個你可以創建一個卷 `create`，`inspect` 查看卷它是怎麼做的，`ls` 就是查看當前所有的卷，`rm` 就是移除，`prune` 也是移除沒有使用的。

<sub>[02:10-03:10]</sub>
我現在 `docker volume ls`，在這邊大家可以看到我們剛才配置的一些東西，在這邊它有一個對應的，就是本地有很多很多的卷。這個卷的位置在哪呢？這些都是沒有名字的，就是匿名的，匿名卷就是沒有給它起名字。因為什麼呢？我們在指定的時候是不是只指定了容器內的，沒有指定容器外的。

<sub>[03:10-04:14]</sub>
為什麼叫匿名掛載？因為我們在 `-v` 的時候只寫了容器內的路徑，沒有寫容器外的路徑。那現在我們去看一下它是掛載在哪個地方的。我們再啟動一個，比如說我現在弄一個具名的，`docker run -d -P --name nginx02 -v juming-nginx:/etc/nginx nginx`。看到了吧，我給這個 `-v` 是不是命了一個名字，這個名字不是一個目錄，它只是一個名字沒有目錄。如果你一旦加了 `/`，這個 `/` 就代表絕對目錄開頭。現在我沒有加，那這就代表它是一個普通的卷名。

<sub>[04:14-05:10]</sub>
啟動起來了，我們再 `docker volume ls`，在這個位置我們就看到了，我們刚才寫的是不是給它指定了一個名字。匿名就是沒有名字只寫了內部的，而這邊還有個具名掛載。具名掛載它是怎麼掛載的？我們通過 `-v 卷名:容器內路徑` 去指定一個名字。

<sub>[05:10-06:14]</sub>
那這個卷在哪個位置？我們可以通過 `docker volume inspect juming-nginx` 來查看它的具體位置。你就可以看到對應的目錄，這就是我們掛載的具體目錄地址。所有 Docker 容器內的卷，在沒有指定目錄的情況下，都是在 `/var/lib/docker/volumes/` 目錄下。

<sub>[06:14-07:00]</sub>
我們可以去看一下，`cd /var/lib/docker`，Docker 裡面的東西都在這裡——Docker build 的文件、Docker 卷掛載的文件、Docker swarm 的文件、Docker 的插件、網絡配置、鏡像、容器。那現在我們用的卷就在 `volumes` 下面，`ls` 看一下，剛才所有的卷是不是都在這個位置。我們剛才測試的 juming-nginx 也在，進去看一下有個 `_data`，裡面就是 Nginx 的配置文件。

<sub>[07:00-08:10]</sub>
所以說我們在這邊通過具名掛載的時候就非常好去查名字。通過具名掛載可以方便的找到我們的卷，這個也是我們大多數情況在使用的，就是具名掛載。不建議大家使用匿名掛載。

<sub>[08:10-09:00]</sub>
那怎麼區分它是匿名掛載還是具名掛載？非常簡單。第一種是 `-v 容器內路徑`，沒有寫容器外的，它就會在 Docker volumes 下面隨機生成一個，這是匿名掛載。`-v 卷名:容器內路徑`，這個卷名不帶路徑 `/` 的，就是具名掛載。`-v /宿主機路徑:容器內路徑`，宿主機路徑一般都是通過 `/` 開頭的，這是指定路徑掛載。

<sub>[09:00-10:22]</sub>
然後拓展一下，你們經常會看到這樣的東西，比如 `-v 容器內路徑:ro` 或者 `:rw`。`ro` 就是 read-only 只讀，`rw` 就是 read-write 可讀可寫。一旦你設置成只讀了，我們的容器就改不了這個權限了。假設我們把這個文件掛載出來了，在容器裡面我們就不能去操作了。`ro` 只能從外部改變，就不能從內部改變了。默認的話就是 `rw` 可讀可寫。RO 就說明這個路徑只能通過宿主機來操作，容器內部是無法操作的。

### P24：數據卷之 Dockerfile

<sub>[00:00-00:13]</sub>
我們來繼續，這個地方就是咱們的方式二。之前的話我們是不是自己去做的，那現在的話我們通過 Dockerfile 來做。

<sub>[00:13-00:45]</sub>
很多小夥伴就好奇了，Dockerfile 是什麼？Dockerfile 就是用來構建咱們 Docker 鏡像的構建文件。就是 Docker 鏡像是怎麼來的，我們就可以通過 Dockerfile 來進行構建。我們之前是不是通過 `commit` 生成過，那現在用第二種方式。它其實就是一段命令腳本，我們只需要去寫一個 Dockerfile 的命令腳本然後執行一下就可以了。

<sub>[00:45-01:15]</sub>
我們現在先體驗一下，我們自己來做一個鏡像玩一下，體驗一下咱們的 Dockerfile。我們回到 home 目錄下，`mkdir docker-test-volume`，我就把我們的測試寫到這個位置。

<sub>[01:15-02:24]</sub>
我們進到 docker-test-volume，`pwd` 看一下當前目錄。我們去寫一個文件，這個文件的名字你們就可以叫 Dockerfile，比如說我寫 Dockerfile1。這個文件裡面寫一堆腳本，通過這個腳本可以生成鏡像。鏡像是一層一層的，所以說這個腳本就是一個一個的命令，每一個命令都是一層。

<sub>[02:24-03:46]</sub>
首先我們要去做一個鏡像，它這個鏡像肯定得跑在一個服務器上。那現在我們就可以寫一下，比如說當前我們這個鏡像 `FROM centos` 以 CentOS 為基礎。有了這個基礎之後，我們就可以在裡面去掛載卷了，通過 `VOLUME` 來掛載，比如說 `VOLUME ["volume01", "volume02"]`。然後我們想確定它打成功了，寫一個 `CMD echo "----end----"`，構建完打印這個消息。構建完了之後默認走 `/bin/bash`。

<sub>[03:46-05:00]</sub>
OK，我們這個 Dockerfile 就寫了這麼幾句話。保存之後，我們來把它生成。通過 `docker build` 來生成——`docker build -f dockerfile1 -t kuangshen/centos:1.0 .`。`-f` 指的是 Dockerfile 的地址，`-t` 是 target 要生成的鏡像名字，最後有個 `.` 別忘了。

<sub>[05:00-07:46]</sub>
構建的時候它是一步一步完成的。第一步就是 `FROM centos` 給我們一個基礎的鏡像，第二步通過 `VOLUME` 進行掛載，第三步寫出了我們的 CMD 命令，然後進入到 `/bin/bash`。生成完之後 `docker images` 看一下，我們自己的鏡像就生成完了，這個地方有個 kuangshen/centos 1.0 版的。

<sub>[07:46-09:15]</sub>
Dockerfile 建議大家文件名字就用 Dockerfile。然後裡面的格式分為指令跟參數，指令都是大寫的。每一步就是我們的一層一層的概念，這裡的每個命令就是鏡像的一層。

<sub>[09:15-10:10]</sub>
我們來啟動一下自己寫的容器，`docker run -it kuangshen/centos:1.0 /bin/bash`。進來之後 `ls` 看一下，發現了一個好玩的東西，我們自己剛才掛載的兩個目錄 volume01 和 volume02 是不是生成在這個地方了。這個目錄就是我們生成鏡像的時候自動掛載的數據卷目錄。

<sub>[10:10-11:14]</sub>
這個卷一定和外部有一個同步的目錄。按照剛才的路徑來看，這個應該是匿名掛載，因為我們什麼都沒寫只寫了容器內的目錄。那我們在 volume01 裡面 touch 一個 `container.txt` 文件。

<sub>[11:14-12:14]</sub>
退出容器後，`docker ps` 查看容器，用 `docker inspect` 查看容器信息。往上找 Mounts，通過 Dockerfile 生成的目錄也直接掛載出來了，對應的容器外的目錄在 `/var/lib/docker/volumes/` 下面生成的一個隨機目錄裡面。

<sub>[12:14-13:15]</sub>
我們 `cd` 進入那個目錄看一下，`container.txt` 果然同步成功了。所以通過這種方式我們也能夠進行卷掛載。這種方式我們未來使用的十分多，因為我們通常會構建自己的鏡像。假設構建鏡像的時候沒有掛載卷，那就要通過 `-v` 手動掛載。一般是 `-v 卷名:容器內路徑` 的具名掛載方式。

### P25：數據卷容器

<sub>[00:00-00:11]</sub>
我們再來看一下，我們來把這個容器數據卷給大家說一下，說白了就是我們要容器跟容器之間同步。剛才我們說到一個問題，就是多個 MySQL 的數據到底該如何同步的問題。

<sub>[00:11-01:05]</sub>
正常情況下，比如說這個地方有一個子容器，這邊有一個父容器，比如說我們把它叫做 centos01。那現在的話我還需要一個容器去掛載它，它們兩個要實現數據同步。那這個命令就是 `--volumes-from`，通過這樣的方式我們就可以跟它同步上了。

<sub>[01:05-01:40]</sub>
這邊這個容器我們一般把它稱為父容器，就是誰去掛載了誰那麼它就稱之為父容器。它一旦掛載這個父容器，centos02 跟 centos01 裡面的同步的數據卷內容就是同步的了。而被掛載的這個所謂的父容器，它就叫數據卷容器。就是它是一個容器但是它有一個掛載的功能，我們可以通過它來讓別人去共享一些內容，或者兩個容器或者多個容器之間實現數據共享。

<sub>[01:40-03:05]</sub>
OK 我們來測一下。啟動三個容器來測試，還是通過我們剛才自己寫的鏡像啟動。`docker run -it --name docker01 kuangshen/centos:1.0`，把它啟動起來，裡面有我們掛載的 volume01 和 volume02 目錄。啟動了 docker01 之後，Ctrl+P+Q 不退出，再創建一個 docker02。

<sub>[03:05-04:20]</sub>
`docker run -it --name docker02 --volumes-from docker01 kuangshen/centos:1.0`。這個 `--volumes-from` 就是讓 docker02 繼承 docker01，它只要跟 docker01 綁定上，通過 `--volumes-from`，裡面的數據就會同步。啟動起來了，`ls -l` 看一下，也有 volume01 和 volume02，沒有任何問題。

<sub>[04:20-05:30]</sub>
我們現在在 docker01 裡面去加一個文件，看 docker02 裡面會不會生成。`docker attach docker01` 進入容器，`cd volume01`，`touch docker01` 創建一個文件。然後在 docker02 裡面，`cd volume01`，`ls` 查看一下，看到了吧！docker01 容器創建的數據同步到了 docker02 上面。

<sub>[05:30-07:20]</sub>
那我們再來一個 docker03 也掛 docker01，`docker run -it --name docker03 --volumes-from docker01 kuangshen/centos:1.0`。docker03 也有數據卷，`cd volume01`，`ls -l` 查看，docker01 在這裡面。我在 docker03 裡面再創建一個 `touch docker03`，然後在 docker01 裡面 `ls -l`，01 這邊是不是也同步過來了。所以說我們之後只要通過這麼創建的，容器之間的數據都是可以共享的。只要通過 `--volumes-from` 我們就可以實現容器間的數據共享。

<sub>[07:20-08:49]</sub>
那現在有同學問了，我把 docker01 停掉或者刪了會怎麼樣？`docker ps` 看一下，把 docker01 停了，甚至把它刪了，`docker rm -f docker01`，彻底沒了。那你們說 docker02 跟 docker03 的數據還在不在？`ls` 查看一下，文件是不是依舊在！

<sub>[08:49-10:04]</sub>
所以說大家可以做一個測試來理解，就數據共享你不應該是容器丟了就沒了，只要有一個容器還在用這個數據它就不會丟失。容器之間它是一種拷貝的概念——雙向拷貝的概念。假設我把 docker01 咔嚓了，但是 docker02 跟 docker03 是不是已經把它裡面的東西拷貝出來了。只要你還活著它就備份過去。

<sub>[10:04-11:24]</sub>
解釋一下，我把這些東西都刪了，再來個 docker04 還是一樣的可以去訪問的。這就是我們的容器數據卷、數據卷容器。我們一般來用什麼？比如說多個 MySQL 實現數據共享、或者 Redis 實現數據共享，都是可以的。

<sub>[11:24-12:46]</sub>
比如說我們啟動了第一個 mysql01，現在再啟動一個 mysql02，`--volumes-from mysql01`，通過同一個鏡像啟動，它們的掛載目錄就是相同的，就可以實現兩個容器數據同步。

<sub>[12:46-15:00]</sub>
容器之間我們可以做一些配置信息的傳遞，比如你們做集群經常要把集群的文件拷貝到多個服務器上面，通過容器可以直接共享就可以了。數據卷的生命周期一直持續到沒有容器使用為止。只要一旦能夠複製它就可以保證高可用。但是記住，一旦你持久化到了本地，也就是你的第一個父容器通過 `-v` 把它持久化到了本地，那本地的數據是不會刪除的。這就是容器數據卷的核心精髓。
