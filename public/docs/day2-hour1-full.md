# Day 2 第一小時：環境一致性問題與容器技術

---

## 一、開場（5 分鐘）

各位同學好，歡迎來到第二天的課程。

昨天我們學了 Linux 基礎操作，今天開始進入這門課的核心主題——容器技術。

在正式開始之前，我想先問大家一個問題：

你們有沒有遇過這種情況——你在自己的電腦上寫好一個程式，測試過了，跑起來完全沒問題。結果把程式碼丟到伺服器上，或是給同事跑，就壞掉了？

有遇過的舉個手。

好，我看到不少人舉手。這個問題在業界有一個非常有名的梗，叫做「It works on my machine」——在我的電腦上明明可以跑啊！

這句話已經變成工程師之間的經典笑話了。網路上還有人做了一個惡搞的認證標章，上面就寫著「Certified: Works on My Machine」，意思是「認證：在我的電腦上可以跑」。

這個問題看起來好笑，但實際上造成了非常多的困擾。開發團隊和維運團隊之間的衝突，有很大一部分就是因為這個問題。

今天這堂課，我們就是要來徹底解決這個問題。我們要學習的容器技術，就是目前業界公認最好的解決方案。

---

## 二、環境一致性問題（15 分鐘）

### 2.1 什麼是執行環境

首先，我們要理解一個概念：程式不是獨立存在的。

當你寫好一個程式，這個程式要能夠執行，它需要很多東西的配合。這些東西加在一起，我們稱之為「執行環境」。

執行環境包含哪些東西呢？讓我一個一個說明：

**第一，作業系統。**

你的程式是跑在 Windows 上還是 Linux 上？如果是 Linux，是 Ubuntu 還是 CentOS？版本是多少？Ubuntu 20.04 和 Ubuntu 22.04 雖然都是 Ubuntu，但有些細節是不一樣的。

**第二，程式語言的執行環境。**

如果你寫的是 Python 程式，你的電腦上需要裝 Python。但 Python 有很多版本——3.8、3.9、3.10、3.11、3.12。不同版本之間，有些語法是不相容的。Python 3.10 加入的 match-case 語法，在 Python 3.9 上就沒辦法跑。

如果你寫的是 Java 程式，你需要 JDK。JDK 8 和 JDK 17 的差異更大，有些 API 在新版被移除了，有些新功能在舊版不支援。

**第三，相依套件。**

現代軟體開發不可能從零開始寫所有東西，我們會用很多第三方套件。一個 Python 專案可能用到 Django、requests、numpy、pandas 等等幾十個套件。每個套件都有自己的版本，而且套件之間還有相依關係——A 套件需要 B 套件 2.0 以上，C 套件需要 B 套件 1.5 以下，這就衝突了。

**第四，系統層級的函式庫。**

有些程式需要系統層級的函式庫支援。比如說，Python 的 cryptography 套件需要 OpenSSL；一些圖像處理的套件需要 libpng、libjpeg；機器學習的套件可能需要 CUDA。這些東西的版本也都要對得上。

**第五，設定檔和環境變數。**

你的程式可能會讀取設定檔，這些設定檔放在哪裡？格式是什麼？內容是什麼？還有環境變數，像是 PATH、JAVA_HOME、資料庫連線字串等等。

這五個層面加起來，就是你的程式的「執行環境」。

現在問題來了：你的開發電腦上的執行環境，和伺服器上的執行環境，一樣嗎？

### 2.2 環境差異的真實案例

讓我舉幾個真實的案例，都是我自己或身邊同事遇過的。

**案例一：Python 版本不一致**

有一次，我在 Mac 上用 Python 3.11 開發了一個資料處理的腳本。這個腳本用到了一個很方便的語法，叫做 structural pattern matching，就是 match-case。我在本機測試，跑得很順。

結果部署到公司的 Linux 伺服器上，直接報 SyntaxError。為什麼？因為伺服器上的 Python 是 3.8，而 match-case 是 Python 3.10 才加入的語法。

**案例二：套件版本衝突**

另一個案例，我有個同事開發了一個 Django 網站。他用的是 Django 4.2。專案裡面有一個 requirements.txt，照理說照著裝就沒問題。

結果維運同事在伺服器上執行 pip install -r requirements.txt，裝好之後一跑就報錯。查了半天，發現是因為伺服器之前已經裝過其他專案需要的套件，有版本衝突。雖然 requirements.txt 裡寫了 Django==4.2，但 Django 4.2 需要的某個底層套件被其他專案綁定在舊版本，升不上去。

**案例三：系統函式庫缺失**

還有一個案例更經典。有個專案需要用 Python 的 Pillow 套件來處理圖片。開發者在 Mac 上一切正常。部署到 Linux 伺服器上，Pillow 裝不起來，報錯說找不到 libjpeg。

原來 Mac 上用 Homebrew 裝 Python 的時候，已經自動把這些圖像處理的函式庫裝好了。但 Linux 伺服器是最小化安裝，這些函式庫都沒有。

**案例四：設定檔路徑問題**

還有一種很隱蔽的問題。程式裡面寫死了設定檔的路徑，比如 /Users/john/config/settings.json。在開發者的電腦上當然存在這個檔案，但伺服器上哪來的 /Users/john？

這些案例都指向同一個問題：**開發環境和部署環境不一致**。

### 2.3 這個問題有多嚴重

你可能會想，這個問題很難解決嗎？不就是把環境弄得一樣就好了嗎？

理論上是這樣，但實際操作起來非常困難。

**首先，環境的複雜度超乎想像。**

一個中型的專案，可能有上百個直接和間接的相依套件。每個套件都有版本號，這些版本號之間的組合可能有幾萬種。要精確重現一個環境，你需要記錄所有這些細節。

**其次，環境會隨時間改變。**

今天你部署成功了，一個月後重新部署，可能就失敗了。為什麼？因為某個套件釋出了新版本，新版本有 breaking change。或者作業系統自動更新了，覆蓋了你之前的設定。

**第三，不同專案的需求會衝突。**

一台伺服器上可能跑好幾個專案。A 專案需要 Python 3.8，B 專案需要 Python 3.11。A 專案需要 Django 2.x，B 專案需要 Django 4.x。這些需求互相衝突，你怎麼辦？

**第四，團隊成員的環境各不相同。**

一個團隊五個工程師，可能用三種不同的作業系統——Mac、Windows、Linux。每個人的環境設定都不一樣。新人加入團隊，光是把開發環境設定好，可能就要花一兩天。

這個問題每天都在消耗工程師的時間和精力。據統計，工程師平均每週花 4 到 8 小時在處理環境問題上。這是非常驚人的浪費。

### 2.4 傳統的解決方案

在容器技術出現之前，我們怎麼解決這個問題呢？主要有兩種方法。

**方法一：寫一份超級詳細的安裝文件。**

把安裝步驟一步一步寫下來。先裝這個、再裝那個、這個設定檔要這樣改、那個環境變數要這樣設......

這個方法有幾個問題：

1. **文件會過時。** 軟體版本一直在更新，文件跟不上。三個月前寫的文件，現在照著做可能就不對了。

2. **文件會漏寫。** 寫文件的人覺得理所當然的東西，可能就不會寫出來。「誰不知道要先裝 Python 啊？」但新人可能真的不知道要裝哪個版本。

3. **不同人照著做結果不同。** 即使是同一份文件，不同人執行的時候，因為之前的環境狀態不同，結果可能也不同。

**方法二：使用虛擬機。**

把整個作業系統和應用程式一起打包成一個虛擬機映像檔。要部署的時候，把這個映像檔丟到伺服器上，開一台虛擬機跑起來。

這個方法確實可以解決環境一致性的問題，因為你把整個作業系統都打包了，當然一致。

但虛擬機有自己的問題：

1. **太肥。** 一個虛擬機映像檔動輒就是幾十 GB。你要傳輸、要儲存，都是負擔。

2. **太慢。** 開一台虛擬機要好幾分鐘。如果你需要頻繁地啟動和關閉，這個時間就很可觀。

3. **太吃資源。** 每一台虛擬機裡面都跑一個完整的作業系統，這個作業系統本身就要佔用 CPU 和記憶體。一台實體伺服器可能只能跑幾台虛擬機。

4. **授權費用。** 如果虛擬機裡面跑的是 Windows，每一台都要授權費。

所以虛擬機雖然能解決環境一致性的問題，但它引入了新的問題。我們需要一個更好的解決方案。

---

## 三、容器技術的誕生（20 分鐘）

### 3.1 容器的基本概念

好，現在我們來講容器。

容器是一種輕量級的虛擬化技術。它的核心思想是：**我們不需要模擬整個作業系統，我們只需要把應用程式和它的相依套件打包在一起就好。**

讓我解釋一下這是什麼意思。

當你跑一個應用程式的時候，這個應用程式需要什麼？它需要一些函式庫、一些執行檔、一些設定檔。但它不需要整個作業系統。作業系統裡面有很多東西是應用程式用不到的——什麼桌面環境、什麼印表機驅動、什麼藍牙支援，對一個跑在伺服器上的 Web 應用來說，這些都是多餘的。

容器的做法是：只打包應用程式需要的東西，其他的一律不要。

那作業系統核心呢？容器不打包核心，它共用主機的核心。

這是什麼意思？

作業系統分成兩個部分：核心（Kernel）和使用者空間（User Space）。核心負責管理硬體、分配資源、排程程序等底層工作。使用者空間是應用程式跑的地方。

虛擬機是在虛擬化層面模擬整個硬體，所以每個虛擬機裡面都要跑一個完整的作業系統，包含核心。

容器不模擬硬體，它直接使用主機的核心。每個容器只有自己的使用者空間，裡面包含應用程式和它需要的函式庫。

這樣做的好處是：

1. **輕量。** 一個容器可能只有幾十 MB，而不是幾十 GB。
2. **快速。** 啟動一個容器只要幾秒鐘，而不是幾分鐘。
3. **省資源。** 容器不需要為核心預留資源，所以同一台機器可以跑更多容器。

### 3.2 容器的技術基礎

容器聽起來很神奇，它是怎麼做到的呢？

容器利用了 Linux 核心的兩個重要功能：Namespace 和 Cgroups。

**Namespace（命名空間）**

Namespace 的作用是隔離。它讓每個容器有自己獨立的視野，看不到其他容器和主機上的東西。

Linux 提供了好幾種 Namespace：

- **PID Namespace**：程序 ID 隔離。在容器裡面，程序的 PID 從 1 開始編號，跟主機上的 PID 完全獨立。容器裡面看不到主機上的其他程序。

- **Network Namespace**：網路隔離。每個容器有自己的網路介面、IP 位址、路由表。容器之間的網路是隔離的。

- **Mount Namespace**：檔案系統隔離。每個容器有自己的檔案系統視野。容器裡面看到的根目錄，其實是主機上的某個子目錄。

- **UTS Namespace**：主機名稱隔離。每個容器可以有自己的 hostname。

- **User Namespace**：使用者隔離。容器裡面的 root 使用者，不一定是主機上的 root。

- **IPC Namespace**：行程間通訊隔離。容器之間不能直接透過共享記憶體等方式通訊。

透過這些 Namespace，一個容器看起來就像一個獨立的作業系統——它有自己的程序、自己的網路、自己的檔案系統。但實際上，它只是主機上的一組程序，被 Namespace 隔離起來而已。

**Cgroups（控制群組）**

Cgroups 的作用是限制資源。它讓你可以規定一個容器最多能用多少 CPU、多少記憶體、多少磁碟 I/O。

如果沒有 Cgroups，一個容器可能會把整台機器的資源吃光，影響到其他容器。有了 Cgroups，你可以說：這個容器最多只能用 2 個 CPU 核心、4GB 記憶體。超過這個限制就會被節流或被終止。

這兩個功能——Namespace 做隔離、Cgroups 做限制——加在一起，就是容器的技術基礎。

要注意的是，這兩個功能都是 Linux 核心原生支援的，不需要任何額外的虛擬化層。這就是為什麼容器的效能幾乎跟直接跑在主機上一樣——因為它本來就是跑在主機上，只是被隔離起來而已。

### 3.3 容器 vs 虛擬機：架構比較

讓我們來比較一下容器和虛擬機的架構。

**虛擬機的架構**

最底層是硬體。硬體上面跑一個作業系統，這是主機的作業系統（Host OS）。在 Host OS 上面有一個 Hypervisor，這是虛擬化軟體，像是 VMware、VirtualBox、KVM 這些。

Hypervisor 的工作是模擬硬體。它模擬出來的虛擬硬體上面，各自跑著一個完整的作業系統，叫做客戶作業系統（Guest OS）。每個 Guest OS 裡面，才跑著應用程式。

所以架構是這樣的：

```
┌─────────────┐ ┌─────────────┐ ┌─────────────┐
│    App A    │ │    App B    │ │    App C    │
├─────────────┤ ├─────────────┤ ├─────────────┤
│  Libraries  │ │  Libraries  │ │  Libraries  │
├─────────────┤ ├─────────────┤ ├─────────────┤
│  Guest OS   │ │  Guest OS   │ │  Guest OS   │
└─────────────┘ └─────────────┘ └─────────────┘
┌─────────────────────────────────────────────┐
│               Hypervisor                     │
├─────────────────────────────────────────────┤
│                Host OS                       │
├─────────────────────────────────────────────┤
│               Hardware                       │
└─────────────────────────────────────────────┘
```

你看，每一個虛擬機裡面都有一個 Guest OS。這個 Guest OS 可能是 Ubuntu、CentOS、Windows Server。光是這個 Guest OS，可能就要佔掉 1-2GB 的記憶體、幾十 GB 的硬碟空間。

**容器的架構**

容器的架構簡單得多。

最底層一樣是硬體，硬體上面一樣有作業系統。但是沒有 Hypervisor，取而代之的是容器引擎（Container Engine），最常見的就是 Docker。

容器引擎上面直接就是容器，每個容器裡面只有應用程式和它需要的函式庫，沒有 Guest OS。

```
┌─────────────┐ ┌─────────────┐ ┌─────────────┐
│    App A    │ │    App B    │ │    App C    │
├─────────────┤ ├─────────────┤ ├─────────────┤
│  Libraries  │ │  Libraries  │ │  Libraries  │
└─────────────┘ └─────────────┘ └─────────────┘
┌─────────────────────────────────────────────┐
│           Container Engine (Docker)          │
├─────────────────────────────────────────────┤
│                Host OS                       │
├─────────────────────────────────────────────┤
│               Hardware                       │
└─────────────────────────────────────────────┘
```

所有容器共用主機的核心。這就是為什麼容器這麼輕量——它沒有多餘的作業系統開銷。

### 3.4 具體數據比較

讓我給你們一些具體的數據：

**啟動時間**

- 虛擬機：分鐘級。開一台虛擬機，要載入整個作業系統，通常需要 1-3 分鐘。
- 容器：秒級。啟動一個容器，通常在 1-2 秒內完成，有時候甚至只要幾百毫秒。

**映像大小**

- 虛擬機映像：GB 級。一個包含作業系統的虛擬機映像，通常是 10-50 GB。
- 容器映像：MB 級。一個典型的應用程式容器映像，可能只有 50-200 MB。當然複雜的應用可能會大一些，但通常不會超過 1-2 GB。

**記憶體佔用**

- 虛擬機：需要為 Guest OS 預留記憶體。一個跑 Ubuntu 的虛擬機，光是作業系統就要 500MB-1GB。
- 容器：只佔用應用程式需要的記憶體。沒有作業系統的開銷。

**效能**

- 虛擬機：有一定的效能損耗，因為要經過虛擬化層。CPU 密集型任務可能會有 5-10% 的損耗。
- 容器：幾乎沒有效能損耗。因為容器裡的程序直接跑在主機核心上，沒有虛擬化層。

**單機可運行數量**

- 虛擬機：一台伺服器通常跑 10-20 個虛擬機就很吃力了。
- 容器：一台伺服器可以跑幾百甚至上千個容器（當然要看應用程式的資源需求）。

**隔離程度**

- 虛擬機：完全隔離。每個虛擬機有自己的核心，安全性很高。
- 容器：程序級隔離。所有容器共用核心，如果核心有漏洞，可能會影響到所有容器。

從這個比較可以看出，容器在資源效率上遠勝虛擬機。虛擬機的優勢是隔離程度更高，適合對安全性要求極高的場景。但對於一般的應用程式部署，容器已經足夠安全，而且效率高得多。

### 3.5 容器不是新技術

我要特別說明一點：容器不是什麼全新的技術。

早在 1979 年，Unix 就有 chroot 這個系統呼叫，可以改變程序的根目錄，這是最早的隔離概念。

2000 年，FreeBSD 推出了 Jails，這是比較完整的容器化技術。

2006 年，Google 開發了 Process Containers（後來改名叫 Cgroups），用來限制和隔離程序的資源使用。

2008 年，LXC（Linux Containers）發布，這是 Linux 上第一個完整的容器實作。

但是這些技術都有一個問題：太難用了。你要自己處理 Namespace、Cgroups、檔案系統，太複雜了。只有對 Linux 核心很熟悉的專家才會用。

直到 2013 年，Docker 出現了。Docker 把容器技術包裝得非常簡單，普通的開發者也能輕鬆使用。這才讓容器技術真正普及開來。

---

## 四、Docker 簡介（15 分鐘）

### 4.1 Docker 是什麼

好，現在我們來正式介紹 Docker。

Docker 是一個開源的容器化平台，在 2013 年由一家叫做 dotCloud 的公司開源釋出。這家公司後來直接改名叫 Docker Inc.，可見 Docker 這個產品有多成功。

Docker 是用 Go 語言開發的，遵循 Apache 2.0 開源協議。

如果要用一句話來描述 Docker，那就是：

**Docker 讓你可以把應用程式和它的執行環境打包成一個標準化的單元，這個單元可以在任何有 Docker 的機器上運行，而且保證行為一致。**

這就解決了我們開頭講的「It works on my machine」問題。現在你不只是把程式碼給別人，你是把整個執行環境一起給別人。

### 4.2 Docker 的核心概念

使用 Docker，你需要認識三個核心概念：Image、Container、Registry。

**Image（映像檔）**

Image 是一個唯讀的模板，包含了運行應用程式所需的一切：程式碼、執行環境、函式庫、環境變數、設定檔。

你可以把 Image 想像成一張光碟。光碟裡的內容是固定的、不可修改的。你可以用這張光碟在很多台電腦上安裝系統，每台電腦安裝出來的初始狀態都一樣。

Image 是分層的，這個我們之後會詳細講。現在你只要知道 Image 是一個靜態的、唯讀的模板。

**Container（容器）**

Container 是 Image 的執行實例。當你「運行」一個 Image，就會產生一個 Container。

繼續用光碟的比喻：如果 Image 是光碟，那 Container 就是用這張光碟安裝好的電腦。電腦開機後，你可以在上面工作、存檔案、裝軟體。這些改動發生在這台電腦上，不會影響到光碟本身。

一個 Image 可以產生很多個 Container，每個 Container 都是獨立的。就像你用同一張光碟可以裝很多台電腦，每台電腦之後的使用是獨立的。

Container 可以被啟動、停止、刪除、暫停。Container 裡面可以執行應用程式、處理請求、存取資料。

**Registry（倉庫）**

Registry 是存放 Image 的地方。

最大的公開 Registry 叫做 Docker Hub，就像 GitHub 是存程式碼的地方，Docker Hub 是存 Image 的地方。

Docker Hub 上有幾十萬個 Image，包括各種官方提供的 Image：Ubuntu、CentOS、Nginx、MySQL、Redis、Python、Node.js、Java......基本上你想得到的軟體都有現成的 Image。

除了 Docker Hub，你也可以架設私有的 Registry，用來存放公司內部的 Image。

### 4.3 Docker 如何解決環境一致性問題

回到我們一開始講的問題——「It works on my machine」。

有了 Docker 之後，工作流程變成這樣：

**第一步：開發者在自己的電腦上建立一個 Image。**

這個 Image 裡面包含了應用程式、所有的相依套件、所有的設定。開發者在本機測試這個 Image，確認一切正常。

**第二步：開發者把 Image 推送到 Registry。**

可以是 Docker Hub，也可以是公司的私有 Registry。

**第三步：維運人員從 Registry 把 Image 拉下來，直接運行。**

不需要安裝任何東西，不需要設定任何環境，直接 docker run 就可以跑。

因為 Image 裡面已經包含了所有需要的東西，所以不管在哪台機器上運行，環境都是一模一樣的。

這就是 Docker 解決環境一致性問題的方式：**不只是分享程式碼，而是連同整個執行環境一起分享。**

開發者說「在我電腦上可以跑」，現在可以直接把那個環境打包給你。你不需要自己去重建那個環境，直接用開發者打包好的環境就行了。

### 4.4 Docker 的架構

Docker 採用的是客戶端-伺服器（Client-Server）架構。

**Docker Client**

Docker Client 是你跟 Docker 互動的介面。當你在終端機輸入 docker run、docker build 這些命令，就是在使用 Docker Client。

Docker Client 本身不做事，它把你的命令送給 Docker Daemon 去執行。

**Docker Daemon（dockerd）**

Docker Daemon 是在背景運行的服務，它才是真正執行工作的。

Docker Daemon 負責：
- 管理 Image：下載、建立、儲存
- 管理 Container：啟動、停止、刪除
- 管理 Network：建立網路、連接容器
- 管理 Volume：管理資料儲存

Docker Client 和 Docker Daemon 之間透過 REST API 溝通。它們可以跑在同一台機器上，也可以跑在不同的機器上。

**Docker Registry**

如前面所說，Registry 是存放 Image 的地方。Docker Daemon 需要 Image 的時候，會從 Registry 下載；你要分享 Image 的時候，會把 Image 上傳到 Registry。

### 4.5 Docker 的生態系統

Docker 不只是一個容器引擎，它已經發展成一個完整的生態系統。

**Docker Desktop**

在 Windows 和 Mac 上，你不能直接跑 Docker（因為 Docker 需要 Linux 核心）。Docker Desktop 是一個桌面應用程式，它內建了一個輕量級的 Linux 虛擬機，讓你可以在 Windows 和 Mac 上使用 Docker。

**Docker Compose**

當你的應用程式由多個容器組成（例如一個 Web 伺服器 + 一個資料庫），你需要一種方式來定義和管理這些容器。Docker Compose 就是做這件事的，它讓你用一個 YAML 檔案定義多容器應用。

**Docker Swarm**

當你需要在多台機器上運行容器，Docker Swarm 提供了基本的容器編排功能。不過現在業界更常用的是 Kubernetes。

**Docker Hub**

最大的公開 Image 倉庫，上面有官方和社群貢獻的各種 Image。

而我們這門課之後會學到 Kubernetes，它是另一個容器編排工具，目前已經成為業界的標準。Docker 負責單機上的容器管理，Kubernetes 負責跨多台機器的容器編排。

---

## 五、Podman 簡介（5 分鐘）

在結束這堂課之前，我要簡單提一下 Podman。

Podman 是 Red Hat 推出的容器引擎，它跟 Docker 的功能幾乎一樣，指令也幾乎一樣。你可以把 docker 命令直接換成 podman，大部分情況下都能用。

Podman 和 Docker 最大的差別是：Podman 沒有 Daemon。

Docker 需要一個持續運行的 Docker Daemon，而 Podman 是 daemonless 的。每次你執行 podman 命令，它直接處理，不需要背景服務。

這有什麼好處呢？

1. **安全性**：Docker Daemon 以 root 權限運行，這是一個潛在的安全風險。Podman 可以完全以普通使用者身份運行，更安全。

2. **資源**：沒有 Daemon 就不需要額外的背景程序佔用資源。

3. **系統整合**：Podman 跟 systemd 整合得更好，可以用 systemd 來管理容器。

在 RHEL、CentOS、Fedora 這些 Red Hat 系的發行版上，Podman 是預設的容器引擎。

不過，Docker 的生態系統還是最完整的，Docker Hub 上的 Image 最多，社群資源最豐富。所以我們這門課主要還是用 Docker，但你要知道 Podman 的存在，以後可能會遇到。

---

## 六、本堂課小結（5 分鐘）

好，讓我們來總結一下今天第一個小時學到的內容。

**環境一致性問題**

- 應用程式依賴執行環境：作業系統、語言版本、套件、設定
- 開發環境和部署環境不一致，導致「在我電腦上可以跑」的困境
- 傳統解決方案：文件（會過時）、虛擬機（太肥）

**容器技術**

- 輕量級虛擬化，只打包應用程式和相依套件，不打包核心
- 利用 Linux 的 Namespace（隔離）和 Cgroups（限制資源）
- 比虛擬機更輕、更快、更省資源

**Docker**

- 最流行的容器化平台，讓容器技術變得簡單好用
- 核心三元素：Image（映像檔）、Container（容器）、Registry（倉庫）
- Client-Server 架構：Docker Client、Docker Daemon、Docker Registry

下一個小時，我們會更深入地了解 Docker 的架構和工作原理。第三個小時，我們就要開始動手安裝 Docker，正式進入實作環節。

有任何問題嗎？

（預留問答時間）

---

## 板書 / PPT 建議

1. 「It works on my machine」梗圖
2. 執行環境五層次圖（OS → 語言 → 套件 → 函式庫 → 設定）
3. 容器 vs 虛擬機架構對比圖
4. 數據比較表（啟動時間、大小、效能、數量）
5. Docker 三元素關係圖（Image → Container, Registry ↔ Image）
6. Docker 架構圖（Client → Daemon → Registry）

---

## 📺 補充教材：狂神說 Docker

> 以下內容來自【狂神說Java】Docker 教程系列，作為本節的補充參考資料。

### P01：Docker 學習大綱

<sub>[00:00-00:08]</sub>
OK，那麼從今天開始呢，我們就正式開始講解 Docker 了。在講解 Docker 之前呢，我們先來聊一下我們該如何去學習以及 Docker 要學什麼東西。很多人啊，學 Docker 的時候他就覺得，這個東西我學會來用它去運行一些環境就好了。但是的話，這是 Docker 的入門啊，它詳細的東西，非常非常的多。

<sub>[00:19-00:43]</sub>
那在這個地方的話，先給大家去說一些東西，比如說我們的一個學前準備。學前準備，那就是你不可能直接什麼都不會，你就上來學 Docker，這是有點難的。你至少要會幾個基本的東西，第一個就是 Linux，你要懂一點點 Linux。第二個的話，你要懂一點點 Spring Boot，這是最好的，你不懂 Spring Boot 也可以學習啊。前提是你得會一點 Linux 基礎，這是一個必要的。

<sub>[00:43-00:56]</sub>
你要有一點 Linux 基礎，什麼叫 Linux 基礎呢，你會一些基本的命令，比如說 CD 切換命令、MKDIR 創建一些目錄，就是可以的了，所以說這個的上手的門檻並不高。然後 Docker 是一個什麼樣的技術呢，給大家說一下。

<sub>[00:57-01:18]</sub>
就是大家經常在問，什麼是 Docker，Docker 是個什麼樣的技術，那隨著我們的學習肯定會給大家揭開什麼是 Docker。那麼在這個地方呢，我們還是先來說一下 Docker 它的一個重要性啊，就是咱們現在為什麼要去學它，對不對。那我們到目前為止的話，已經把 Java 全棧差不多都走完了，前端後端，還有一些基本的常用開發，還有一些項目研究差不多都走完了。

<sub>[01:19-01:45]</sub>
那現在的話，我們就來到了咱們的一個 Docker 的學習。可以這麼去說，假設在 2020 年，你要去做一個 Java 開發工作，或者你想做一個高級工程師，這個東西是你必備的一個技能。如果你不會 Docker，那可能的話，你平時去裝一個集群，你裝起來都費勁，對不對。那現在的話，以前你必須要會 Spring，那現在的話你就必須要會 Docker，它的重要性就在這個地方。因為現在咱們比較流行，比如說開發運維、DevOps 等等。

<sub>[01:55-02:11]</sub>
那像這樣的一些東西的話，我們怎麼樣去給它進行學習呢。那在這個地方的話，今天我們大概會花大概兩到三天的這麼一個時間去學習，我們晚上從七點到十二點給大家進行分享的講解。然後當然的話，給大家說一下，我還是一個個人啊，不會任何機構站台，編程只是我的一個愛好。我就希望把我學到的東西給大家用我所謂理解的一些通俗的方式教給大家。

<sub>[02:33-02:51]</sub>
OK，那我們來看一下 Docker 的一個學習。那 Docker 學習的話，我們要學習什麼東西。很多人就是認為——給大家分享了一句話在這，就是三體裡面的，他說了這麼一句話，叫什麼「弱小和無知，不是生存的障礙，傲慢才是」。有的人啊，他拿到 Docker 之後，他會運行容器了，就是運行 Docker Run，他會運行了，然後會開一個端口了，他就會覺得他把 Docker 學會了。

<sub>[02:59-03:14]</sub>
但是這個東西根本就不叫學會啊，Docker 裡面要學的東西非常非常多。我們來說一下，首先第一個點，那我們首先要了解什麼是 Docker，對吧，Docker 裡面有哪些名詞，所以說 Docker 概說這是一定要要的。那聊完這些基本入門，我們肯定要去了解一下。

<sub>[03:15-03:44]</sub>
Docker 怎麼安裝，對不對，那安裝完之後我們怎麼去用。那安裝完之後我們就要去學跟 Linux 一樣，我們學完它，它主要是以命令操作，所以說我們要去學一下咱們的命令。這個命令的話就是大家非常常用的一些，比如說你們很多人學到的，比如說鏡像命令。那還有一些容器的命令，等等，對不對，或者一些常用的其他命令啊，就是關於操作相關的命令。那這樣的一些命令的話都是 Docker 的基礎，你會了它的話就好比你學了 Java 會了關鍵字就可以去寫代碼了，對不對。

<sub>[03:44-04:07]</sub>
然後再往下走呢，我們要去把 Docker 的鏡像詳細的說一下，因為這個東西是 Docker 裡面很核心的一個點，對不對。那聊了 Docker 鏡像之後不夠，我們這個 Docker 它跟我們的服務器還有咱們的數據是互相隔離的，我們要怎麼去做呢，所以說我們還要去研究咱們的一個容器的數據卷。那這個卷的技術也是非常非常牛的，而且在 Docker 裡面非常非常核心。

<sub>[04:07-04:28]</sub>
然後再往下的話，這個地方其實學完很多人就夠用了，就是很多人學 Docker 就覺得學到這，好像我就懂了。但是這個地方連 Docker 的入門都不算，能理解我的意思啊。這些東西不算入門，我們還要往下學。比如說我們寫完一個項目之後，我們要去把我們的微服務項目構建成一個鏡像，怎麼樣去構建。如何自己去做一個鏡像，對不對。

<sub>[04:28-04:51]</sub>
所以說我們還要學習 Dockerfile，Dockerfile 裡面的話也有非常多的難點，當然的話我會用通俗的方式給大家講。那到了這個位置還不夠，Docker 的話，它主要還基於網絡的，所以說我們還要去了解 Docker 的網絡原理。那在這個地方的話，我希望大家有一些計算機網絡原理的基礎啊，如果你沒有基礎的話，我也能給你講懂。但是的話可能就會吃力一點，也希望大家把我們之前的一些網絡相關的全部了解一下。

<sub>[04:51-05:10]</sub>
OK，那講完 Docker 網絡原理之後呢還不夠，我們還要往下面走，我們最重要把代碼微服務發到 Docker 裡面是吧。IDEA 怎麼去整合 Docker。OK，到這個地方很多人覺得，老師我覺得 Docker 到這個地方應該差不多了，對不對。那這些東西我只能把它稱之為什麼，稱之為單機版的 Docker。能理解我的意思啊，什麼叫單機版的 Docker 呢，就好比你們去寫一個項目，它是一個單體應用。

<sub>[05:15-05:43]</sub>
這根本是不符合咱們真正企業中的要求。企業中一般都是什麼，都是集群，對不對，那這個時候這個集群我們是不是要去學習怎麼樣處理啊。那所以說我們至少還要去學什麼呢，Docker Compose。來，我們要一次性去管理那麼多服務，假設你們寫一個項目，100 個服務，300 個運行環境，那你怎麼去一次性管理呢。你不可能一個一個啟動，所以說這種集群的編排管理也非常重要。那其實 Compose 的話還是比較簡單的，那再往下的話，你們還要去學集群的一些管理呢。Docker Swarm 也是必須要學的。

<sub>[05:47-06:15]</sub>
那市面上的話很多教程，關於這一塊的講解就非常非常的少了，上面這一塊的話講的還是比較多的，因為這個很基礎。那下面這個集群的話，一般工作中是必須要學習的。如果你只是一個學習的開發者的話，上面的就夠用了。那我們在這個地方第一波階段的話就給大家講到這個位置，然後關於集群的話，之後我們做項目的時候再給大家詳細的去分析講解。然後有的人剛才問了一下，K8S 會講嗎？K8S 的話也是會講到。Docker Swarm 的話，相當一個簡化版的 K8S。Kubernetes 的話我們之後還有個專題來單獨講。

<sub>[06:26-07:05]</sub>
因為這個對於咱們的運維來說的話，或者說你要從開發升級到一個高級開發，還有咱們現在的一些敏捷開發你必須要去做這些事情。那到了這個地方你會了集群管理，還會了容器編排之後還不夠。我們還要去看一下咱們公司裡面的 CI/CD，也就是所謂的持續集成跟持續部署它是怎麼做的。那比如說 Jenkins 的一個流水線，那這個的話在公司裡面是非常重要的。如果你們去上班的話，你們只會上面這點技術根本上不了班。下面這些東西才是考驗一個開發人員真正的功底。

<sub>[07:05-07:17]</sub>
那 OK，那這是我們 Docker 的一個學習路徑，我們整個課呢也就會這麼樣去講，當然裡面的點都會非常的多，我會詳細給大家去說。那在這個地方的話，再回過頭來給大家去說這句話，叫什麼意思呢。

<sub>[07:18-07:58]</sub>
就是我們學習的時候一定要放低自己的心態啊，就包括給大家分享課一樣的話，我也是在用一種很謙卑的心態，我甚至想給大家一塊去探討和交流，我認為這才是重要的。就好比我們去學習，最開始我們知道了一個東西，我們覺得這個世界就這麼小。那這時候你的知識體系是一個圓，對不對。那你能接觸到的外面的面積就這麼大，隨著你的知識體系越來越大，你的這個圓的外面積會越來越大。就是有一句話叫什麼呢，就是叫「知道的越多，不知道的就越多」。這個東西是一種學習的思想，就是一定要謙杯啊，所以說我覺得三體裡面這句話非常有感觸。

<sub>[08:02-09:09]</sub>
所以說我們一定要打好學習的心態，然後去學習。就是學習一下我是怎麼樣去學習的，你只有把這種思想學習完了，你才能夠在未來的學習中越來越快啊。所以說大家在學習的時候要學習什麼——我的思想。就是學習一下我是怎麼樣去學習他的，你只有把這種思想學習完了，你才能夠在未來的學習中越來越快。

<sub>[09:37-10:15]</sub>
OK，那還是在公屏上打出那句話，我們的學習座右銘就是——「即使再小的帆，也能遠航」。這個是我的一個真實的事件啊，其實我自己並不覺得我特別特別的厲害，但是我就是比較喜歡鑽研學習和分享。那這個就是我最喜歡的一個點了，所以說大家一定要把這句話也作為自己的一個思想。只要學不死，就往死裡學啊。

<sub>[10:26-10:41]</sub>
OK，這就是我們的一個 Docker 的開場給大家講解的所有東西，希望大家了解完 Docker 的學習路徑之後，也希望大家學前記得三連關注分享。OK，那我們就開始我們正式的一個學習啊。

### P02：聊聊 Docker 為什麼會出現

<sub>[00:00-00:14]</sub>
我們剛才已經給大家介紹了我們如何去學習 Docker 路線。我們就從 Docker 概述開始給大家講解。那第一個，首先我們要去聊一個點，Docker 為什麼會出現，然後它這個東西出現了，為什麼還這麼火。這是不是我們要去研究的，對不對。

<sub>[00:14-00:43]</sub>
那我們來想想大家在生活中有沒有遇到過這樣一些場景。比如說大家經常去做一款產品，你們可能是寫一個項目，對不對。你應該會經歷幾個階段，第一個就是你們要開發，第二個就是要上線。開發跟上線的話，你們經常會有兩套環境，是不是這樣的，兩套環境。那這個環境部署的話，最開始時候是非常麻煩的，不知道大家有沒有體驗過。你們有可能是咱們的應用環境，你們的項目裡面用到所有的環境，在線上還要再去部署，這個是非常麻煩的。

<sub>[00:43-01:06]</sub>
第二個，你們應用還要配置。那應用的配置，比如說配端口、配連接，這些東西都非常非常的麻煩。那這個地方產生了兩個角色，一個角色叫做開發人員，還有一個角色叫做運維人員，這是公司裡面是必備的兩個人員，對不對。那在這個地方，他們兩個之間就產生了一些很微妙的關係。我不知道大家有沒有遇到過一個就是所有的互聯網公司都會面對的一個問題。

<sub>[01:07-01:37]</sub>
那什麼問題呢，就是咱們的話——「我在我的電腦上可以運行」。有沒有聽過這句話？這個東西在大量的情況下存在，就是這個東西我在我的電腦上是可以運行的，為什麼到你那就不能運行了，對不對。而且就是我們這裡面的話還有一些問題就是，比如說咱們的版本更新，導致咱們的服務不可用了。我們上線的一個項目，因為一些組件或者一些環境的版本更新，我們服務不能用了。那這些問題都非常麻煩。

<sub>[01:37-02:17]</sub>
那對於咱們的一個運維來說，是不是咱們的考驗就非常大。由於存在這個問題，我們就要去解決它，對不對。要麼兩種方法，開發要去管理運維，要麼就運維來學開發。那現在的話，對我們來說是不是開發兼運維。就是你們要學開發的話，運維的活也要幹。所以說你說我們作為一個 Java 後端人員，作為全棧真的累。要會前端、還要會後端、還要會運維、還要會架構，真的是痛苦。但是沒辦法，學的多壓力大，我們的工資可能會更高一點。

<sub>[02:17-03:03]</sub>
那我們來看一下，既然有這個問題，我們就來繼續聊。那比如說我們去配置一個環境，那環境配置是十分麻煩的。那環境配置十分麻煩，我們就要去解決它，對不對。比如說我每一個機器都要部署環境，這個是大量耗時間的。而且比如說你們最簡單的，有的人去部署集群，你用 Redis 集群還好，你們去備一個 ES 的集群，肯定要備一個 Hadoop 的集群，等等。像這樣的一些集群，都是特別浪費時間的，很多人一部署還會部署錯，對不對。那非常麻煩，如果部署錯了甚至可能要重頭來過。所以說這個地方是費時費力的。

<sub>[03:01-03:40]</sub>
那現在的話，我們就要去解決這個問題，對不對。我們要從根本上解決問題。那怎麼去解決？就是比如說我現在發布一個項目，以前的話我們可能是發一個 JAR 包或者是一個 WAR 包。現在的話我們要怎麼發布？我們這個 JAR 包需要的環境，比如說現在我研發了一個項目，它需要 Redis 的環境、需要 MySQL 的環境、需要 JDK 的環境、需要 ES 的環境等等。你本來去運行這個 JAR 包是一分鐘的事情，但是因為部署這個環境，可能部署一天都部署不好，能理解我意思吧。

<sub>[03:31-04:03]</sub>
這個時候假設我們想一下，我們能夠直接帶著這些環境把我們的項目上線多好。什麼意思？原來我們要發布一個 JAR 包或者 WAR 包，假設現在我就以 JAR 包為例。那我們發布項目的時候，就是用這個 JAR 包帶上我們的環境進行發布。能理解這一層東西嗎？就是以後運維拿過去的話，它是一整套的東西，而不是這麼一個 JAR 包。它之後執行，它就可以直接執行我們這整個給它發布的東西。因為這裡面的容器是我們開發用的，所以說就能夠保證一致，對不對。

<sub>[04:01-04:20]</sub>
所以說這個地方就來了一個詞，就是我們的一個項目能不能夠帶上咱們的一個環境安裝打包，對不對。這個時候就非常非常重要了。我們安裝或者運行項目的時候，跟著這個環境一塊。那 Docker 就是來幹這麼一個事情的。

<sub>[04:22-05:08]</sub>
那比如說還是繼續來講，這個思維很重要。那之前我們在服務器配置咱們的一個應用的環境，假設要配件搭建這麼多環境，如果你們要用 Hadoop 的話，那 Hadoop 環境更難搭建，尤其你們做集群非常麻煩。你安裝這麼多環境的話，配置超麻煩了，如果配錯了就完蛋了。而且它最大的東西，它是不能夠跨平台的。很多東西它是不能夠跨平台的，你的項目是通過 Java 可以跨平台，但是它這些環境不是跨平台的，也非常麻煩。那你們有可能最開始的開發環境跟我們現在一樣，我們假設在 Windows 上講課，那最後發布到咱們的 Linux 上，這上面的話環境是有天壤之別的。

<sub>[05:08-05:46]</sub>
那這個東西就非常非常麻煩。那傳統的就認為，我們就開發人員給運維丟一個 JAR 包就行了，開發給它丟一個 JAR 包，所有的東西交給運維來做。那現在的思想，就是我們開發的時候，打包部署上線，咱們一套流程做完。就是原來我們需要很多人做的事情，現在就一個人就可以搞定了。這也就是為什麼有句話叫什麼呢：「高級開發人員，千金難求，低級開發人員，滿大街都是」。

<sub>[05:57-06:32]</sub>
那 Docker 之所以發展這麼的迅速的話，就是因為它對於以上這些問題，Docker 給它了一個解決方案。那通過 Docker，我們就可以直接去完成上面所遇到的所有的困境，我們直接一個開發人員去部署上線。你會發現使用 Docker 之後，它真的是太簡單了。

<sub>[06:33-07:17]</sub>
舉一個最簡單的例子，比如說你們現在去開發一個 Android 應用，你們首先的話這個 APK 肯定不是自己寫的，你們可能要通過 Java 代碼去寫，構建一個應用之後，然後你們要把這個應用發布出去。那發布到哪個地方來？這個地方應該是一個咱們的應用商店，能理解我的意思。那應用商店的話，這個軟件就有人用了。比如說這個人要去下載，那他只需要做一件事情，我去這個商店把這個 APK 軟件給下載下來，那下載下來安裝即可用，是不是這麼一個環境啊。

<sub>[07:17-08:24]</sub>
那現在我們用了 Docker 之後，你們按照這個同理來證。我們也是比如說我們通過 Java 開發了一個項目，那這個項目的話是一個大項目，那這個項目裡面可能還有一些環境，對不對。那這些環境的話，我們都把它放在裡面。我們要做一個事情就是打包，打包咱們的項目，那這個時候帶上環境。那這個東西我們先讓大家去理解，它稱之為鏡像。你們下載 Windows 的時候，你們去裝 Windows 系統是不是你們裝好之後，電腦裡面會自帶一些軟件，對不對。那這個其實就是 Windows 的鏡像。那現在我們用 Docker 的話，這個名字叫什麼？叫 Docker 鏡像。我們要把這個鏡像放在我們的一個 Docker 倉庫裡面，這個 Docker 倉庫就好比是咱們的一個商店。

<sub>[08:01-08:44]</sub>
那現在的話，我們開發就做這一步，那以後別人要用我們這個環境或者運維要來部署，就非常簡單了，它只需要去商店下載我們這個鏡像。下載我們發布的鏡像，然後直接運行即可。它根本不需要去考慮裡面環境的問題，因為我們打包鏡像的時候，所有東西在這個位置就處理了。運維只需要把它下載下來運行就好了。這就是 Docker 最大的一個亮點。Docker 就是為什麼能夠打破一些傳統的應用，就是因為它通過這個鏡像機制，這個鏡像是非常非常重要的。

<sub>[08:38-09:10]</sub>
那我們來看一下 Docker 這裡面的一個 Logo。Docker 這個 Logo 的話，現在是最新版的了。這個地方是一個小鯨魚，對不對。這個小鯨魚上面放了很多很多的集裝箱。那其實 Docker 它的一個思想就來源於集裝箱。Docker 的思想就來自於咱們的一個集裝箱。

<sub>[09:10-09:54]</sub>
那集裝箱是一個什麼東西來？它是不是一個箱子，互相隔離，對不對。那我們現在的一個系統上面要去運行很多很多軟件，那原來這些軟件我們可能會發生衝突。比如說我們跑了一個 Java 程序放在裡面，那上面可能跑了多個應用，多個應用之間如果因為一些端口的佔用或者一些衝突，是不是就可能會報錯。最簡單的大家經常會遇到端口衝突，那這個情況下就會報錯，甚至還有一些配置錯誤、一些連接錯誤。

<sub>[09:53-10:15]</sub>
在原來都是咱們的一些交叉的，你一個項目可能都是用的一套環境。現在我們要把它隔離出來，那這個隔離是 Docker 的核心思想。Docker 核心思想，那這個隔離就好比這個集裝箱，對吧。我們把所有東西打包裝箱，每一個箱子是互相隔離的。

<sub>[10:15-11:00]</sub>
那所以說我們去學習它就跟這個 Docker 裝集裝箱一樣。我們以後再也不用因為去擔心我們的什麼應用端口衝突出問題了，也不用擔心環境的問題了，我們直接就可以通過 Docker 來進行部署。那比如說這個集裝箱，我們最簡單的例子，我們一個集裝箱來裝水果，另外一個咱們來裝生化武器。那這兩個東西你們原來的話是不是不能把他們放在一個船裡面去運。但是現在的話我們就是為了讓這些不同的東西甚至是完全不一樣東西能夠放在同一個環境裡面去運行。比如說這是我們的 Linux 服務器，在這個服務器裡面原來把這兩個全部都塞到裡面，他們兩個可能就感染了或者出問題，一個壞了可能會影響另一個。

<sub>[11:00-11:54]</sub>
現在我們就通過鏡像來進行隔離，或者叫容器。我們把它一個隔離出來之後，那它跑它的，它壞了不影響我們的水果。他們兩個同時放在我們的系統上也是完全沒有問題的。那這個樣子還有什麼好處來？我們可以把咱們 Linux 服務器是不是壓榨到極致。你們平時跑應用的時候，有時候會覺得非常浪費，空間那麼大我還沒有辦法去用。那現在的話 Docker 它通過一種隔離機制，可以將咱們的服務器環境利用到極致。就是你們幾乎有一點空間就可以把它壓榨出來去運行東西，完全是 OK 的。

<sub>[11:54-12:20]</sub>
那這些東西就是關於 Docker 為什麼會出現的。就是因為我們出現了這麼多問題，對不對。開發跟部署、開發跟運維之間這麼多的問題，怎麼樣去解決呢？我們想辦法把環境跟咱們的所有東西打包成鏡像來發布。那鏡像的話就好比咱們的一個應用商店，我們以後也想用一些東西，假設這個應用商店裡面有，我們就可以把它下載下來用。假設應用商店沒有的話，那我們就自己可以去做一個鏡像來把它發布。

<sub>[12:20-13:20]</sub>
比如說大家經常去安裝 Tomcat，這個在 Windows 下一下就安裝了。但是大家安裝 MySQL，那這個就非常麻煩，對不對。尤其 MySQL 你們要去部署集群，或者 Redis 集群，這些東西你們在當前電腦上去安裝是非常非常痛苦的。那現在通過 Docker，你們安裝它，一鍵就可以安裝了，就是因為官方已經幫你去寫好了它對應的配置，我們就可以直接把它拿過來使用起來。那這就是 Docker 的理念，我們所有東西都是一種隔離的。那到了這個位置，我們就理解了 Docker 的一個出現的過程了，對不對。因為有這麼多的問題，所以要解決它。Docker 提出了一個鏡像，還有一個通過容器進行相互隔離的機制。所以說本質就是所有的技術都是因為出現了一些問題，我們需要去解決，才去學習。

### P03：聊聊 Docker 的歷史

<sub>[00:00-00:17]</sub>
我們來繼續學習啊，還是一樣的，我們聊完 Docker 為什麼會出現之後，我們來聊一下 Docker 的歷史。這個東西是我講所有的課都喜歡講的一個點，對不對。就是如果你不知道這個東西的歷史，說白了你跟它就沒有什麼緣分。你只是會用一個它調的 API 而已，你跟別人聊都聊不起來。所以說學東西一定要從它的歷史去研究起啊。

<sub>[00:31-01:02]</sub>
那在 2012 年呢，幾個搞 IT 的年輕人，就在美國成立了一家公司。那這家公司的名字最開始叫 dotCloud。這個 dotCloud 的話，他並不是 Docker，他們最開始做一些咱們的 PaaS 的雲計算服務。那什麼是 PaaS 什麼是 SaaS 呢？我們之前也說過，對不對，IaaS 基礎設施即服務、平台即服務以及咱們的一些最終的應用即服務等等還有軟件即服務。那這樣的一些東西他們是通過這個來進行掙錢盈利的。

<sub>[01:22-01:44]</sub>
如果你們了解 Linux 的話就知道咱們 Linux 裡面有虛擬機，對不對。那這個虛擬機有關的容器技術說白了，我們之前講過 Linux，現在我要在我的 Windows 上虛擬出來一台 Linux，我可以通過咱們的 VMware 這種軟件去開起來。

<sub>[02:01-02:56]</sub>
那在最開始的時候啊，他們就去做類似於這樣的一些事情。那最開始的話他們去做完這個東西開始上線的時候，他們就將自己的容器化技術進行了一個統一的簡化命名，那這個命名就是 Docker。但是你們以為最開始這個 Docker 就很火嗎？這是不可能的，Docker 剛誕生的時候，這個技術剛誕生的時候就好比你們現在自己去做一個 UP 主，你們就知道最開始起來特別特別的累，因為沒有人支持你或者說就是關注你的人非常少。那這個時候的話誕生的時候啊，它是沒有引起行業的注意。

<sub>[03:25-04:05]</sub>
那這是幾個年輕人，他們至少要在社會上生存的話第一動力是不是要生存活下去啊。那活下去沒錢的話是不行的，而他們這個 Docker 的最開始是一家非常小型的公司。那在這個激烈的競爭中他就活不下去了，怎麼辦呢？他們就想了一個詞冒出來叫做「開源」。什麼是開源呢？說白了就是開放源代碼，也就是我把我的東西公布出去，也就是將我們原來內部的一些代碼全部都開放給所有人。

<sub>[04:14-04:53]</sub>
那這個時候讓所有人一起參與進來，就好比最開始的阿里是一樣的。那有的軟件呢是一開始就做開源的，而有的軟件呢是混不下去再做開源的，對不對。那這個創造者呢又不想放棄，Docker 呢明顯就屬於後者。那所以說 2013 年的時候，Docker 公司的一個創始人，也就是 23 歲的 Solomon Hykes，那這個時候呢他就將 Docker 開源了。

<sub>[04:40-05:16]</sub>
那開源之後啊，很多人就覺得這個東西真的好呀，對不對。不開源不知道，一開源就一鳴驚人。現在的話就是非常多的工程師發現了 Docker 的一個優點，就是越來越多的人發現了 Docker 的優點。從開源之後，2013 年開源之後，它幾乎每一個月 Docker 都會更新一個版本。那這麼樣去更新的話就說明它的社區是非常火熱的。

<sub>[05:42-06:10]</sub>
那到了咱們的 2014 年 4 月 9 日，Docker 1.0 發布。那這是不是一個里程碑的時刻啊。那距離我們現在 2020 年也僅僅 6 年，但是 6 年時間它就成為一個我們現在所有的開發人員幾乎必須要掌握的一門技術。那我們的話很多人覺得學入門就夠了，但是我希望我給大家講到精通。

<sub>[06:17-07:10]</sub>
那為什麼 Docker 這麼火呢？Docker 因為這麼火的話，就是它相對於咱們的一個虛擬機來說啊，它十分的輕巧。因為在此之前，我們知道我們的 VMware 就是虛擬機技術這個時候已經發展了非常久。在容器技術出來之前，那這個時候的話我們都是使用的虛擬機技術。虛擬機技術比較出名的就是咱們的 VMware 或者咱們的 OpenStack。那相信呢很多人也都用過虛擬機，因為我們在之前講解 Linux 的時候讓大家去安裝了。如果你們沒有錢去買阿里雲的服務器，你們可以在本地通過 Linux 虛擬機安裝，但是你們那玩意是不是特別佔內存呀，你們根本開不了幾個。

<sub>[07:17-08:10]</sub>
那這個虛擬機是怎麼操作的？我們就來聊一下虛擬機。首先我們要在咱們的 Windows 中或者在你們的 Mac 中裝一個咱們的虛擬機軟件，比如說 VMware。那通過這個軟件，我們可以虛拟出來一台或者多台電腦。那這個一台或多台電腦它跟我們真實的開發其實就是差不多的。它就跟一台真的電腦一樣，它是十分笨重的。你們隨便一個虛擬機，比如說一個基本的鏡像 19 個 G。那這 19 個 G 我就跑一些基本的命令，真的浪費。那這些的話相當於個子電腦，非常非常麻煩。但是好處是這裡面的每個虛擬出來的電腦跟我們真實的電腦是差不多的，而且每個電腦是相互隔離的。但是就是比較笨重，這是它的一個缺點。

<sub>[08:23-09:06]</sub>
那我先說一下咱們的虛擬機也是屬於咱們的虛擬化技術。那現在的話，我們的 Docker 呢其實也是一種虛擬化技術。那它是一種容器技術，Docker 的容器技術也是一種虛擬化技術。所以說它們本質是差不多的，但是的話就是他們的一個體量的原因。那虛擬機的話非常笨重，我們要去虛擬一個電腦出來還有它的硬件軟件的話非常麻煩。而容器呢我們就只在乎當前那些軟件就好了。比如說我原來的話用 VMware，我需要去下載一個 Linux 的鏡像，比如說我下載一個 CentOS 的鏡像，那這個鏡像的話可能就是一個原生的鏡像，相當於一台新的電腦。

<sub>[09:28-10:08]</sub>
那現在假設我們用了咱們的一個 Docker，用了 Docker 的容器的話是什麼樣子呢？還是一樣的，我們也是需要做隔離。但是現在的話它是用那種鏡像機制，那這個鏡像它怎麼做的呢？它不是這麼一個完整的原生的電腦了，它只是一個最核心的環境。比如說咱們 Linux 最核心的環境可能的話，最小其實 4 兆就夠了。它的最核心就是一些命令加上咱們的一個開機啟動。那現在在上面我們就加比如說我們要裝個 JDK，那我就把這個 JDK 放到這個環境裡面。比如說我安裝 MySQL 就把 MySQL 安裝在這個環境裡面，把它打包成一個鏡像。那一般的話這個鏡像就十分的小巧。那我們要運行的話，直接運行這個鏡像就可以了。

<sub>[10:23-11:06]</sub>
OK，那所以說啊我們說了 Docker 為什麼這麼火，就是因為它十分的輕巧。這個一定要記住啊。那它啟動的時間是非常快的。用一個最簡單的詞，比如說你們去一般用一個虛擬機，幾十個 G 是正常的吧，或者幾個 G 吧。幾個 G 裝一個虛擬機這其實就非常的小了，非常麻煩。而容器的話可能就是幾兆，幾個幾兆甚至幾十兆或者幾百兆，幾百兆的話都已經非常大了，最小的話甚至是 KB 級別的。大體積上面就快了，而且這個虛擬機啟動一般都要幾分鐘，而容器呢幾乎是秒級啟動。什麼意思啊？我要啟動 MySQL，我一秒鐘就可以起來了，就這麼快。

<sub>[11:12-11:41]</sub>
那這就是關於 Docker 的一個歷史啊，大家一定要去了解一下。在這個地方呢我又給大家提了一些新的詞，比如說「開源」，我希望大家未來的話也有一種開源精神。其實我自己的話就是一個非常非常熱愛開源的人。那其實的話我認為開源是一種思想，而且未來的社會的話其實已經是一種開源的。所以說開源其實是一種很好的思想，希望所有的開發人員都有這樣一種思想啊。

### P04：Docker 能做什麼

<sub>[00:00-00:16]</sub>
那我們接著來看，聊了 Docker 的歷史之後，也知道 Docker 官方地址之後，我們來聊一下 Docker 能幹嘛。然後我們就要越來越深層次，從不認識這個技術，你要學習的方式也是從不認識，了解它的名詞，然後我們要知道它能幹嘛，對不對。

<sub>[00:22-01:00]</sub>
我們從虛擬機出來比對。之前的虛擬機技術，那虛擬機的話說白了就是我們帶一些環境安裝的一種解決方案，對不對。那它是一個什麼樣的呢？比如說我們這個地方是我們的一台大大的電腦，電腦的話首先我們一般都有一個內核，比如說它是一個 Kernel 內核。那這個內核再往上的話，可能是放一些我們運行所需要的庫，對不對。那在這個庫上面的話，可能就是跑了我們的一些程序，一個電腦上面可以跑很多應用。

<sub>[01:10-01:42]</sub>
這就是我們傳統的方式。現在的話我們的 Windows 或者你們的虛擬機其實就是幹這個事情。我現在虛擬出來一台電腦，這個電腦核心是個內核，上面有預先運行的環境。那我們的把項目是不是發布到這上面啊？這是我們傳統的虛擬機的方式。那這種方式大家想一下也有弊端，我們之前就說了。那這個東西其實模擬了一台電腦出來，它內存非常非常大，很多東西的話它資源也比較多，就是浪費的。

<sub>[01:42-02:22]</sub>
那現在的話，我們想用的是咱們的一個容器化技術。那了解容器化技術之前呢，我們肯定要來說一下咱們的虛擬機技術基本的一些缺點。第一個，由於它是咱們一個電腦一個電腦的虛擬，這個環境非常大，第一個就是資源佔用十分多。第二個就是我們要去啟動它的話，它可能要去開機啊，開機的過程也非常麻煩，冗餘步驟非常多。第三個最不得忍受的就是我要開多個，它啟動很慢。

<sub>[02:33-03:06]</sub>
OK，那這是我們的一個虛擬機技術。那現在我們來看一下容器化技術。容器化技術就是什麼樣的呢？它不是模擬的一個完整的操作系統，把這一句話記住。它並不是一個完整的系統，比如說我安裝一個 CentOS，我只需要它的核心就可以了。我們就是把所有東西打包進行隔離。

<sub>[03:02-03:55]</sub>
現在的話我們這個內核還是這個內核，但是的話它可能是一個簡化版的。現在的話它是一種隔離機制，比如說我這個地方是個容器，現在的話可能會存在很多很多的容器。那我們把這個地方寫過來，比如說這個地方來一個咱們的 Lib（庫），那這個就是我們的容器內部環境。就是我們這個容器裡面需要什麼，我們就跑在這，然後我們一個應用把它隔離了。那現在的話，你們就把它稱之為一個容器就好了，這個容器呢包含了什麼：我們的一些運行環境以及這個應用。

<sub>[03:55-04:27]</sub>
那每個容器它是互相隔離的，那這裡面的話就放容器。那這些容器直接運行在咱們的操作系統之上，它可以充分的利用咱們操作系統的一個資源。你可以看到容器之間互相隔離，對不對。每一個項目它可以跟自己的運行環境打包形成一個鏡像，而且這個東西呢它跟其他東西互相隔離，就是非常安全。而且假設我這一塊崩了，其他三個依舊是互相隔離、是好的，對不對。而且可以把咱們的服務器的資源利用到最大。

<sub>[04:33-05:12]</sub>
這是它們最本質的一個區別。那下面寫一下，就是比較咱們的一個 Docker 和咱們的一個虛擬機技術的不同。咱們的傳統虛擬機它是虛擬出一套硬件，這是一套完整的電腦，對不對。運行一個完整的操作系統，然後在這個系統上安裝和運行軟件。這個非常非常麻煩。而咱們容器是怎樣？容器內的應用是直接運行在咱們的宿主機的內核中。那容器是沒有自己的內核的，我們看來看了，內核是在服務器上的。容器直接利用咱們宿主機的內核。它也沒有虛擬我們的硬件，所以說它非常非常快。

<sub>[05:55-06:29]</sub>
所以就輕便了，說白了就在上面的基礎上進行簡化。而且咱們每個容器間是互相隔離的，對不對。因為隔離所以說我們能夠保證安全。每個容器內都有一個屬於自己的文件系統，我存東西也是存到我自己的文件系統裡面，對不對，互不影響。

<sub>[06:27-07:09]</sub>
OK，那這就是我們的一個好處。那現在的話給大家對比了一下，Docker 能幹嘛大家現在知道了。它是容器化技術，可以節省咱們的資源。比如說原來的話你一個電腦上可能只能裝一個 Redis 群，我可能裝三個就卡死了。現在你用了 Docker，你可能能裝 30 個，這就是它的區別。你原來可能啟動三個 Redis 就卡了，現在啟動 30 個都不一定卡。它可以極大的利用咱們整個內存還有資源。所以說現在很多小伙伴離開了 Docker，他就已經不想手動裝環境了。很多人學了 Docker 就依賴上 Docker 了。

<sub>[07:34-08:05]</sub>
那 Docker 能幹嘛？在這個地方有一個非常重要的詞，大家聽過嗎？DevOps。這個是什麼意思？兩個單詞，一個是咱們的開發（Development），一個是咱們的運維（Operations），對不對。這也是咱們現在作為一個開發人員的一個要求。這也是你能夠升值加薪或者在職場裡面更有競爭力的一個核心實現。它裡面有四個重要的特點。

<sub>[07:53-08:56]</sub>
第一個是什麼？更快速的交付和部署。這是第一個，就是我們這個應用它一定要保證能夠更快速的交付跟部署。那現在的話我們剛才說了，Docker 是不是能夠幫我們去做到這一點。傳統的應用開發完之後就要給它提供一堆大大的幫助文檔，讓你去看一下它怎麼弄，還有一堆安裝程序去安裝，非常非常的麻煩然後再部署。而利用了 Docker 之後，我們直接一鍵運行。打包鏡像、發布測試、一鍵運行，這個的話非常非常輕鬆。

<sub>[08:57-09:32]</sub>
第二個就是什麼？更便捷的升級和擴縮容。我們現在都知道有個彈性，對不對。我們比如說原來要去升級一個服務非常麻煩。現在的話，我們只需要把這個鏡像升級一下就好了，我們裡面的東西不用管，我們還是打包成一個鏡像。使用了 Docker 之後，我們搭建應用、部署運用就和搭積木一樣。

<sub>[09:44-10:33]</sub>
比如說原來的話我們這個項目要升級了，我要去換裡面的環境。比如說現在我用的是 Spring Boot 1.x、Redis 5、Tomcat 8。那曾經我們要去升級是不是要每個都去升級，非常麻煩。現在我們通過 Docker 直接就把它變成一個鏡像了。我們要做的操作就是把這個包整體升級一下，那別人拿到這個包直接再去運行這個包就好了，非常簡單。比如說現在我把一個項目打包為一個鏡像，那這個鏡像就是一個應用，它可以帶著環境直接跑起來。

<sub>[10:43-11:07]</sub>
我們要去做一些水平擴展，比如說服務器出現問題了，性能到瓶頸了，我現在要水平擴展做負載均衡。我直接在服務器 B 上直接一鍵運行咱們的這個鏡像，那麼這個 B 就被擴展起來了。而且不需要像原來這麼去一個一個的做。而且我們一個虛擬機上或者一個服務器上可以同時運行很多個容器，容器之間也能交互，這也是非常方便的。

<sub>[11:08-11:46]</sub>
那除了這個之外，它是更簡單的系統運維。就是我們以前的運維是非常麻煩的，對不對。容器化運行之後，我們的生產、開發、測試都是一樣的了。就是在容器化之後或者用 Docker 之後，就是我們的開發還有一些測試環境都是高度一致的。就是不會再因為出現我們原來說的——在我的電腦上可以運行，在你的電腦上不能用了，這個問題就徹徹底底的把它解決掉了。

<sub>[11:48-12:19]</sub>
最後一個，它也能滿足就是咱們更高效的計算資源利用。它可以把一個服務器的性能壓到極致。可能你們想不到，我現在一個一核兩極的服務器，我可以運行幾十個 Tomcat 甚至幾十個 Redis。你們之前能做到嗎？不可能的。現在用了 Docker 之後就是可以做到，就這麼簡單。你們可以同時運行幾十個 Redis，你們去做集群完全是不會有任何問題的，非常非常簡單。

<sub>[12:20-12:46]</sub>
那 Docker 我們說了，它是什麼？內核級別的虛擬化，這是它非常重要的一個點。它不像其他傳統虛擬技術，它要去用一些 Hypervisor 管理程序的支持。它不需要這些管理程序，它可以在一個物理機上可以運行很多的容器實例。比如說我可以運行一個 Tomcat，我還能去運行其他的一些環境，比如說 ES、比如說 Kibana 等等。相關的全部都可以跑起來，就是咱們的服務器的性能可以被壓榨到極致。
