# Day 3 第十四小時：Dockerfile 實戰與課程總結

---

## 一、前情提要（2 分鐘）

上堂課學了 Dockerfile 基本指令。

這堂課進入實戰：
- Multi-stage Build
- 完整專案打包
- 常見問題排解
- 課程總回顧
- 銜接 Kubernetes

---

## 二、Multi-stage Build（15 分鐘）

### 2.1 為什麼需要 Multi-stage

傳統 Dockerfile 的問題：

```dockerfile
FROM node:20
WORKDIR /app
COPY package*.json ./
RUN npm install
COPY . .
RUN npm run build
CMD ["node", "dist/index.js"]
```

這個映像檔會包含：
- 原始碼
- 開發依賴（devDependencies）
- 建構工具（TypeScript、Webpack 等）

結果：映像檔很大，而且有不必要的檔案。

### 2.2 Multi-stage Build 原理

```
Stage 1: Build Stage          Stage 2: Production Stage
┌─────────────────────┐       ┌─────────────────────┐
│ Node.js + npm       │       │ Node.js（精簡）      │
│ 原始碼              │  ──►  │ 編譯後的程式碼       │
│ 編譯工具            │ COPY  │ 生產依賴            │
│ devDependencies     │       │                     │
│ 編譯後的程式碼       │       │                     │
└─────────────────────┘       └─────────────────────┘
     1.2 GB                        200 MB
```

只把需要的東西複製到最終映像檔。

### 2.3 Multi-stage Dockerfile

```dockerfile
# ===== Stage 1: Build =====
FROM node:20 AS builder

WORKDIR /app

# 安裝依賴
COPY package*.json ./
RUN npm ci

# 複製原始碼並建構
COPY . .
RUN npm run build

# ===== Stage 2: Production =====
FROM node:20-slim

WORKDIR /app

# 只複製需要的檔案
COPY --from=builder /app/dist ./dist
COPY --from=builder /app/node_modules ./node_modules
COPY --from=builder /app/package.json ./

# 非 root 使用者
RUN useradd -m appuser
USER appuser

EXPOSE 3000

CMD ["node", "dist/index.js"]
```

### 2.4 關鍵語法

**命名 Stage**

```dockerfile
FROM node:20 AS builder
```

**從其他 Stage 複製**

```dockerfile
COPY --from=builder /app/dist ./dist
```

**從外部映像檔複製**

```dockerfile
COPY --from=nginx:alpine /etc/nginx/nginx.conf /etc/nginx/
```

### 2.5 Go 語言範例

Go 可以編譯成靜態二進位，Multi-stage 效果更明顯：

```dockerfile
# Build stage
FROM golang:1.21 AS builder

WORKDIR /app
COPY go.mod go.sum ./
RUN go mod download
COPY . .

# 靜態編譯
RUN CGO_ENABLED=0 GOOS=linux go build -o main .

# Production stage - 使用空映像！
FROM scratch

COPY --from=builder /app/main /main

ENTRYPOINT ["/main"]
```

最終映像檔只有幾 MB，因為 `scratch` 是空的。

### 2.6 Java 範例

```dockerfile
# Build stage
FROM maven:3.9-eclipse-temurin-17 AS builder

WORKDIR /app
COPY pom.xml .
RUN mvn dependency:go-offline

COPY src ./src
RUN mvn package -DskipTests

# Production stage
FROM eclipse-temurin:17-jre-alpine

WORKDIR /app
COPY --from=builder /app/target/*.jar app.jar

RUN addgroup -S appgroup && adduser -S appuser -G appgroup
USER appuser

EXPOSE 8080

ENTRYPOINT ["java", "-jar", "app.jar"]
```

---

## 三、完整專案打包實戰（15 分鐘）

### 3.1 專案結構

假設有一個 Node.js + TypeScript 專案：

```
my-api/
├── src/
│   ├── index.ts
│   ├── routes/
│   └── services/
├── package.json
├── package-lock.json
├── tsconfig.json
├── .dockerignore
└── Dockerfile
```

### 3.2 .dockerignore

```
# .dockerignore
node_modules
npm-debug.log
dist
.git
.gitignore
.env
.env.*
*.md
.vscode
.idea
coverage
tests
__tests__
*.test.ts
*.spec.ts
Dockerfile
docker-compose.yml
```

### 3.3 完整 Dockerfile

```dockerfile
# ===== Build Stage =====
FROM node:20-alpine AS builder

# 設定工作目錄
WORKDIR /app

# 複製 package 檔案
COPY package*.json ./

# 安裝所有依賴（包含 devDependencies）
RUN npm ci

# 複製原始碼
COPY tsconfig.json ./
COPY src ./src

# 建構 TypeScript
RUN npm run build

# 移除 devDependencies，只留生產依賴
RUN npm ci --only=production

# ===== Production Stage =====
FROM node:20-alpine

# 安裝 dumb-init（正確處理信號）
RUN apk add --no-cache dumb-init

# 設定環境變數
ENV NODE_ENV=production

# 建立非 root 使用者
RUN addgroup -S appgroup && adduser -S appuser -G appgroup

# 設定工作目錄
WORKDIR /app

# 從 builder 複製檔案
COPY --from=builder --chown=appuser:appgroup /app/dist ./dist
COPY --from=builder --chown=appuser:appgroup /app/node_modules ./node_modules
COPY --from=builder --chown=appuser:appgroup /app/package.json ./

# 切換使用者
USER appuser

# 暴露 port
EXPOSE 3000

# 健康檢查
HEALTHCHECK --interval=30s --timeout=10s --start-period=5s --retries=3 \
  CMD wget --no-verbose --tries=1 --spider http://localhost:3000/health || exit 1

# 使用 dumb-init 啟動
ENTRYPOINT ["dumb-init", "--"]
CMD ["node", "dist/index.js"]
```

### 3.4 為什麼用 dumb-init

Node.js（和大多數應用程式）不善於處理 Linux 信號。

問題：
- `docker stop` 發送 SIGTERM
- Node.js 可能不正確處理
- 導致強制 kill（資料可能遺失）

`dumb-init` 是一個輕量的 init 系統，正確轉發信號。

### 3.5 建構和測試

```bash
# 建構
docker build -t my-api:v1 .

# 查看大小
docker images my-api:v1

# 執行
docker run -d --name my-api \
  -p 3000:3000 \
  -e DATABASE_URL=postgresql://... \
  my-api:v1

# 查看日誌
docker logs -f my-api

# 測試健康檢查
docker inspect --format='{{.State.Health.Status}}' my-api

# 停止（測試優雅關閉）
docker stop my-api
```

### 3.6 映像檔大小優化

**優化前後比較**

| 版本 | 大小 |
|-----|------|
| node:20 + 所有檔案 | ~1.2 GB |
| node:20-slim + Multi-stage | ~300 MB |
| node:20-alpine + Multi-stage | ~150 MB |

**優化技巧**

1. 使用 alpine 基礎映像
2. Multi-stage build
3. 只安裝生產依賴
4. 清理快取
5. 合併 RUN 指令

---

## 四、常見問題排解（10 分鐘）

### 4.1 建構緩慢

**問題**：每次建構都重新安裝依賴

**原因**：COPY . . 在 npm install 之前，任何檔案變動都導致快取失效

**解決**：先複製 package.json

```dockerfile
# 好
COPY package*.json ./
RUN npm install
COPY . .

# 不好
COPY . .
RUN npm install
```

### 4.2 映像檔太大

**診斷**

```bash
docker history my-image:latest
```

查看每一層的大小。

**常見原因**

- 沒用 Multi-stage
- 基礎映像太大（用 alpine）
- 沒清理快取
- 複製了不需要的檔案（用 .dockerignore）

### 4.3 容器無法啟動

**診斷步驟**

```bash
# 1. 查看日誌
docker logs my-container

# 2. 互動式執行
docker run -it my-image sh

# 3. 檢查 CMD/ENTRYPOINT
docker inspect my-image --format='{{.Config.Cmd}}'
docker inspect my-image --format='{{.Config.Entrypoint}}'
```

**常見原因**

- 命令不存在
- 權限問題
- 缺少依賴
- 環境變數未設定

### 4.4 COPY 失敗

**錯誤訊息**

```
COPY failed: file not found in build context
```

**原因**

- 檔案不在 build context
- 被 .dockerignore 排除
- 路徑錯誤

**診斷**

```bash
# 查看 build context 裡有什麼
docker build -t test --progress=plain .
```

### 4.5 權限問題

**問題**：容器內無法寫入檔案

**原因**：使用非 root 使用者，但目錄屬於 root

**解決**

```dockerfile
RUN mkdir -p /app/data && chown -R appuser:appgroup /app/data
USER appuser
```

或用 COPY --chown：

```dockerfile
COPY --chown=appuser:appgroup . .
```

---

## 五、Docker 課程總回顧（10 分鐘）

### 5.1 兩天學習內容

**Day 2：Docker 基礎**

| 小時 | 主題 | 重點 |
|-----|------|------|
| 1 | 環境一致性問題 | 為什麼需要容器 |
| 2 | Docker 架構 | Client-Daemon-Registry |
| 3 | Docker 安裝 | CentOS、Ubuntu、Desktop |
| 4 | 基本指令（上） | pull、images、run、ps |
| 5 | 基本指令（下） | stop、rm、logs、exec |
| 6 | Nginx 實戰 | Port mapping、Volume |
| 7 | 實作練習 | 綜合應用 |

**Day 3：Docker 進階**

| 小時 | 主題 | 重點 |
|-----|------|------|
| 8 | 映像檔深入 | 分層、儲存、快取 |
| 9 | 容器生命週期 | 狀態、資源限制、重啟 |
| 10 | 容器網路 | Bridge、Host、自訂網路 |
| 11 | Port Mapping | -p 語法、綁定策略 |
| 12 | Volume | 三種掛載、備份還原 |
| 13 | Dockerfile | 指令、建構 |
| 14 | Dockerfile 實戰 | Multi-stage、完整範例 |

### 5.2 核心概念回顧

**容器 vs 虛擬機**

```
容器                        虛擬機
┌─────────────────┐        ┌─────────────────┐
│    App A        │        │    App A        │
├─────────────────┤        ├─────────────────┤
│   Container     │        │   Guest OS      │
├─────────────────┤        ├─────────────────┤
│   Docker Engine │        │   Hypervisor    │
├─────────────────┤        ├─────────────────┤
│   Host OS       │        │   Host OS       │
└─────────────────┘        └─────────────────┘
     輕量、快速                  完全隔離
```

**Docker 三元素**

```
Registry (Docker Hub)
        │
        │ docker pull
        ▼
      Image ──────────► Container
              docker run
```

**Dockerfile 流程**

```
Dockerfile ──► docker build ──► Image ──► docker run ──► Container
```

### 5.3 重要指令速查

**映像檔**

```bash
docker pull nginx:alpine
docker images
docker rmi nginx
docker build -t myapp:v1 .
```

**容器**

```bash
docker run -d --name web -p 8080:80 nginx
docker ps -a
docker logs -f web
docker exec -it web sh
docker stop web
docker rm web
```

**網路**

```bash
docker network create mynet
docker run --network mynet ...
docker network ls
```

**Volume**

```bash
docker volume create mydata
docker run -v mydata:/data ...
docker volume ls
```

### 5.4 最佳實踐回顧

1. **映像檔**
   - 指定版本，不用 latest
   - 使用官方映像
   - 選擇 alpine 或 slim

2. **Dockerfile**
   - Multi-stage build
   - 合併 RUN 減少 Layer
   - 不常變的放前面（利用快取）
   - 不用 root

3. **容器**
   - 一個容器一個程序
   - 設定資源限制
   - 使用健康檢查
   - 使用 restart policy

4. **網路**
   - 使用自訂網路（有 DNS）
   - 只暴露必要的 port
   - 敏感服務不要對外

5. **Volume**
   - 重要資料用 Volume
   - 定期備份
   - 給 Volume 有意義的名稱

---

## 六、銜接 Kubernetes（6 分鐘）

### 6.1 Docker 的限制

單機 Docker 可以管理幾個、幾十個容器。

但當你有：
- 數百個容器
- 多台主機
- 需要高可用性
- 需要自動擴展
- 需要滾動更新

單機 Docker 不夠用了。

### 6.2 為什麼需要 Kubernetes

Kubernetes 解決的問題：

| 問題 | Kubernetes 解決方案 |
|-----|---------------------|
| 多主機管理 | 叢集（Cluster） |
| 容器調度 | Scheduler |
| 高可用 | ReplicaSet |
| 負載均衡 | Service |
| 滾動更新 | Deployment |
| 設定管理 | ConfigMap、Secret |
| 儲存管理 | PersistentVolume |

### 6.3 Docker 到 Kubernetes

Docker 的概念在 Kubernetes 裡的對應：

| Docker | Kubernetes |
|--------|------------|
| Container | Pod |
| docker run | kubectl create |
| docker-compose | Deployment + Service |
| 手動擴展 | ReplicaSet 自動擴展 |
| 手動健康檢查 | Liveness/Readiness Probe |

### 6.4 下一步學習

Kubernetes 課程將包含：

- Kubernetes 架構（Master、Node）
- Pod 基本操作
- Deployment 部署
- Service 網路
- Ingress 入口
- ConfigMap 和 Secret
- 持久化儲存
- Helm 套件管理

### 6.5 Docker 技能的延續

你在 Docker 學到的：
- 容器化思維
- Dockerfile 撰寫
- 映像檔管理
- 網路和 Volume 概念

這些在 Kubernetes 都會用到！

Kubernetes 是 Docker 的延伸，不是替代。

---

## 七、課程總結與問答（2 分鐘）

### 7.1 學習成果

完成這兩天的課程，你應該能夠：

- 理解容器化的價值
- 使用 Docker 指令管理容器
- 撰寫 Dockerfile 打包應用程式
- 設定網路和資料持久化
- 為 Kubernetes 做好準備

### 7.2 課後練習建議

1. 把自己的專案容器化
2. 用 Multi-stage Build 優化映像檔
3. 設定適當的健康檢查
4. 嘗試多容器應用（資料庫 + 應用）
5. 練習備份和還原 Volume

### 7.3 推薦資源

- Docker 官方文件：docs.docker.com
- Docker Hub：hub.docker.com
- Play with Docker：labs.play-with-docker.com
- Kubernetes 官方文件：kubernetes.io/docs

### 7.4 下次見

下一堂課，我們將進入 Kubernetes 的世界！

---

## 板書 / PPT 建議

1. Multi-stage Build 流程圖
2. 完整 Dockerfile 範例
3. 兩天課程內容總覽表
4. Docker 到 Kubernetes 對應表


---

## 📺 補充教材：狂神說 Docker

> 以下內容來自【狂神說Java】Docker 教程系列，作為本節的補充參考資料。

### P31：發布鏡像到 DockerHub

我们来继续刚才的话我们已经把Docfail彻底写完了也编写了自己的一个镜像接下来我们就要发布自己的镜像来 想不想学这个 想学同学扣意

其实我们要把镜像发布到我们的外边让所有人都能访问比如说发到的Docfab或者发送到你的阿里云这个都是可以的

看一下这个发布镜像来如何做首先还是一样的我们先来看一下把镜像发送到DocfabDocfab的话我们知道

这是我们所有的地址打开我们的一个Docfab在这个Docfab上面首先大家还是干嘛来得去注册一个账号

第一个 它的一个地址这个网站比较慢第一个 就是地址我们需要这边注册账号注册自己的账号

你们去注册就完了在这边直接注册我已经注册好了我这边的账号就叫狂神 看到同学课一样

如果你们没有注册这边是让你们登录的我注册了就看嘛就别显示我的一个信息在这边一旦登录之后

我们就可以使用它了所以说第二步 确定这个账号这个账号可以登录这个账号可以登录就可以了

这账号可以登录你们如果不能登录是不行的我这个账号就叫狂神我已经把这个非常霸气的名字

给你们抢了你们想想抢我也抢不到那这个时候我们就要干嘛来来 在我们的服务器上提交在我们的

在我们服务器上提交自己的镜像那这个时候你们就跟这个社会连接了以后你们写的镜像

想让别人用你去Docker还不上下吧我的镜像已经提交上去了当然的话国外的网是非常慢的

我们先看一个命令我们先要从这边去登录咱们的Docker这里面有个命令叫Logging命令我们把这个命令写到这儿

我们要通过命令行来登录登录了才能放上去你不要傻傻的直接Docker Push你都没有登录

你这Push能Push上去就见鬼了所以说一定要先登录所以说登录完之后再提交那我们登录的时候怎么登录呢

可以通过Gang You Gang P的方式登录Gang You You the nameGang P password的那登录完了之后

登录完毕后就可以干嘛提交镜像了就可以提交镜像了就是一步一步

那就是一步来就是咱们的Docker Push命令把它Push上去所以说这个流程跟Gateup是一模一样的

我们这些学Gate时候都讲过了那我们先来登录一下比如说Docker LoggingGang You那比如说我要登录这个狂声

回车它就会让我输入密码我来输入一下我自己这个密码看一下这个密码对不对如果成功的话

它就说Logging成功如果失败的话肯定它这边就要报失败了我也我都快忘记我的密码了不知道我的密码是啥

来我这个地方它说我登录失败了那我们就是密码错了比如说我来换一个密码试一下看一下是不是这个密码回车

如果能登录成功它就会告诉你当前登录成功了Logging success的不是吧

我这密码这么复杂吗就是你们自己设密码的时候尽量设一个能想得起来的我这个人的话就是喜欢为了安全

我所有网站的密码都不相同登录成功了这就是登录成功的样子来写到这把它写下来

OK 它就会告诉你success Logging对不对拿过来所以说我经常忘记B站的账号以后你们会发现

我B站没有更新视频就是我忘记账号密码了OK吧给自己找了个借口真棒

OK 现在登录成功了之后我们就可以提交进相了对不对现在的话我们就来提交一下进相来

这个地方写下倒开ps我们在这个地方做的我们的进相比如说这边有一个DIYTOMCAT

这个是我们自己的进相现在的话我们就干嘛就把它PUSH上去可以了PUSH的时候一定要带作者名字比如说是狂神

你有写狂神的这个TOMCAT就是不要瞎写我们这边的话是一个DIYTOMCAT那我干嘛我就把它PUSH上去

然后要带上咱们的版本号就是这些东西是必要的就是这个人的名字就是用来做分类

不然很多人都叫DIYTOMCAT了对不对后面的话就是你的一个标签名字然后回车它就会PUSH了我们来看一下

它当前说的我们这个地方的进相已经有了它说我可能这边试一下

放一个2.0当前没有这个进相是吧我们先正常来PUSH一下直接PUSH我们这个DIYTOMCAT就是我们上面自己写了一个DIYTOMCAT

这是我们自己写的我们来试一下看一下能不能PUSH上去回车你看它真的就PUSH了对不对

但是这个地方PUSH就比较慢这样就已经成功了我就不让它真PUSH了我这600多照来这个地方就已经PUSH成功了

然后这个地方还是让大家建议尽量带上它的一个版本号来 PUSHPUSH自己的进相到咱们的一个服务器上

OK这边的话肯定会说失败你看它是不是就说失败了我刚说完它就失败了

这样的话它是会被拒绝的这是会被拒绝了拒绝拒绝所以说在这个时候

我们一般干嘛呢我希望加上咱们作者的一些信息那我们来再重新PUSH一下CLEARCLEAR

EARDocker IMAG我应该是之前发送过这样一个进相来Docker IMAG

我们来把它发布上去还是一样的Docker PUSH那比如说我这个地方叫DIYTOMKET那现在我给它一定要改名字

比如说我这个地方来一个狂神然后下面来一个DIYTOMKET2.0呢看一下能不能上去回车

这个的话是因为我们这个地方已经有这些东西了我想换一下换个二十一下那这就不行了

是因为我这边已经有了就是你们正常的话这边直接能PUSH上去的你们去做肯定是可以的因为我之前做过

我把这个命令写过来就可以了给大家你们要换的话肯定要换一个进相就尽量不要每次做的话

就用相同的进相那我比如说写到这1.0你都是用小本门记着密码那你的那个就完了就出问题了

那所以说这个地方一定来PUSH进相的时候的一些问题找问题

我们还是来把这个问题解决一下PUSH的时候应该是没有带上我的一些信息但是我本地的话需要有一些版本信息

对不对那这个时候的话我们可以将我们的本地就是假设报了这么一个错

我把这个错也写到这吧就是在乱七八糟的错我都给你们处理了假设出了这种错的话我们怎么解决了

增加一个咱们的TUG这玩意没TUG我们就增加一个嘛

把我气的是吧来 没有我就知道你们不想让我这样就此而过一定要让我解决

那我就给你们解决嘛来Docker里面有个TUG标签是来给他增加标签的那我们给当前这个金像

来加个标签你们那点小心思我不知道只要我不讲你们就放弃了来

TUG来 我们给当前这个DIY他们开的给他加一个版本或者加一个名字比如说

给他来加一个我给他给他换一个什么了就改成狂神吧来 狂神的他们咔他

来 狂神他们开的来 比如说1.0可以吧 我给他改个名字现在的话

你会发现我的金像又多了一个来 这边是不是又多了个1.0看到同学可以那我再提一下嘛Docker

PUSH那我这次再提一下再来看一下能不能成功那这边的话就是我这个狂神的他们咔他1.0

也就是我这边改完名字这个把它提一下上去这是大了版本好的来走你

你看这不就提交了吗也是一样的所以说我这个命令写在这地方你们自己去改他

也是能够成功的那现在的话他就能够提交成功来 写到这把这个命令干嘛

都给你们放到这里大爷们 是吧来 直播的都是直播 看过直播都是大爷来 写到这的

这是咱们的一个Docker PUSH上去即可上去

即可就是一定要带版本号就是自己平时发布的金像自己发布的金像尽量带上版本号

OK 那现在的话他就正在发布了这个发布的话600兆发布完了干嘛 就成功了

他发布你会发现他干嘛他也是按照这个一层一层发布的看到同学扣药就真的提交的时候

他也是按照层级来的所以说这个是核心的来 提交到时候

也是按照金像的层级来按照金像的层级来 进行提交的提交OK 那这个是

把咱们的金像发布到Docker Hub上但是这个的话有时候会比较慢你们用的阿里云服务器其实还可以的

如果你们用自己的虚拟机几乎就发不上去你这网络根本就不会拒绝了你家的网络

因为翻墙呢OK 那给大家来停一下这是发布到咱们的Docker Hub上我们再来给大家讲一个就是可以发布到咱们的一个

阿里云金像服务就是给你发到阿里云金像服务上去服务上那我这两个都给大家讲

以后大家走遍天下都不怕 是吧来 给大家停一下

### P32：發布鏡像到阿里雲容器服務

我们来继续跟着刚才的节奏我们现在把它发不到阿里云上还是说一下你都看到这了你还没有关注三连对得起我吗所以说一定要干嘛

三连关注加投币那看直播的话你也可以干嘛去公众号可以赞赏一下就是做一些支持当然的话也都是用外发电

那我们来看一下怎么发不到阿里云镜上还是第一步我们之前用过镜像加速服务是吧那所以说登陆阿里云

那第二步找到我们的一个容器镜像服务镜像服务那这个容器镜像服务里面的话也是有我们的一些信息的

那你找到我们这个容器镜像服务这是我们昨天已经进去过我们之前是用过这个镜像加速器那现在的话我们来用它的一个什么镜像仓库

那在这个地方的话你就可以看到镜像仓库还有一个什么还有个秘密空间那它是一个怎么样的创建规则呢

我也一步一步来给大家做还是一样的第三步找到这个镜像之后我们需要在这边干嘛

去创建一个秘密空间这个秘密空间呢一般就是指的一个非常大的项目而这个项目里面可能有很多很多的镜像

然后我们把这个镜像干嘛存到这里所以说第一步就是创建咱们的一个秘密空间

就是为了隔离一样创建秘密空间就是防止咱们的一个冲突第四步就是创建咱们的一个容器镜像OK

那说白了就是两步对不对第一步秘密空间点击创建就可以了比如说点击创建

那在这边去秘密一个秘密然后这个地方记住一个账号只能创建三个秘密空间所以说我就不创建了你点击这个地方

比如说我现在写的是BilibiliGangKwangShenBilibiliGangKwangShenOK

那这是我写的镜像你点击确定就可以了那创建完之后就变成这个样子那我们写到这就这么一个空间

有时空间之后我们还要创建一个容器镜像那点个这个镜像仓库那镜像仓库这边你干嘛

直接创建进行仓库就可以了还是按着这个步骤一步一步往下写比如说仓库的名称那我这边的仓库名叫KwangShenTest

KwangShenGangTest然后这边可以选择它是私有还是公有的你默认的话就选这个私有就好了如果是公有的话

别人就能访问如果是私有的话就只有你自己访问这个加要就是一个介绍你比如说

KwangShenNiubi对不对来公平是二一步然后我们这个地方就写一个介绍

然后再往下点击下一步下一步的话这边要去选一个仓库的信息我们这边就选本地仓库就好了所以这个地方创造手机的点本地仓库

就这两步没了然后再点击就创建成功了创建成功之后就会得到我当前这样一个

进向的地址就是我这个样子OK我们把它拿过来写过来

那写过来之后我们就可以干嘛来来第五步来

浏览一下这个页面的信息浏览这个信息浏览这个浏览阿里云看一下这里面有哪些

妖魔鬼怪的东西吗点开这个仓库你就可以去提交了这个仓库里面有些基本的信息比如说看到地址同学科业

这个地方有一个公网地址就是我们可以通过这个地址来访问我当前这个仓库然后再往下走

就是我们要怎么去操作你看人家写了来把这个地方给大家解个图过来太清楚了

我的妈呀来一步一步来第一步我们要通过login登录

对不对登录阿里云之后再从阿里云干嘛拉取我们的一个经向那拉取下来的话

我们就不要了因为我们还没上传我们就直接上传那我们就是将进向干嘛推送上去

人家都告诉你了怎么样推送一步一步写得丝丝的那我们就来做这么一个事情你看这边还没发不成功

clear那我们就不用这个了看一下有没有logout这个命令你看这个命令还真的有你看到没有

我都是忙猜的你看现在的话我们就登录一下阿里云的那我们就登录一下来

跟着还是三步曲先登录阿里云才能传到阿里云嘛那密码的话试一下不是这个

找不到啊密码看一下回车哎哟

我就知道会错那又不是这个密码然后登录成功了那现在登录成功了之后我们在下面干嘛

他说要让我们给这个进向生成一个版本号看到了同学可以就是给这个进向生成一个版本号

这个手机号暴露了这个不是我的手机号了已经这个手机我已经没用了我已经换号了来

在这个地方的话已经有了就是比如说这边有个tug这是我们刚才用的对不对就是生成这个进向的名字

我们已经生成过了来那现在我们就要去提交这个进向提交的话还是干嘛Dockerpush

把它push上去来push上去来push

push的话就是push到具体的地址嘛那我们就来push一下来这次就是push到阿里云了

来看着Dockerpush还是用我们当前这个Dockerimages

Dockerpush那把我们当前这个他们看他0.0干嘛给他push上去

Dockerpush上去走你把他push过去1.0的走你

你看他现在是不是就push上去了看到了同学扣一样所以说阿里云上给我们写的清清楚楚怎么做

你要将这样推送到远程就这么几步推送过去就完事了看到了同学扣一样这就是一个基本的

然后进向如果真的把它发不上来之后你就可以看到在这边会有一个进向的版本看到同学扣一样

就是如果同步上来之后这边就会产生一个版本那你要让别人下载你就可以在这边下载这可以去点进去

你点这个层一些它每一层是怎么做的你可以发现我现在上上来的每一层就是我们刚才写的每一个步骤

这个步骤是来自于哪了就是我们写的Doc Fair这一切是不是就跑通了来理解的同学

扣个一这一切的一切没有大家想的那么难所以说重点就是要了解

阿里云这个东西怎么样去使用来给大家停一下那所以说现在的话就记住了

阿里云的步骤就参考官方文档来阿里云容器进向的

进向的进向的就参考咱们的一个它的一个官方地址就可以了

官方地址那这官方地址就是像我这样子先创建一个命名空间然后再创建一个

进向容器然后再点进这个容器你就能看到它的地址了就是下面这个地址然后三个命令

我都已经给大家去讲过了现在的话大家就掌握了一个技能你可以将Doc Fair就是将你的进向推动到Doc Fair

也可以通到阿里云阿里云有公用私用你是不是以后想把自己的东西发到哪发到哪

以后你给别人发布你的项目就是发布一个Doc Fair以后我告诉你假设我要把我的网站开源那我就做一个事情

我直接发布一个进向给大家一个地址你们要用直接Doc Fair能理解我的一次同学课你们就直接Doc Fair

把我这个进向拉来拉然后Doc Fair就能运行我的网站了就这么简单听懂的同学课一样

那这是我们给大家讲了发布自己的进向到咱们的Doc Fair跟阿里云这两个大家干嘛

都可以测一下尤其是阿里的技术它真的是非常的使用很多东西一定要慢慢的多去理解这个产品

因为公司里面很多都用这种第三方服务的来给大家挺一下

### P33：Docker 所有流程小結

OK 那我们来接着看现在的话我们已经会了Docker的全流程我们来做一个小学要把这一切的东西来 这个地方小节有一幅图来 小节 这幅图是官方的其实

来 在这个地方给大家看一下我们现在的话就懂了这个Docker以后你看到这幅图就不陌生了Docker的全流程就在这个地方比如说 你想要用DockerFail

去编写所有的原代码最终干嘛呢 可以bill的bill的成为一个什么呢 成为一个镜像这个镜像 你想把它发布上去那就铺实到远程仓库 对不对

然后如果你要运行那么就Ruang把它运下来就变成了一个容器容器修改完之后 你想提交Comet提交 就变成了一个新的镜像

这是一个基本的流程 对不对第二个就是我们下载的流程怎么下载来 从Docker仓库把它干嘛 铺实 铺下来拉到本地之后呢 我们就可以干嘛

就可以把它Ruang运行起来了 对不对运行起来 你要修改也可以Comet再提交然后启动的时候呢 有三个命令停止启动一些重启命令

那之后的话 你想把这个镜像保存发布给别人 那这两个命令大家干嘛 自己一侧便通就是你想将镜像 来 打包打包 成为一个架

来 成为一个咱们的压缩包来 成为一个压缩包压缩包 这个压缩包呢就是可以发送压缩包给别人压缩包给别人

发送压缩包给别人那你发送压缩包给别人的话跟从Docker仓库去拉我宁愿选择干嘛肯定是去从仓库拉好一点 对不对

你们弄自己的一个阿里云镜像然后让别人过来拉这多快你没必要去给他备份如果要备份的话这两个命令也就是这么简单

通过Docker-C5方法然后Gunk-Low的方法那学习的方式还是一样的比如说Docker-C5那这个时候你就要看一下

来 杠杠Hellp来 看一下他是怎么使用的就可以了回这你就可以看到他对那参数就一个O参数他输出到哪一个

路径就没了 对不对所以说这个命令使用的非常简单你要把这个一个就是价包给他一个咱们的压缩包给他Low的进来的话也是一样的

你Gunk-Hellp来 Gunk-HellpGunk-Hellp那在这个地方就可以看到就是我们input

从哪个地方进来就是把它读取过来所以说这两个命令大家可以自行测试没有什么讲的意义了

毕竟我们都已经会发不到远程了那现在能把这幅图完全看懂得同学在公平上扣个意那其实到了这个位置你用Docker已经没有什么问题了

但是你要再往上精通一点你要去做运为的高层或者说你未来要学习咱们的Docker Compose然后或者Docker Swarm

等等那些东西的话你必须要把Docker网络干嘛在深入研究之后才能去掌握真正企业级开发实战也有东西

随着到目前为止我们到这个地方的话你Docker已经没问题了你再往下面的话就是更偏向运为层面的了

而且的话难度的话会偏向极群但是我还是通俗的给大家去把这个搞定我们把这个网络讲完今天今天把这个网络讲完的话

这个视频暂时的话其实就是齐全的了最终的话就是Docker SwarmDocker Compose3等我们之后有时间在直播的时候

我再给大家去分享讲解就可以了然后我们现在回过来我们在最开始的时候给大家说过Docker的组成然后什么Docker的一些命令

你现在看这些东西那简直就是小二科对不对学习就是这么一种方式如果你把这种方式学透了

你在我这边学东西就非常非常非常快然后再往下的话之前我还看过一幅非常完整的图我来找一下

隔哪来那现在去看这幅完整的图你也没有什么压力了来 往上找一找我们之前结果图的

我记得很清楚我结了图来 这就是完整的图现在你去看这幅图压力都没什么压力了比如说这是我们的仓库

那仓库的话有两个命令登陆跟退出和搜索除了这个之外你可以从仓库拉取下去或者铺上去东西

如果是个Docker fare你就可以可以bill的上去如果是一个别人生产的压缩帮那你就可以通过load跟import跟它加在进去

再往上走如果你是一个imagine就是一个进向进向的话有hits的ratetag remove

移出进向还有进向查看所有进向几个基本的命令然后进向的话通过wrong方式

来 通过wrong方式直接启动它就运行起来了然后你要停止的话那这就是什么care mini等等

然后最终的话它里面有哪些命令呢在容器里面的命令就是这下面这些比如说移除容器

然后移动然后tops查看它的一些进程什么exec进入容器执行touch进入当前的命令行执行inspike查看它的信息

等等暴露端口它的一些就是不停止推出然后这边的话你想把容器内的文件考配到外面

是不是通过copy这里面的所有的命令我们已经干嘛全全掌握了 开心吗OK 那这就是我们到目前为止

学习的所有关于刀核东西而我们这个刀核网络是为了给我们未来做一个铺垫就是给做未来做铺垫

我们后面要做容器编排我们要做容器编排集群部署像这种东西刀核comboce 刀核swarm那我们就需要学刀核网络

所以说我在这个地方也给大家把刀核网络深入一下来 给大家先停一下

### P38：Redis 集群部署實戰

来,我们来实战,部署一下Reduce集群那这个集群我们干嘛,不是那个,就是跟那个咱们之前的是不一样的我们之前学Reduce的时候,我给大家讲的是哨兵模式,对不对我这个Reduce应该是全网最全的了就是你们不可能在b站然后在其他地方找到比我这个Reduce更加详细的课了

然后在这边的话,我们之前讲过Reduce集群那我这个地方就不给大家去说咱们的一个哨兵的集群哨兵的集群咱们再不先不说我们这个地方用的是咱们的一个分片咱们以前用过的分片

就是分片部署加咱们的高可用和负载均衡吗高可用加咱们的一个负载均衡这是我们要用的一个,来就是我们做这个集群的目的,只是为了干嘛去替代一些问题来,给大家放大一点

什么意思呢,比如说现在我们这个地方至少要有六个Reduce我们一起动就要启动六个Reduce那现在的话,比如说这个地方有个Master来,比如说我给地方写个ReduceMaster-1更M1,来ReduceMaster-1

那现在的话,我们应该是三组三重,对不对三个组继,三个重击来,这是咱们的3,2OK,那除了这个之外,他们应该每个人都有自己的一个重击那我就写个Slower-S

然后S2-S3来,3那现在的话,我们要实现它的高可用首先我们这上面的话,都是要保证可用,对不对那可用的情况下,下面就是做一个备份了

这就是一个备份,这就是一个备份,这就是一个备份那假设我们这个主机挂了也就是我这个机子突然一下死掉了,变成红色的那么干嘛,这个S2-S3,它的背机干嘛,就要去替代,顶上它能理解我的意思,我们就做了这样一个模型

这个模型的话,还是比较轻松的我们拿过来,所以说我们至少要启动六个集群就是六个容器,至少要启动六个容器,然后来测试那这六个容器,一个人启动太慢了,我们就还是写一段share-gabin来,写一段share-gabin就行了

那这个Reduce,这是一个单独的集群集群我们就要建自己的网卡,所以说我们要建一个Reduce的网络网卡对不对,那现在的话,我们去建一下这个网卡这网卡我已经写在这了,我就不再重复写了我们来看一下,还是一样的

我们来看一下我们当前这些容器正在运行的我们把这些停一下,不然的话一下启动十几个服务肯定会卡住一下,来,Gun F,我们把这个MGun F我们把它移除一下,把这些容器现在都移了来,Docker PS,Gun AQ,还是一样的

那把当前这四个都移除掉,好了那我们来创建一个网卡,还是一样的Create一个Reduce,我们这边的Subnet就是咱们的一个实料那接下来我们也能放非常多,对不对,回车那现在的话,我们在这边Docker Network去看一下

我们这边是不是就有咱们的一个Reduce的服务了,看到同学可以那你要去看它的Inspect,具体详细信息,也是可以看到的比如说要看一下咱们的一个Reduce的那Reduce的,那这就是我们自己分配的,172.38开头了现在底下是没有一个网络的

那接下来的话,我们就要创建6个Reduce的服务那Reduce的话,我们知道它要去写配置文件,对不对那这个地方我们就用脚本,一次性写完配置文件来看一下这段配置文件,比较简单设有脚本,我们通过Four循环,循环6次

然后去创建我们的Reduce的一些配置文件然后去创建Reduce的Configure配置然后把这个配置文件里面写上咱们的具体配置那就是比如说容器端口,6379然后集群的核心配置在哪个地方来

也就是Class2 Iable,把它开启这个集群看到了同学可以,然后它去连接具体的IP那我们来把这个创建一下来,直接一件创建复制给大家创建一下咱们的这边

来,粘贴,回车那现在的话,它就已经创建完了我们来CD看一下来,CDMyData来,ZS看一下,这边是不是有个Reduce

CDReduce,ZS看一下这边是不是咱们的6个节点,是不是就出来了CD,Node01还是一样的我们进去看一下,LES

在这里面有我们的Comp,配置文件,CDComp,LES看一下ReduceConfigure,Cat来看一下,是不是一样的OK,走你那这边就配置完了我们所有的配置文件我们一次你是不是写了6个文件

这就是编程的魅力如果你真是傻乎自语写这6个配置文件就过你写很用户了编程的话,是不是一个负雄话就搞定了这就是为什么大家要学习编程

能够帮咱们节省大量的工作那这个地方的话,你可以看到我们这边的绑定地址也都是没有什么问题的暴露的端口也都是OK的6379,冒着党

好了,那现在我们已经把这个启动完了之后然后我们就该去启动了,对不对启动的话,我们就一个一个的启动还是一样的

我们这地方就不再写了东西都讲过回录来看一下第一个,DockerRound,GunP就是我们通过6371跟6379做个音色

我们在外部的话是6371方法然后GunP这边也是一样的Name,Reduce1,我们还会有R3456然后这边的话,GunV,Node1Node1,解点

然后下面的话网络是使用我们自己Reduce的网络GunD是后台运行GunIP,这边就是它绑定的一个IP号然后GunReduce的一个版本

这边就是我们用的一个5.09的一个Reduce版本然后干嘛,把它启动启动的时候呢通过咱们的一个ReduceConf来启动就行了这一行命令就是干嘛,就开启一个服务了嘛

通过后台运行起来咱们的一个Reduce服务来,回车它会先去下载这个服务下载完之后ReduceSeverGunConf这个就是我们讲Reduce时候给大家讲过的

那我们来看一下是不是启动起来了高跟PS来,当前这个Reduce已经开起来了这是咱们的ReduceGun1然后这边的端口音设也是没有问题的

那我们再来多开启几个比如说我们要开启2的话,就要改6372,把这个端口一定要变这个叫ReduceGun2这边是172

Node,第二个解点,第二个解点的配置那下面的话也是一样的这是咱们12的配置剩下的东西应该是不用变的来,回车

再开启一个那这就是咱们的第二个服务对不对这是6372然后音设到容器内的6379容器里面的肯定是不变的

然后这边的地址服务名称,下面的配置没问题,回车,就启动一个OK,启动完之后我们再来启动一个来,6373

然后6373然后再来一个Reduce3然后再来一个Node3Node3,然后03OK,我们把它再来启动一下

来,粘贴,启动OK,搞定,3也好了我们再来一个4吧44,我们要启动6个机子

其实你们也可以通过脚本来启动我只是这个地方,想让你们去理解我们是在一个一个启动来,4它把这个4也启动

OK,这个为啥不变成可以呀,你刚才把我这段弹码继续复制上,它就启动起来了我这弹码都写好了,就在这来,我们把它启动一下

来,回车它现在又启动了一个4那就启动了4个了再启动一个5那这边都改一下

5这是我们一次就启动6个你也可以发现你们正常训练就是你们的服务器是带不起来

那么多Reduce的但是用Docker就可以所以说你再次体验了它的一个清亮器,对不对再启动一个,粘贴过来

这边的话都是咱们的一个6,3,7,5,Reduce 5没有什么问题然后咱们再来一个6这就够了

我们这边6个接电就全部开起来了6个接电6个接电6,6

然后,刚6我们把它跑起来OK,我们来看一下好了,那现在6个接电都起来了我们来看一下

Docker,PS,走你那这边是不是我们的Reduce1,2,3,4,5,6那看懂了同学科研那现在我们就启动了6个服务

那接下来的话我们干嘛呢我们就要去创建集群了对不对,我们来创建一下集群来,Docker,ES,EC

我们先随便进入一个就可以了比如说,Gun ID,Reduce,Gun 1我们进入第一个并,那这个地方你输半十因为发现他找不到

Reduce这个镜像里面它是没有半十命令的它用的是SH命令这其实都是一样WinLinux上面就这么几个命令

来,进来之后它默认的PWD是Gun DataMulu来,列丝看一下这边的话,是不是就是咱们的一个节点配置跟咱们的

N,AOP,AOF的配置对不对,这是我们的持久化那我们之前说过那我们来看过一下在这里面的话,我们干嘛

我们就可以去创建我们的集群了我们可以连接一下通过Reduce,Gun,Clant来,这是我们的Reduce,Gun,Clant连接,然后我们要通过什么方式连接呢

通过Gun集群方式,对了Cluster集群的话,你要去创建吧Great,CREATCREAT,你要创建多少呢

就是我们干起来,1272点,咱们的38.0.11看一下,是不是38.0.11,OK我们来创建一个集群

大家都是把Reduce学完的人这个东西,闭这眼睛,看吧来,不知过来我们要创建一个大点的剧情这是咱们的01

然后再来一个02这边的话,我们要把端口也写上不然的话连接不上6379这个内部的端口都是6379

然后12的6379OK,再往下的话就是13咱们的一个6379379然后再往下走的话就是咱们的

14,来6379你们以后为了慢慢做做到架构的级别看这种代码,太轻松了来,6465

来,这边的话6379然后再往下走的话就是咱们的一个16咱们的一个6379OK,这些的话差不多就全跑起来这是我们的几个服务

然后最后的话集群的一个切片我们要先上刚刚ClusterReflect His这是我们之前讲过的,对吧那写个1就可以了

OK,看一下有没有什么问题没有问题的话,回车来,他现在这边就已经说你当年是不是要这么去练Must01,然后去连接Slot

这边是插场,就把它插进去123,三个主机,是不是三个重机,对不对,就把它连接上了这边问你,是否要这么配置呢你点击Yes就可以了

OK,他正在配置这个集群正在配置这个集群OK,集群配置完毕那我们现在来连接一下集群那我给大家把这段代码

复制过来就可以了OK,这是我们的一个连接集群的配置来,创建集群的配置,写到这儿的写到这儿

创建完之后,我们就可以玩了你们想怎么测怎么测来吧,给大家玩一下比如说我们来Reduce-Client我们来Reduce-Client

一定要GunC,Reduce-Client是单机Reduce-C,这个Cluster是集群那我们来GunC,那现在连接集群我们可以看一下咱们的集群信息ClusterInfo

这都是我们讲过的Info回车,那这边的话,你可以看到当前的话,集群的数量组从复制,擦草

这个信息是没有问题的你也可以看一下它的Notes来,这边的一个节点来,看得非常清楚吧对不对,我们这个地方有三组三重

这三个是重机这三个是重机,然后这三个是组织那现在组织跟重机都没有出现任何的问题,都是连接上的能看到同学可以啊

那现在我来在这里面Set一个值,来,Set一个A B我随便Set的,我在这边Set一个值,在这个容器里面干嘛应该有值给我分配,那回车一下

那在这个地方,你看到当前处理我们这个A B的是哪个服务来,就是我们这个13.679,而这是我们的第三个节点第三个节点是咱们的Must节点

来,找一下13,那这边的13是它在处理那现在,这个值是不是应该存到这个13里面的,也就是它那现在它的这个重机里面

应该也有这么一个值那现在如果它挂了这个重机是不是应该能替代上来这才叫高可用那我们来再玩一下

我们来把这个停了我们来玩一些过分的来,高可PS,我们来把这个刚才这个,来看一下是我们的这边放的是13的这个63.79

13的63.79是哪个端口来13,我们来找一下这边,看一下Reduce13,13是我们的3号吧63.79

来,我们把它停一下高可Stop,把这个Reduce3给它干了来,把它删了看到吗,我现在直接把这个容器停了停了的话,你们想一下

这个Gate到,能Gate到Master的值吗这个主机都已经挂了能理解我意思啊这个主机挂了,我现在Gate一下你们猜一下Gate A能不能Gate到

如果高可用,集群搭建成功架构是OK的那Gate A它的重机会自动替代主机,对不对,拿到这个值回录,看一下最终的一个值

它在等待啊,它现在要去去判断,看一下它有没有这么一个值这里面的很多东西,是非常有意思的你看它这个地方,是需要一些时间它可能这边正在去选举啊,等等

等它,等它彻底跑起来就可以了我没有停错吧,这边是一个07B什么开头的那我们这边的话,应该也是差不多的Reduce3,我就怕它刚才停错了再来联一下

Gate A,回车看到了同学扣音,现在是不是Gate到了我虽然说那个服务器已经停了它在哪地方Gate到,它在14Gate到的这个14是谁来的,是13的重机

完全没有什么问题来,Docker,Nodes,看一下结果不是Docker,一激动,这输Docker了来,Class Nodes,来这个不要太爽测试的

高可用机器搭建,分分钟搭建好它这个地方已经显示了,当前我们E3,Must,Fail,故障转移了现在E4,它是不是变成我们的Must了看到同学扣音,所以说

搭建机群快不快,使用咱们的Docker之后搭建机群,就是分分钟的事情来,Docker,搭建Reduce机群,搭建Reduce机群

完成,对不对所以说,就是我们使用了Docker之后我们使用了Docker之后所有的技术都会慢慢的变得简单,起来

我们要用它的话,那简直是太快了,对不对我们使用了Docker之后OK,来,给大家把视频停下大家下去干嘛,自己要去把Reduce机群

部署一下,OK,给大家停一下

### P39：SpringBoot 微服務打包 Docker 鏡像

我们再来做最后一个小测试就是我们以前写了那么多微服我能不能把它打包成一个Docker经向呢对不对,我们要把Spring Boot微服打包成Docker经向发布运行这个才是我们最终也想去玩的一个行

我们来创建一个微服来,我们就手中创建一个嘛这个东西写起来那不就是分分钟的事情对于大家来说肯定也是这样的那这边还是这样的,我们来创建一个Spring Boot项目

创建完之后呢,我们去写一个基本的Hallower的就可以了然后我们来测试一下打包,把它发布到咱们的经向中看一下能不能操作这边的话,我就是直接名字都不想改了,就叫Diamond吧来,直接Nexter

我都突然到这个经济了,然后选中咱们的外部我过一个外部就好了你看这新版本都用到这了不知道能不能下来下来,Nexter然后我们就把它放到桌面上吧,等会打个包用就可以了

我们就随便建一个Spring Boot项目第一步,够近咱们的Spring Boot微服项目一般也就是提供接口嘛,第二步来,打包,应用然后第三步,边写Dock Fail

第四步,构建进向第五步,发布运行就这么简单,来看一下怎么做首先我们来写一个基本的应用那这边这个经向还在下呀

这个又更新了,这个家伙我们换一个办法,20205应该是有的这样就不用下了,不用等来看一下,你看这个或者202.7其实我都有,没错的话

双心想,就是我们不让这个去耽误时间了我们来直接写一个经向我们在我们这个demo下面,等它加载出来这个Spring Boot加载加载完之后呢,这边是一个加瓦程序

这是我们一个启动程序,我们来写个Ctrl 测试来,Ctrl 测试Ctrl 测试还是记得一定要关注3.0哦来,Hello,Ctrl

我们就写这样一个服务,我们来看一下能不能把它发布并且测试成功,Rest它,Ctrl是不是一写到加瓦蛋码,你们瞬间感觉好清晰呀我的妈呀来,Hello

不要沉迷于天天写加瓦这些基本蛋码加瓦这个东西,其实你学通了非常快来,Request它,Mapping你看一下,我这整套课程你们学的话半年多了但是我讲的话,其实很快就讲完了

就好比我讲导渴,你们下去看的话要看很久来,我其实两天就讲完了,对不对Hello,KwangshengOK,假设等会儿这个能访问成功就是没问题的我们先本地自测来访问一下,启动一下

这不认识8080吧,应该是没问题的来,我们来看一下这个能不能访问成功如果能够访问成功是没有问题的8080,我们来测一下,现在本地自测你这个经向能通过来,Local Host

来,8080走你,写了8008080你看,这看到这就没问题了,其实Hello走你,Hello,Kwangsheng,反回成功

这个接口是能用的,我们接下来就把它打个包来,在这边点击Package我们来构建,给它打一个包就可以了看一下,一定要BuildsXS才可以BuildsXS打包成功

打包成功了之后,我们在这边就可以看到一个经向了来,Target里面,这边就是我们的Demo这就是我们的一个架包,我们把它干嘛拿出来还是一样的,在这个地方,我们来在这个地方测试一下能不能运行来,加发,上架

来,下载,执行我们的Demo,走你如果在本地这个地方也能启动起来测试成功的话那这个服务就是没有问题的了也没有问题,成功启动好嘞,我们这个架包完美了

那现在有了这个架包之后,我们要把它发挥到DocFail里面我们肯定要写个DocFail,对不对那这个时候,我们需要在IDL里面装一个插件不然的话,它不高量你在这地方点击Package,搜一个插件,就叫Doc

那就是它,没有下载的,就去这边下在这地方搜Doc,就是这个人,用的最多的这个其实,没有一个必要,非要把它下载下来那下载下来,它其实可以连接远程仓阔你们在Setting这边可以连接Build

我们以前不是连Maven,连Gate嘛这个地方就可以连Doc,比如说你要去连接远程的仓库你点一下,然后这地方就了你要去连接仓库的一个地址我就不教这个了,我们平时没怎么用,懒得教这个,没什么意义

那我们在这边去写个DocFail,你下了那个插件你在这边写DocFail的时候,它会高量看到了同学扣验就是这个文件它是高量的那我们在这里面就可以写我们DocFail的一个脚本了对不对,那这个时候还是一样的

它是有提示的,比如说加瓦八我们基础处进向是通过加瓦八来进行构建的然后我们要把当前的一些文件干嘛,拷贝进去我把当前目录下的新点儿匠来,新点儿匠,就是当前目录下的所有的价包

都给它拷到项目下面,对,我把它叫做App价也是最后我深沉的,长一个应用那就记住,服务器的话我们应该就是只发布这一个来,就是把我们这个Doc点价包以及跟我们的DocFail发布上去

就你提交到网上的时候就只发布这两个东西其他东西都不要发布,然后把它打包到咱们的金箱内部然后再CMD,一般的话你要在这儿写下东西对不对,你可以刚刚sure,比如说你要写个音设服务的地址,sure点pot,ser,ver点pot

那之后的话别人也可以通过这个来改对不对,比如说8080,某人就是8080然后再往下走的话,比如说我们要暴露钻口expose,来,我们要对外暴露8080,然后最后干嘛呢来,这边写安吹炮蝶,我们要执行一个命令

执行什么命呢,加碗,刚加命令是不是,觉得非常亲切的同学扣一样是不是,只要你们把前面的也写完了,这些太轻松还是一样的,我们要执行谁呢,执行当前项目下的App刚加碼来,拿这,App第二下

OK,那现在假设这个脚本能够成功首先构建咱们的金像,然后copy了咱们的金像确定也copy过去了,然后这边CMD也能够成功上传校酒端过去呢,暴露,加碼钻,干嘛,也能执行那这边的文件就变现完了,下一步就是把我们的金像发布上去

那我先把这边的Docker都给它停了来,我们把刚才的Reduce集群给它停了,我们这边起了6个Reduce来来,我们把它先清一下,还是一样的,DockerRemove我们把它都移了,把这儿都移了来,DockerPS,我们把它之前先移掉

然后我们把这个copy上来就可以了我们现在进我们自己的目录,比如说我还是在后幕目录下里面我来Test,例子看一下我再建一个目录,建一个咱们的IdeaMuLOK,那这是我们MacDear一个IdeaMuL

我们建一个IdeaMuL,我们进这个IdeaMuL我们把文件就放在这里面,这里面没东西,打包上传一下你上传到服务器的就是你的架包,对不对,跟DockerFair但是你不要傻傻的把你的架包发给别人我们来把它启动一下

我们把它启动一下我们把它传上去文件在这里,我们只需要把我们这个DockerFair跟咱们的架包丢上去就可以了,这是咱们的架包还有一个DockerFair,这个架包还挺大的,你看16兆

就是如果你的架包非常小,就说明有问题架包一定是很大的,因为它要包括咱们很多内置的依赖你们的架包不可能很小,所以说这是WiFi服务的一个弊端就是它要启动很多这个,但是比较快你看一下,这边快上传完了,等它上传完毕了

来,上传完毕了,列示看一下,那我们这边的数据就过来了OK,看到我这边的数据同学可以那现在我们是不是就要通过这两个东西干嘛打一个包,能理解我这个意思的同学,扣1那现在的话DockerFair已经有了,我们就直接Build就好了

DockerBuild,对不对,DockerBuild,就是我刚剃那我这边的话,我就要我给它生成一个景象,我就叫狂声666OK,那最后来个第二那Build,走你,你看它真的Build的,第一步From,加瓦8它就去下载这个东西,下载完了之后呢

它这个命令还是跟着我们DockerFair走的,最终要把这个包打包进去那一启动就可以执行了,对不对,来看一下看一下能不能把它成功搞定啊,这个能搞定的话以后你们还有啥项目不能放到Docker上啊,轻轻松松,so easy,对吧来,等它把这个编译完

还是比较快啊,因为是内容,所以说等不了很久卡的话肯定是你们自己的问题啊,我这直播间怎么可能卡来来,拷贝完毕,运行完毕,Build成功,来,DockerImage看一下,来,狂声666,进行成功搞定那我们来运行一下,DockerRuang,GangD,GangP,GangGangName

对不对,然后IDL,狂声666狂声666,狂声的外部吧SpringBoot吧,狂声SpringBoot,外部OK,我们来看一下,起个这么长的名字,没有什么用啊然后最后的话是执行我们这个镜像,我们这个镜像叫狂声666

OK,如果能够成功的话,那是不是就能打印出一内小侠来,回车来,启动成功,那我们来试一下CUI了,我们来访问一下Localhost先看一下它的一个镜像地址吧,来,Localhost看一下能不能访问成功,来,Localhost这是我们的一个,32779,32779,回车

来,看到了吗,我们这个地址是不是成功了我现在访问这个地址的Hello请求,狂声点Hello这个就是我们的404错误页面的返回码嘛下面就是我们的一个具体的地址嘛,现在是不是就成功了也就是说,未来你把项目给别人的时候,别人干嘛

下载下来的是一个镜像,而不是一个驾包那这个镜像里面他们要用的话,直接运行下来了以后我要写一个项目,那我要把这个项目打成一个镜像我把它铺使到公用人仓库,你们直接一个铺拉下来,一个软运行,两步搞定那所以说,就非常简单,那以后我们使用了

干嘛,使用了,来,我们使用了咱们的一个导渴之后那给别人的,来,给别人的交付,给别人交付的就是一个镜像就可以了,就是一个镜像即可那也可能有很多镜像,你们要写很多镜像,对不对那这个地方给大家写个预告,预告

就是如果有很多镜像,来,有很多镜像那怎么样去处理呢,假设我这个地方有100个镜像我每个100个镜像都去启动,然后100个镜像分别维护是不是非常麻烦,对不对那这么多镜像怎么维护它呢

所以说,真正的企业实战呢,是不是在这边还要去学3个东西一个是咱们的compose容器编排,还有一个就是咱们的集群部署那这两个东西的话,就非常重要那这要compose的话,编排会搞定了那swarm,其实它非常简单,它其实也就是个银样matter

其实swarm我也觉得非常简单,它就是一些容器的编排部署这个darkswarm就是学完之后呢,再往后面学一下kbs相关的了darkswarm其实就相当一个非常精简的kbs它比kbs简单,命令也少一点,kbs比它功能强大多了所以说,一定要这么样子去把它学下去

那这就是我们后面的预告,假设这一期或者这个系列的视频播放量非常高的话我们就出时间把下面三个播了如果这个播放量和分享量并没有那么高的话,或者关注量也没有那么高的话那这三个就暂时夭折OK,那这是我们的一个预告

那其实到了这里啊,来,到了这里,我们已经完全够用了我们已经完全够用了,来,够用了对不对,darkswarm就是以后别人再问你新darkswarm相关的东西,你闭着眼睛给大家想你绝对能给它统

就是你至少碾压上面,视面上的所有的颗是没问题了,对吧OK,那这是我们的一个基本的步骤那接下来的话,我希望大家以以前做的项目干嘛来都可以通过darkswarm的方式,打包成一个镜像,然后把它发布运行OK吧,这是我们的这个结尾

那我们最后再来一个小节,来给大家提一下小节我们来说一下资料怎么获取啊,最后

### P40：結語

OK,来回顾一下我们这两天的直播也都是答应大家了,从7点播到12点现在的话都已经0.04分了,对吧我们来把这个全部总结一下到目前为止的话,Docker是什么东西

大家完全没有问题了,Docker使用大家觉得非常爽了那再来的话就是巩固练习,对吧这个Docker的学习就跟Linux的命令是一样的我就希望大家每天去敲,你每天去用那很快就把它掌握了,如果你不用

那很快就把它遗忘了那最重要的话,其实我认为入门阶段就是这些命令,这些命令都会了然后把我这些作业都认真做了的人其实没有什么问题

那接下来的话,这几个东西都非常重要我们后面讲的,比如说Docker经向讲解它的经向原理,对不对数据券,数据是怎么挂在的DockerFair,如何自己构建一个这样

Docker网络,它里面那么多神奇的网络到底是怎么构建成功的最后你们怎么样把自己的项目打包经向那这些我都把它统称为Docker作为一个正常人该了解的

那当然很多人连这个水平都没达到所以说大家还是认真的帮我去分享一下这个视频,至少所有人能达到这个基本的水平嘛有了这个水平,我们才能往企业区区进展否则的话,职业现在讲企业,也没什么太大意啊

这里面都是集群部署一上来,你们至少要,来这个刚刚,这要准备六台,咱们的一个阿里云服务器就是一台服务器是搞不定的至少要六台服务器,一上来就是六台

能理解我的意思所以说这是真正企业级实战我们不会再像之前这样的现在我们的单机完全够玩了,对不对OK,那这些东西都是我们学习的一个重点

那现在大家就知道,未来该怎么去学习了那还是说一下,视频点赞三连最基本的,对不对然后这个资料的话,最终我有可能会发布到咱们的马云就是,假设我视频同步发布的时候,我就会发布到马云就是在这边,还是放到这个资料里面

还是希望大家所有人把这个马云帮忙关注一下因为马云这边的话,对我推荐也是挺高的然后没有去死大的,记得也死大一下然后公众话也是一样的,B站也是一样的那如果真的觉得我有帮助到你的话

直播间的话,这边的赞赏码或者微信的赞赏码也可以给我一定的支持最后还是感谢大家的收看那记得赞,关注,支持,是吧,给大家听一下啊

