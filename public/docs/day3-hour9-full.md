# Day 3 第九小時：容器生命週期管理

---

## 一、前情提要（2 分鐘）

上堂課講了映像檔的分層結構。

這堂課講容器本身：
- 容器的完整生命週期
- 容器狀態轉換
- 資源限制
- 重啟策略
- 監控與健康檢查

---

## 二、容器生命週期（12 分鐘）

### 2.1 容器狀態

容器有以下狀態：

| 狀態 | 說明 |
|-----|------|
| created | 已建立，尚未啟動 |
| running | 執行中 |
| paused | 暫停 |
| restarting | 重啟中 |
| exited | 已停止 |
| dead | 無法移除的錯誤狀態 |

### 2.2 狀態轉換圖

```
                docker create
        ┌──────────────────────────┐
        │                          ▼
        │                      [created]
        │                          │
        │                    docker start
        │                          │
        │                          ▼
        │    ┌───────────────► [running] ◄───────────────┐
        │    │                     │                     │
        │    │        ┌────────────┼────────────┐        │
        │    │        │            │            │        │
        │    │   docker pause  docker stop  docker kill  │
        │    │        │            │            │        │
        │    │        ▼            │            │        │
        │    │    [paused]         │            │        │
        │    │        │            │            │        │
        │    │  docker unpause     │            │        │
        │    │        │            │            │        │
        │    └────────┘            ▼            │        │
        │                      [exited] ────────┘        │
        │                          │                     │
        │                    docker start                │
        │                          │                     │
        │                          └─────────────────────┘
        │
        │                    docker rm
   docker run ◄──────────────────────
   (= create + start)
```

### 2.3 各狀態的詳細說明

**created**

```bash
docker create --name my-nginx nginx
```

容器被建立，但程序沒有啟動。
- 分配了 ID
- 準備了檔案系統
- 但沒有執行任何程序

**running**

```bash
docker start my-nginx
# 或
docker run nginx
```

容器的主程序正在執行。

**paused**

```bash
docker pause my-nginx
```

所有程序被暫停（使用 cgroups freezer）。
- 程序狀態保留在記憶體
- 不消耗 CPU
- 恢復後從暫停點繼續

```bash
docker unpause my-nginx
```

**exited**

```bash
docker stop my-nginx
```

主程序已經結束。
- Exit code 0 表示正常結束
- 非 0 表示錯誤

```bash
# 查看 exit code
docker inspect -f '{{.State.ExitCode}}' my-nginx
```

**dead**

這是錯誤狀態，通常是 Docker 嘗試刪除容器但失敗。
可能原因：檔案系統忙碌、資源被鎖定。

### 2.4 docker run = create + start

```bash
docker run nginx
```

等同於：

```bash
docker create nginx
docker start <container_id>
```

用 `create` 的場景：
- 想先建立，之後再啟動
- 需要在啟動前修改設定

---

## 三、docker inspect 詳解（10 分鐘）

### 3.1 基本用法

```bash
docker inspect my-nginx
```

輸出 JSON 格式的完整資訊，非常長。

### 3.2 常用資訊提取

**容器狀態**

```bash
docker inspect -f '{{.State.Status}}' my-nginx
docker inspect -f '{{.State.Running}}' my-nginx
docker inspect -f '{{.State.StartedAt}}' my-nginx
docker inspect -f '{{.State.FinishedAt}}' my-nginx
docker inspect -f '{{.State.ExitCode}}' my-nginx
```

**網路資訊**

```bash
docker inspect -f '{{.NetworkSettings.IPAddress}}' my-nginx
docker inspect -f '{{.NetworkSettings.Ports}}' my-nginx
docker inspect -f '{{.NetworkSettings.Gateway}}' my-nginx
```

**掛載資訊**

```bash
docker inspect -f '{{.Mounts}}' my-nginx
docker inspect -f '{{json .Mounts}}' my-nginx | jq
```

**環境變數**

```bash
docker inspect -f '{{.Config.Env}}' my-nginx
```

**啟動命令**

```bash
docker inspect -f '{{.Config.Cmd}}' my-nginx
docker inspect -f '{{.Config.Entrypoint}}' my-nginx
```

### 3.3 格式化輸出

用 Go template 語法：

```bash
# 多個欄位
docker inspect -f 'Name: {{.Name}}, Status: {{.State.Status}}' my-nginx

# 迴圈
docker inspect -f '{{range .NetworkSettings.Networks}}{{.IPAddress}}{{end}}' my-nginx

# JSON 輸出
docker inspect -f '{{json .Config}}' my-nginx | jq
```

### 3.4 比較多個容器

```bash
docker inspect container1 container2
```

---

## 四、資源限制（15 分鐘）

### 4.1 為什麼要限制資源

- 防止單一容器吃光所有資源
- 多容器環境的公平分配
- 避免 OOM（Out of Memory）影響系統

### 4.2 記憶體限制

```bash
docker run -d --name limited-app \
  --memory 512m \
  --memory-swap 1g \
  nginx
```

**參數說明**

| 參數 | 說明 |
|-----|------|
| --memory, -m | 最大記憶體 |
| --memory-swap | 記憶體+swap 總和（-1 表示無限制） |
| --memory-reservation | 軟限制（優先被回收） |
| --oom-kill-disable | 禁止 OOM killer（危險，不建議） |

**單位**

- b, k, m, g（bytes, kilobytes, megabytes, gigabytes）

**查看容器記憶體使用**

```bash
docker stats my-nginx
```

### 4.3 CPU 限制

**方法一：CPU 份額（相對權重）**

```bash
docker run -d --cpu-shares 512 nginx
```

預設值是 1024。512 表示只有預設的一半權重。

只在 CPU 競爭時生效。如果系統不忙，還是可以用到更多 CPU。

**方法二：CPU 核心數**

```bash
docker run -d --cpus 1.5 nginx
```

最多使用 1.5 個 CPU 核心。

**方法三：指定 CPU 核心**

```bash
docker run -d --cpuset-cpus 0,1 nginx
```

只在 CPU 0 和 1 上執行。

適用於 NUMA 架構優化。

### 4.4 磁碟 I/O 限制

```bash
docker run -d \
  --device-read-bps /dev/sda:10mb \
  --device-write-bps /dev/sda:10mb \
  nginx
```

限制每秒讀寫 10MB。

### 4.5 修改執行中容器的資源限制

```bash
docker update --memory 1g --cpus 2 my-nginx
```

不需要重啟容器。

### 4.6 查看資源限制

```bash
docker inspect -f '{{.HostConfig.Memory}}' my-nginx
docker inspect -f '{{.HostConfig.NanoCpus}}' my-nginx
```

---

## 五、重啟策略（10 分鐘）

### 5.1 什麼是重啟策略

當容器停止時，Docker 該怎麼處理？

- 什麼都不做
- 自動重啟
- 只在錯誤時重啟

### 5.2 四種重啟策略

| 策略 | 說明 |
|-----|------|
| no | 不重啟（預設） |
| on-failure[:max-retries] | 只在非 0 exit code 時重啟 |
| always | 總是重啟，包括 Docker daemon 重啟後 |
| unless-stopped | 總是重啟，除非手動停止 |

### 5.3 使用範例

```bash
# 總是重啟
docker run -d --restart always nginx

# 失敗時重啟，最多 3 次
docker run -d --restart on-failure:3 my-app

# 除非手動停止，否則重啟
docker run -d --restart unless-stopped nginx
```

### 5.4 always vs unless-stopped

兩者的差別在手動停止後 Docker daemon 重啟的行為：

| 場景 | always | unless-stopped |
|-----|--------|----------------|
| 容器 crash | 重啟 | 重啟 |
| Docker daemon 重啟 | 重啟 | 重啟 |
| 手動 stop 後 Docker 重啟 | **重啟** | **不重啟** |

`unless-stopped` 比較合理——你手動停的容器，重開機後應該保持停止。

### 5.5 修改執行中容器的重啟策略

```bash
docker update --restart unless-stopped my-nginx
```

### 5.6 查看重啟策略

```bash
docker inspect -f '{{.HostConfig.RestartPolicy.Name}}' my-nginx
```

---

## 六、docker stats 監控（5 分鐘）

### 6.1 即時監控

```bash
docker stats
```

輸出：

```
CONTAINER ID   NAME       CPU %     MEM USAGE / LIMIT   MEM %   NET I/O          BLOCK I/O   PIDS
abc123def456   my-nginx   0.00%     7.934MiB / 512MiB   1.55%   1.45kB / 0B      0B / 0B     5
```

**欄位說明**

| 欄位 | 說明 |
|-----|------|
| CPU % | CPU 使用率 |
| MEM USAGE / LIMIT | 記憶體使用 / 限制 |
| MEM % | 記憶體使用率 |
| NET I/O | 網路輸入/輸出 |
| BLOCK I/O | 磁碟讀/寫 |
| PIDS | 程序數 |

### 6.2 監控特定容器

```bash
docker stats my-nginx
docker stats my-nginx my-mysql
```

### 6.3 單次輸出（不持續更新）

```bash
docker stats --no-stream
```

適合寫腳本收集數據。

### 6.4 自訂格式

```bash
docker stats --format "table {{.Name}}\t{{.CPUPerc}}\t{{.MemUsage}}"
```

---

## 七、健康檢查（5 分鐘）

### 7.1 什麼是健康檢查

容器在跑不代表服務正常。

健康檢查讓 Docker 定期測試容器內的服務是否正常。

### 7.2 docker run 設定健康檢查

```bash
docker run -d \
  --name web \
  --health-cmd "curl -f http://localhost/ || exit 1" \
  --health-interval 30s \
  --health-timeout 10s \
  --health-retries 3 \
  --health-start-period 40s \
  nginx
```

**參數說明**

| 參數 | 說明 | 預設 |
|-----|------|-----|
| --health-cmd | 健康檢查命令 | - |
| --health-interval | 檢查間隔 | 30s |
| --health-timeout | 超時時間 | 30s |
| --health-retries | 連續失敗幾次算不健康 | 3 |
| --health-start-period | 啟動緩衝時間 | 0s |

### 7.3 查看健康狀態

```bash
docker ps
```

STATUS 欄會顯示：`Up 5 minutes (healthy)` 或 `(unhealthy)`

```bash
docker inspect -f '{{.State.Health.Status}}' web
```

### 7.4 查看健康檢查日誌

```bash
docker inspect -f '{{json .State.Health}}' web | jq
```

會顯示最近幾次檢查的結果。

---

## 八、本堂課小結（1 分鐘）

這堂課學了容器生命週期管理：

**容器狀態**
- created → running → paused/exited → dead
- docker run = create + start

**docker inspect**
- 取得容器完整資訊
- 用 Go template 格式化輸出

**資源限制**
- 記憶體：--memory
- CPU：--cpus, --cpu-shares
- 可用 docker update 動態調整

**重啟策略**
- no / on-failure / always / unless-stopped
- 生產環境建議 unless-stopped

**監控**
- docker stats：即時監控
- 健康檢查：--health-cmd

下一堂：容器網路基礎。

---

## 板書 / PPT 建議

1. 容器狀態轉換圖
2. 資源限制參數表
3. 重啟策略比較表
4. docker stats 輸出範例


---

## 📺 補充教材：狂神說 Docker

> 以下內容來自【狂神說Java】Docker 教程系列，作為本節的補充參考資料。

### P15：作業二：部署 Tomcat

<sub>[00:00-00:12]</sub>
我們來看一下，有了剛才的 nginx 的基礎之後，我們來裝 Tomcat。那我就直接一波錄到底，我們把它寫到這，clear。我們會裝越來越複雜，慢慢的給你裝。

<sub>[00:12-00:30]</sub>
首先我們來直接運行，我們這次就不去 pull 下載了。我們說 `docker run` 是不是還會自動去幫我們下載呀，我這地方就直接 run 下載。比如說 `docker run` Tomcat，那我把它下載下來的話，比如說 Tomcat 我可以指定一個版本，比如說幾點幾。那還是要去看一下版本好吧，你不想用最新的你就去看版本號。

<sub>[00:31-00:56]</sub>
我們來看一下，去 Docker Hub 搜一下，然後這個 Docker Hub 也告訴你了它怎麼去使用。我們來說一下這邊的 run 吧。很多小細節這個官方文檔一定要翻爛，我都快把它翻爛了。Tomcat 我們搜索到了之後，下面有些版本，你看這個 Tomcat 的版本多的要死是吧，都已經到 10 點幾了，你們很多人還在用 9 點幾。那這個 Docker Hub 你看這個版本簡直了，太多了你們都可以使用。

<sub>[00:56-01:15]</sub>
那再往下走的話，它這個地方又告訴你了怎麼樣去使用鏡像。這個地方有一句話叫 `docker run --rm Tomcat:9.0`。來把這個官方的坑說一下，這是官方使用。那官方是這麼使用的，`docker run -it` 我們都知道直接進去運行嘛。

<sub>[01:15-01:56]</sub>
那這個 `--rm` 是什麼，來說一下這個意思。我們之前的啟動都是後台，對不對？然後停止了容器之後，咱們的容器還在，容器還能查到。那有時候我們測試的時候，我們想這個東西用完就關對不對。那這個 `docker run --rm` 就是我們把這個容器啟動了之後，它會再把它刪掉，就是用完之後直接刪掉。

<sub>[01:48-02:09]</sub>
所以說 Docker 官方這種方法一般用來測試，**用完即刪**。我們也可以來體驗一下，用完就刪除，自動的。我們來運行一下 Tomcat，大家非常熟悉，走你。首先它要去下載 Tomcat 對不對，等會它就把它刪掉。它現在正在下載 Tomcat 這個東西，下載起來非常快 50 幾兆嘛，以我們阿里雲的速度分分鐘下載完。

<sub>[02:17-02:58]</sub>
這個下載完了之後呢，你們就可以看到它跳下去啟動，啟動的話就是一個 Tomcat 的日誌，大家就非常熟悉了。來我們來看一下，來我們來看一下這邊，最後一個包下載完就可以了。等它下載完，這個也是內網下載的，所以說你們可以看到這個下載非常非常的快。那現在下載完了，這是不是就是我們正常去啟動 Tomcat 的一個樣子，你看啟動成功了，現在是 8080 對不對。那現在我把它停掉，比如說我 Ctrl+C，那我停掉之後按照剛才的邏輯 `docker ps` 我們應該能找到它。

<sub>[03:03-03:20]</sub>
`docker ps`，看一下我的 Tomcat 在嗎？不在對不對。所以說咱們官方給我們的這種方式就是**用完即刪**，我們不建議這樣用，前期測試的時候可以這麼用。我們現在的話還是老老實實先下載然後再啟動。

<sub>[03:20-04:05]</sub>
我們來下載啟動一下按照我們的邏輯。首先 `docker pull` Tomcat，把這個命令都學起來，要部署 Tomcat 的就直接 `docker pull` Tomcat，現在我就把它部署起來了。那現在的話它就過去下載，剛才下載過了現在又得去下載，因為剛才 `--rm` 它把鏡像全部刪了。我們不用這個版本吧，直接下載 Tomcat 最新版的，這樣的話它找得快。來你看這個就直接找過來了，我們這方就直接找最新版的。

<sub>[04:05-04:55]</sub>
來讓它下載，下載完之後我們把它啟動運行。啟動運行我們沒有指定版本號它就是最新的。我們來看一下它正在下載。OK 它說這個地方已經下載好了 Tomcat 對不對，那我們可以 `docker images` 看一下。那這邊有我們的 Tomcat 9.0 以及我們的最新版。我們這邊已經下載了 Tomcat，我們就把它啟動了。還是一樣的 `docker run` 啟動對不對，以後台的方式啟動，`-d -p` 暴露端口。比如說我現在來一個 3355 吧，我們要把容器外部的 3355 端口給容器內部的 8080 做一個映射。那這個時候我們通過外部的 3355 是不是就能訪問到 8080 的內容了。

<sub>[04:59-05:36]</sub>
來這時候給它起個名字，比如說這是咱們的 tomcat01，那我就可以啟動很多個 Tomcat 對吧，我們通過我們的 Tomcat 鏡像來啟動。走你，現在的話我們的 Tomcat 就啟動成功了。那現在啟動成功了，如果在本地測試沒有問題，我們來外部直接訪問一下。我們來訪問一下咱們的公網地址 3355 回車。

<sub>[05:25-06:13]</sub>
現在的話發現一個 404 Not Found，說明這個 Tomcat 不是完整的。記住一句話就是**外網已經可以訪問了，測試訪問沒問題**。那這個地方為什麼沒有顯示 Tomcat，來我們來進去一探究竟。`docker exec -it` 進入我們這個 tomcat01 剛才我們啟動這個容器，`/bin/bash`。OK 那我們列示看一下，這是不是就是我們非常熟悉的。來你看到它連 `ll` 命令都沒有，它命令都已經被閹割了。

<sub>[06:00-06:45]</sub>
你在這個地方可以看到這裡面是有我們的 webapps，我們來看一下我們的 Tomcat 項目都放在這。我們來 `cd webapps` 看一下，這裡面很多命令都沒有。cd 進去之後 `ls`，你看一下這個目錄是空的，對不對。那這個地方就出現了一個問題，然後我們進入容器。進入容器之後發現這個東西它是被閹割過的。**發現問題：第一個就是 Linux 命令少了，第二個就是它沒有 webapps。**

<sub>[06:45-07:32]</sub>
沒有咱們的 webapps 也就是會直接導致我們訪問它顯示了一個「未找到」，因為沒有網站嘛所以它肯定找不到。那這個原因是什麼原因呢？就是因為阿里雲的原因還有它的一個鏡像的原因。其實它默認是**最小的鏡像**，也就是它會把所有的不必要的都剔除掉。剔除之後它只用保證最小環境，所以它可以用了就算了，它是保證它的一個**最小可運行的環境**。

<sub>[07:29-08:09]</sub>
那這個時候我們就可以把它搞過來。在這個地方呢我也細心的去研究過它，來給大家玩一下。看一下 clear 命令還是有的，那現在的話 webapps 下面，我們來回到這個容器裡面還有一個目錄叫 `webapps.dist`，它的輸出目錄。它的文件藏在這裡面，我們可以把這裡面東西複製過來，這也是可以的。我們來看一下 `webapps.dist`，我們來 cd 進去一下。

<sub>[07:59-08:43]</sub>
那在這裡面的話發現我們那個 ROOT 是不是在這裡呀，我們要是把這個文件複製過去是不是就可以用了。那或者是把這個 webapps.dist 給它改個名字，這都是可以的。那這個地方的話兩種方式都可以，那你們可以退出比如說 `cp -r webapps.dist/* webapps/`。來看一下把 webapps.dist 下面的所有東西給它拷貝到我們當前的 webapps 目錄下。

<sub>[08:28-09:03]</sub>
如果能夠拷貝成功的話，來拷貝成功的話我們再進入我們的一個 webapps 看一下。來那這邊的話就有我們的目錄了對不對，現在我們再來刷新一下。就是說白了我們剛才這個 webapps 項目裡沒有項目，現在我們部署了一些項目。那部署的網站它現在就可以訪問了，我們來看一下能不能訪問，刷新。來看到的同學可以，我現在是不是通過 Docker 把我們的一個項目部署上去了，你們自己寫項目是不是在裡面去改就好了，一模一樣的。

<sub>[09:03-09:43]</sub>
OK 那這個地方的話說一下，再思考一個問題。我每一個練習上面都會讓大家思考問題，就是我們以後要部署項目，如果每次都要進入容器是不是十分麻煩？這個東西是非常費時間的，我們不想費進去。就是還是那種話，要是有一種技術，我們可以在容器外部提供一個映射路徑，達到在容器外部修改就可以進行發佈多好。

<sub>[09:29-10:08]</sub>
容器外提供一個映射路徑比如叫 webapps，那我們在外部放置項目就自動同步到內部就好了。有這麼一個機制就非常非常爽。現在的話這個 Docker 容器它是包含了我們的 Tomcat 加我們的網站的。那現在我去把這個容器刪了，那我刪庫跑路了。那以後的話你們把這個 Docker 裡面放的是 MySQL，你原來是上 MySQL 才刪庫跑路，現在你把這個容器刪去刪庫跑路了。

<sub>[10:16-10:46]</sub>
這個東西不科學對吧，所以說我們之後要把它解決掉。這是我們的第二個作業讓大家部署 Tomcat，簡不簡單啊？又給大家去講了一些新的小知識點，比如說剛剛 `--rm` 以及咱們進入容器去進行觀察。那做完這個作業的時候咱們再來做一個作業，就是作業三，我們來部署咱們的 ES 加咱們的 Kibana。

---

### P16：作業三：部署 ES + Kibana

<sub>[00:00-00:23]</sub>
我們來停一下，我們怎麼樣去部署我們的 ES 加上我們的 Kibana 呢？這個怎麼樣去弄，對不對？那還是先思考一個問題，這個 ES 可不像那種基本的東西，它暴露的端口很多。而且第二個就是 ES 十分的耗內存，這個是非常麻煩的。

<sub>[00:26-00:48]</sub>
而第三個就是 ES 的數據一般需要掛載出去，需要放置到安全目錄，就是我們要讓它安全，所以說我們這方也得掛載。這個掛載我們剛才說了，現在我們沒學，所以說你們就去思考。那前面這兩問題怎麼解決了？我們一個一個來看。首先思路還是一樣的，我們繼續來研究，就是我多做幾遍大家就徹底懂了。

<sub>[00:48-01:22]</sub>
Elasticsearch，來搜索一下。Elasticsearch 我們再往下翻，這邊就告訴你怎麼樣去用，這是它的一個默認的一個文檔。我們去看一下，這是一個官方鏡像，下面有個 How to use，然後它就是如何使用讓你點擊這個地方，你點擊這個地方它就會跳轉到 Elastic 的官方文檔。我們來點一下，等它這方跳過去。然後下面的話也有一個運行的命令，其實我們可以把這個命令複製過來。

<sub>[01:49-02:22]</sub>
Elasticsearch 下面這個地方有一個啟動命令，我們把這個命令取過來。取過來之後你發現這玩意，它可不是一般的，它啟動了非常非常多的東西。比如說 `docker run`，然後 Elasticsearch，然後這個地方有個坑叫 `--net somenetwork`，這個我們還沒有講，這是什麼東西？那這是一個**網路配置**，我們之後學到 Docker 網路時候再講，Docker 網路非常重要。那我們先把它去掉不用它。

<sub>[02:13-02:50]</sub>
然後它暴露的是 9200 跟 9300，然後這邊的話是一個集群，默認的話它是單個節點嘛對不對。然後啟動的話是我們當前的版本 7.6.2。那以前你們要去安裝 Elasticsearch 非常麻煩，現在用了 Docker 之後只需要一步對吧。那我們來啟動一下 Elasticsearch，走你。那它現在就去下載這個鏡像了。

<sub>[02:53-03:15]</sub>
下載之後我們會發現一個問題，這個東西極其的卡，就卡的要死的那種。集群怎麼弄？這個問題問得好，我們後面會講。我們把這個地方，我們把這個瀏覽器關了吧，關了重新開一下這個瀏覽器卡死。對 ES 是特別耗內存的，我們怎麼樣去解決這個問題呢？我們要把這個問題解決了，所以說每一個作業裡面我都在給大家講東西。

<sub>[03:34-04:11]</sub>
我們進一下這個 Docker Hub，然後去點過來。我們把這個地方寫到這，Elasticsearch 回車一下在這邊去搜。這邊有很多鏡像，我們就用第一個，剛才應該是 7.6.2 最新的。這邊下載完了，下載完了之後並且它也啟動了對不對。那我們來看一下 `docker ps`，你看我現在敲命令都卡了，看到了嗎？這個東西是非常非常卡的。

<sub>[04:07-04:48]</sub>
來寫到這裡，**啟動之後發現整個 Linux 虛擬機都不好了**。我們知道 ES 非常耗內存，一啟動就是幾個 G 對不對。啟動了，虛擬機就咱們的 Linux 服務器就卡了，就很卡。我們這就 1 核 2G，我們怎麼可能承受 Elasticsearch 這麼大的一個東西，就卡住了。那現在的話我們要去解決它。

<sub>[04:46-05:57]</sub>
來那卡了怎麼辦呢？我還是想測試一下，我們來用一個命令，再學一個新的命令。`docker stats`，就是你可以通過它來看它的一個 CPU 的狀態，**查看咱們的 CPU 狀態**。那我們得想辦法把它啟動起來對不對，我這個服務器卡住了。退出來了 clear 清理一下，`docker ps` 我們來停掉一些。剛才我們還啟動了一個 Tomcat，我們把那個 Tomcat 先停一下就可以。因為我們是 1 核 2G 的，啟動了一個 Elasticsearch 那還得了。你看這個 `docker ps` 都不給我出來了，要等它把這個加載出來了我們才好去把它結束掉。

<sub>[05:49-06:27]</sub>
那在這個地方就發現了這個問題，那這種東西不動的一個原因就是咱們的 ES 十分耗內存的。比如說這個地方一啟動就是 1 點幾個 G 對不對，那現在的話我的服務器只有咱們的 1 核 2G，沒有錢嘛。也就是大家所買的學生機跟大家環境是一樣。那現在的話它是啟動不起來了，它啟動起來這邊卡住了。那這個時候我們就只能把它停止了，`docker stop`，來停止咱們的整個 Docker，或者重啟服務器都有可能。

<sub>[06:27-07:14]</sub>
因為我們這個 Docker 現在已經掛了動不了了，內存太大了。終於出來了，你可以看到這邊有咱們的名字，那現在我們先 `docker stop` 把 Tomcat 停一下，可能給我們節省出來一點內存吧。先把這個 Tomcat 停掉，停掉之後的話這個 Elasticsearch 可能會好那麼一點點，一定要把它停了，停了之後可能會好一點。反正解決方案的話你們可以停整個 Docker，一般的話不建議。那我們來這一趟測試，查看咱們的一個 CPU 的內存狀態，我們通過 `docker stats` 進行查看。那這個停止的話你看它又是這麼長的時間，這個就是因為我們 CPU 的資源有限，**所以說平時一定一定要謹慎謹慎再謹慎**。

<sub>[08:15-09:11]</sub>
終於把它停了，停了之後可能就好一點了。我們先試一下，訪問一下看能不能訪問成功。訪問一下咱們的 `localhost:9200`，走你。你看到嗎，是不是安裝成功了？You know, for search，把它取過來。停了一個 Tomcat 順便它就好一點了，這個地方 You know, for search 發現出來，**說明咱們的 ES 安裝好了**。我們再來通過這個 stats 來看它占了多大的內存。

<sub>[08:39-09:15]</sub>
我們把這個地方寫到這，clear 清理一下。回車，我們就可以看。查看內存，它其實也是一個非常耗資源的命令。等它，等我們看到的時候你就能理解了。看到這邊就有了，你可以看到，我的天哪，剛才為什麼卡？我們裝了一個 Elasticsearch，**我們的服務器總共才 2 個 G，它就佔了 1.23 個 G，佔了 71.8% 的內存**，所以說它卡了。這個地方就非常非常大擋。

<sub>[09:16-10:03]</sub>
那現在的話我們發現這個內存是非常耗內存的，非常耗內存的話我們就要盡量把它關閉，然後去加一個內存的限制。那我們來把它退出一下，`docker ps`，我們來把我們的 ES 先給大家停止，然後我們來看一下我們這邊的官方頁面打開沒有。那這邊的話有我們的一個頁面。

<sub>[09:48-10:36]</sub>
`docker stop` 趕緊把它停了，這個太耗內存了。停掉，停止了之後我們這個虛擬機就非常快了，我們這個服務器就不至於這麼慢了。然後停止完之後我們要增加一些內存限制對不對。這個怎麼加呢？這個一般其實用過 ES 的人都知道可以去修改配置文件。那我們在這邊的話就是通過 `-e` 來加上咱們的一個配置修改，這個 `-e` 就是 environment 就是一個環境的配置修改。我們可以通過 `-e` 參數來進行一個環境的修改。

<sub>[10:21-11:04]</sub>
我們來看一下，我現在做了三個作業：第一個給大家講暴露端口的重要性；第二個就是我們給大家講解官方測試以及如何進入容器查看的重要性；第三個就是給大家講咱們如何去看這個容器的當前存活狀態、如何進行修改。這三個作業認真做完之後，把上面的命令補充出來，這才是構成了我們完整的 Docker 使用命令。

<sub>[10:43-11:36]</sub>
現在我們這邊停止了，停止之後我現在再啟動。現在啟動的話我就不這麼傻乎乎的啟動了，這個是不是乾乾淨淨的什麼都沒加。我們要去加上我們關於 ES 的配置，`-e`。我們這邊的 `-e` 的配置，我們要去配置什麼東西？還是一樣的要去加環境的配置對不對。我們的 ES 配置有一個 `ES_JAVA_OPTS`，來 `-Xms64m -Xmx512m`。這個給大家講 JVM 的時候都給大家講過了，這是咱們的 64 兆，那這樣的話它最多只讓我 64 兆的內存。最大的話我給它一個 512 兆，就不能再多了，再多的話我這個服務器扛不住。

<sub>[11:30-12:02]</sub>
這樣的話就可以使用了，我們再來啟動一下，就直接加了一個 `-e` 給它限制了一些基本的運行環境的參數。再來啟動。你看這次啟動肯定就沒有剛才那麼慢了，首先它說 container already，它說它已經啟動了。我們剛才的話這個名字已經用過了，我們要換一個名字。比如說 elasticsearch02 吧，我給它改個 02 換個名字。我們再來啟動，走你。

<sub>[12:03-12:33]</sub>
來啟動成功了，`docker ps` 看一下它的一個容器 ID。我們再來查看一下 `docker stats`，來。我們來看一下，進去之後你又發現，看到沒有？這個時候就是我們希望的，這個內存就非常小了。看看咱們的一個效果，就是這樣的話就可以發現它已經變小了。然後再次查看咱們的 `docker stats` 就可以搞定了。那現在的話關於我們的環境配置我們也徹底搞定了。

<sub>[12:37-13:13]</sub>
那現在的話我們再來訪問一下能不能成功。來退出一下，比如 `curl localhost:9200`，來回車。You know, for search，一樣可以測試成功。所以說到目前為止的話，我們這樣才算構建了一個完整的知識體系。

<sub>[13:13-13:48]</sub>
然後我們再來聊一個問題。我現在給大家留一個作業就是一個小作業，我們現在已經把這個 ES 安裝好了，ES 安裝好了但 Kibana 還沒。Kibana 它是要去連接 ES 的，這個地方就存在一個網路的問題。就是我們要留一個作業，使用咱們的 Kibana 連接咱們的 Elasticsearch。我來看一下，來給大家畫幅圖讓大家思考一下。

<sub>[13:38-14:12]</sub>
這個東西非常的經典，理解了這個模型你才能理解後面我們要學到一些網路相關的知識。Docker 裡面的東西非常深，你要學深的話，我們說了我們不是為了入門，我們是為了精通。來把它往下走，那現在的話是這麼兩個東西的，比如說我現在啟動了一個服務對不對。那現在假設我啟動了一個 Kibana，那 Kibana 的啟動方式跟它一樣，Docker run 啟動起來就可以了對吧。然後這邊的話咱們再一個 ES。

<sub>[14:02-14:55]</sub>
那現在我有兩個服務，比如說這個地方我啟動了咱們的一個 9200，或者這邊的話就是 Kibana 的一個端口地址。那現在的話**這兩個容器是互相隔離的**，對不對？它有它的一個內部地址，它有它的一個內部地址。那它怎麼去訪問呢？有的人就想我能不能直接 localhost 連過去？來思考一下，你能直接 localhost 連過去嗎？比如說我 Kibana 想去連這個 ES，你在這個地方來一個 `localhost:9200`，你想我直接連過去。告訴你們**這個是不現實的，我們說了容器內部互相隔離**，這個想法不現實。

<sub>[14:38-15:27]</sub>
那怎麼辦？我們可以通過這種方式，它們兩個都是搭建在我們的 Linux 服務器上的對不對。這是我們有個 Linux，我們一般有個內網 IP，那這個時候它可以轉發。我們可以通過內部地址跟內網都能訪問它，這個時候我們可以把這個請求，首先 Kibana 走到內網地址，通過內網地址請求到 ES，是這麼一個連接方式。這樣的一個概念，在這個地方的話還要用到很多技術，比如說這地方有個 veth-pair 技術，就是 Linux 它是如何配對的，那我們到時候也會給大家講。

<sub>[15:09-15:56]</sub>
這個地方它並不是直連的，它有個數據過程。而這一塊就是咱們要思考的一個東西叫 **Docker 網路原理**。那這個東西也是要精通 Docker 的一個必經之路。這個地方需要大家有一些網路的基本知識，那大家沒有聽我的課也能聽得懂。來把這個圖給大家提過來，這就是給大家留了一個思考的問題，思考咱們的一個網路如何才能連接過去。

<sub>[15:40-16:03]</sub>
就是我們學完肯定要精通，如果只是個入門你還學什麼了對不對。OK 給大家提一下，這是我們的三個作業練習。那經過這三個作業練習，是不是大家就覺得前面學的這些命令瞬間就掌握了一些了？

---

### P17：Portainer 可視化面板安裝

<sub>[00:00-00:13]</sub>
我們來繼續，我們學了這麼多命令之後，學了容器跟鏡像，要是有個可視化面板跟我們管理就好了。就好比 Swagger 它可以去管理我們所有的請求對不對，那這個時候就要給大家說一些基本的管理鏡像的工具。

<sub>[00:12-00:38]</sub>
那這個地方有個叫 **Portainer**，這個什麼東西呢？就是我們現在先用這個，它不是我們最佳的選擇，先用這個。那我們之後還要學一個咱們的 Rancher，這個的話我們也會去用。這個的話什麼時候用呢？我們要做持續集成跟持續部署的時候再做，CI/CD 的時候再用。

<sub>[00:33-01:10]</sub>
現在的話我們就用一個面板來操控一下，理解一下它的一個概念就好了。那首先我們來理解一下，什麼是咱們的 Portainer？它是一個 **Docker 的圖形化界面管理工具**。就是如果你覺得這些命令非常麻煩，你想通過圖形化——不建議大家使用啊還是多調命令——大家可以提供一個後台面板供我們操作，我們就可以使用它來進行操作，非常非常簡單。

<sub>[01:06-01:47]</sub>
那我們來可以把它安裝一下，安裝的命令就是這個，這個命令來解釋一下都能看得懂。`docker run` 啟動，我們通過內網是 9000 端口，外部訪問的是 8088。然後這個地方有個 `--restart always`，這個的話你就把它想成我們的一個方式就好了，先不管它。然後 `-v` 是我們的掛載，就是它把這裡面的一些數據掛載到我們的本機。那這個地方你也可以先不用管它，後面的話是給它授權讓我們可以訪問，最後的話就是安裝咱們的一個控制面板。

<sub>[01:33-02:00]</sub>
來說白了把它縮短的話就是這麼個命令，這個命令大家都能看懂，把它啟動下載運行。中間這一堆是關於它的一些權限和本地卷掛載的。來我們來粘貼走一下，那現在它就去下載這個。這個下載起來非常非常的快，那我們就可以看到下載完它也啟動成功了。我們就可以訪問測試一下。

<sub>[01:56-02:42]</sub>
訪問測試，我現在的話是連接了外網，我們應該訪問的是我的外網的 8088 端口對不對。那當然的話你們自己的服務器是沒有開通外網的，一定阿里雲那邊開通，怎麼開通呢我們在講 Linux 就講過了。我們來訪問一下我們的 8088，來外網地址的 8088 走你。來現在的話這上面的路由已經變成了，說明它已經找到了。

<sub>[02:26-03:35]</sub>
拿過來在這邊的話等它加載，這第一次加載的話它要去加載裡面的所有數據，還要去連接 Docker 是比較慢的，所以說可能會比較慢。比如說這是我的一個地址，那我把這個容器停了也就訪問不了了。所以說你們一般的話如果你們用我這個情況就是把地址換成 IP 就可以，但是前提的話一定要把阿里雲的安全組打開。它正在加載 Portainer，第一次加載的話時間比較久因為它把所有的數據給它掛載出來。

<sub>[03:37-04:20]</sub>
那讓它加載吧，加載完之後我們就可以通過它訪問了。OK 訪問的話也比較簡單，然後我們把它訪問了，然後給大家截幾個圖就可以了大家就會了。我們來看一下 `docker ps`，這個的話也是在運行的沒有什麼問題。我們可以在本機來訪問一下，但是本機的話是內部測試。`localhost:8088`，你看這邊就是能訪問出來數據，這個就沒問題，內網很快。

<sub>[04:22-05:07]</sub>
外網慢死了。這個比較慢，它正在加載 Portainer。第一次加載的話時間比較久。加載完之後是什麼樣子我給大家看一下。在這個 Portainer 打開的話，下面會得到這麼一個登錄界面。就是我現在要加載出來的就是這個登錄界面。進來之後就會加載出這麼一個登錄界面，你要給它設置一個密碼就可以登錄了。

<sub>[04:58-05:35]</sub>
登錄完之後呢，再往下走，我們可以在這邊去設置數據。一般的話我們就選擇本地的就可以了，因為我們不去查看遠程的。我們對於這個本地的面板來說就是本地的。選擇本地的，你看本地這邊就一下就過來了，這邊有個 local，還有一個登錄的一個面板。

<sub>[05:25-06:00]</sub>
等了半天終於還是過來了，這個就跟我們剛才這邊一樣對不對。現在的話我們就加載完了，然後我們現在登錄，要設置一個密碼我們來設置一下。比如說隨便設個密碼，然後我們來創建這樣一個用戶。它說我們密碼太簡單了，那就給它改複雜一點，我們來創建這個用戶。

<sub>[06:10-06:58]</sub>
然後進入之後，我們就可以進到裡面面板。在這個面板裡面我們就可以看到這邊的信息，比如說這邊的話當前這個上有兩個 container，正在運行的有幾個，停止的有幾個。這邊的話 Image 運行的話也是一樣的，當前倉庫就是咱們的 Docker 裡面有兩個鏡像。然後這個網路怎麼配置的，還有咱們的一些數據卷掛載的，都可以在這邊配置。

<sub>[07:07-07:30]</sub>
但是的話這個並不是一個重點。來我們把這個關了，大家的話下去這個地方可以自行測試一下。**可視化面板我們平時不會用**，大家自己測試玩一下就可以了，測試玩完即可，也比較簡單。

