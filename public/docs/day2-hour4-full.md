# Day 2 第四小時：Docker 基本指令（上）

---

## 一、前情提要（2 分鐘）

Docker 裝好了，現在開始學指令。

這堂課學「取得和執行」：
- docker pull：拉取映像檔
- docker images：列出映像檔
- docker run：執行容器
- docker ps：查看容器

下一堂課學「管理和清理」。

---

## 二、docker pull - 拉取映像檔（10 分鐘）

### 2.1 基本用法

```bash
docker pull nginx
```

從 Docker Hub 下載 nginx 映像檔。

### 2.2 指定版本

```bash
docker pull nginx:1.25
docker pull nginx:1.25.3
docker pull nginx:alpine
docker pull nginx:1.25-alpine
```

**常見 Tag 含義**

| Tag | 含義 |
|-----|-----|
| latest | 預設標籤（不一定是最新） |
| 1.25 | 主版本號 |
| 1.25.3 | 精確版本號 |
| alpine | 基於 Alpine Linux（超小） |
| slim | 精簡版 |
| bullseye/bookworm | Debian 版本代號 |

### 2.3 從其他 Registry 拉取

```bash
# Google Container Registry
docker pull gcr.io/google-containers/nginx

# 私有 Registry
docker pull 192.168.1.100:5000/myapp:v1
```

### 2.4 拉取過程解析

```bash
$ docker pull nginx:1.25

1.25: Pulling from library/nginx
a2abf6c4d29d: Pull complete      # Layer 1
a9edb18cadd1: Pull complete      # Layer 2
589b7251471a: Pull complete      # Layer 3
186b1aaa4aa6: Pull complete      # Layer 4
Digest: sha256:abc123...          # 映像檔的唯一識別碼
Status: Downloaded newer image for nginx:1.25
docker.io/library/nginx:1.25
```

每一行是一個 Layer。如果 Layer 已經存在本機，會顯示 `Already exists`。

### 2.5 查看可用版本

Docker Hub 網站搜尋映像檔名稱，可以看到所有可用的 Tag。

或用命令查詢（需安裝額外工具）：

```bash
# 使用 skopeo
skopeo list-tags docker://nginx
```

---

## 三、docker images - 列出映像檔（8 分鐘）

### 3.1 基本用法

```bash
docker images
```

輸出：

```
REPOSITORY   TAG       IMAGE ID       CREATED       SIZE
nginx        1.25      a6bd71f48f68   2 weeks ago   187MB
ubuntu       22.04     ca2b0f26964c   3 weeks ago   77.9MB
python       3.11      22140cbb3b0c   1 month ago   1.01GB
```

**欄位說明**

| 欄位 | 說明 |
|-----|-----|
| REPOSITORY | 映像檔名稱 |
| TAG | 版本標籤 |
| IMAGE ID | 唯一識別碼（前 12 碼） |
| CREATED | 建立時間（映像檔本身，不是下載時間） |
| SIZE | 大小 |

### 3.2 篩選映像檔

```bash
# 只看特定映像檔
docker images nginx

# 只看特定標籤
docker images nginx:1.25
```

### 3.3 格式化輸出

```bash
# 只顯示 IMAGE ID
docker images -q

# 自訂格式
docker images --format "{{.Repository}}:{{.Tag}} - {{.Size}}"

# 輸出成 JSON
docker images --format json
```

`-q` 很常用，配合其他命令批次操作。

### 3.4 查看所有映像檔（包含中間層）

```bash
docker images -a
```

會顯示建構過程中產生的中間映像檔，通常不需要。

### 3.5 Dangling Images

沒有 Tag 的映像檔，顯示為 `<none>`：

```
REPOSITORY   TAG       IMAGE ID       CREATED       SIZE
<none>       <none>    d1234567890a   1 hour ago    150MB
```

這些通常是舊版映像檔被新版覆蓋後留下的。

查看 dangling images：

```bash
docker images -f dangling=true
```

清理：

```bash
docker image prune
```

---

## 四、docker run - 執行容器（25 分鐘）

這是最重要的指令。

### 4.1 基本用法

```bash
docker run nginx
```

用 nginx 映像檔建立並啟動一個容器。

如果本機沒有這個映像檔，會自動 pull。

### 4.2 前景執行 vs 背景執行

**前景執行（預設）**

```bash
docker run nginx
```

容器的輸出直接顯示在終端機。按 Ctrl+C 會停止容器。

終端機被佔住，不能做其他事。

**背景執行（-d）**

```bash
docker run -d nginx
```

容器在背景執行，終端機立刻回來。

輸出容器 ID。

### 4.3 互動模式（-it）

```bash
docker run -it ubuntu bash
```

- `-i`：保持 STDIN 開啟（可以輸入）
- `-t`：分配偽終端（有正常的終端介面）

通常 `-it` 一起用，進入容器的 shell。

```bash
root@abc123:/# ls
root@abc123:/# exit
```

### 4.4 指定容器名稱（--name）

```bash
docker run -d --name my-nginx nginx
```

不指定的話，Docker 會自動產生隨機名稱（like `admiring_newton`）。

名稱必須唯一，重複會報錯。

### 4.5 自動刪除（--rm）

```bash
docker run --rm nginx
```

容器停止後自動刪除。

適合一次性任務、測試用。

### 4.6 環境變數（-e）

```bash
docker run -e MYSQL_ROOT_PASSWORD=secret mysql
```

設定容器內的環境變數。

可以多次使用：

```bash
docker run -e VAR1=value1 -e VAR2=value2 myapp
```

或用檔案：

```bash
docker run --env-file ./env.list myapp
```

### 4.7 Port Mapping（-p）

```bash
docker run -d -p 8080:80 nginx
```

把主機的 8080 port 對應到容器的 80 port。

格式：`主機Port:容器Port`

現在訪問 http://localhost:8080 就能看到 Nginx。

多個 port：

```bash
docker run -d -p 8080:80 -p 8443:443 nginx
```

### 4.8 Volume 掛載（-v）

```bash
docker run -d -v /host/path:/container/path nginx
```

把主機的目錄掛載到容器內。

詳細留到 Day 3 講。

### 4.9 組合範例

```bash
docker run -d \
  --name web \
  -p 8080:80 \
  -e NGINX_HOST=example.com \
  -v /data/html:/usr/share/nginx/html \
  --restart unless-stopped \
  nginx:1.25
```

這個命令：
- 背景執行 nginx:1.25
- 容器名稱叫 web
- 主機 8080 對應容器 80
- 設定環境變數
- 掛載目錄
- 除非手動停止，否則自動重啟

### 4.10 docker run 完整流程

當你執行 `docker run nginx`：

1. Docker Client 送請求給 Daemon
2. Daemon 檢查本機有無 nginx Image
3. 沒有的話，從 Registry 下載
4. 用 Image 建立 Container
5. 分配網路、準備檔案系統
6. 啟動 Container 內的程序
7. 連接終端（如果是前景執行）

---

## 五、docker ps - 查看容器（10 分鐘）

### 5.1 查看執行中的容器

```bash
docker ps
```

輸出：

```
CONTAINER ID   IMAGE   COMMAND                  CREATED         STATUS         PORTS                  NAMES
abc123def456   nginx   "/docker-entrypoint.…"   5 minutes ago   Up 5 minutes   0.0.0.0:8080->80/tcp   my-nginx
```

**欄位說明**

| 欄位 | 說明 |
|-----|-----|
| CONTAINER ID | 容器 ID（前 12 碼） |
| IMAGE | 使用的映像檔 |
| COMMAND | 啟動命令 |
| CREATED | 建立時間 |
| STATUS | 狀態（Up、Exited 等） |
| PORTS | Port 對應關係 |
| NAMES | 容器名稱 |

### 5.2 查看所有容器（包含已停止）

```bash
docker ps -a
```

已停止的容器 STATUS 會顯示 `Exited (0) 2 hours ago`。

### 5.3 只顯示容器 ID

```bash
docker ps -q      # 執行中
docker ps -aq     # 全部
```

用於批次操作：

```bash
# 停止所有容器
docker stop $(docker ps -q)

# 刪除所有已停止的容器
docker rm $(docker ps -aq -f status=exited)
```

### 5.4 篩選容器

```bash
# 根據名稱篩選
docker ps -f name=my-nginx

# 根據狀態篩選
docker ps -f status=running
docker ps -f status=exited

# 根據映像檔篩選
docker ps -f ancestor=nginx
```

### 5.5 格式化輸出

```bash
docker ps --format "table {{.Names}}\t{{.Status}}\t{{.Ports}}"
```

常用模板變數：
- `.ID`：容器 ID
- `.Image`：映像檔
- `.Names`：名稱
- `.Status`：狀態
- `.Ports`：Port 對應
- `.CreatedAt`：建立時間

### 5.6 查看最後建立的容器

```bash
docker ps -l    # 最後一個
docker ps -n 5  # 最後五個
```

---

## 六、實作練習（3 分鐘）

現在動手試試：

1. 拉取 nginx:alpine 映像檔
2. 用這個映像檔啟動一個容器，名稱叫 test-nginx，背景執行，port 8080
3. 查看容器是否在執行
4. 用瀏覽器訪問 http://localhost:8080

```bash
# 參考答案
docker pull nginx:alpine
docker run -d --name test-nginx -p 8080:80 nginx:alpine
docker ps
# 開瀏覽器訪問 http://localhost:8080
```

---

## 七、本堂課小結（2 分鐘）

這堂課學了四個基本指令：

| 指令 | 功能 |
|-----|-----|
| docker pull | 拉取映像檔 |
| docker images | 列出本機映像檔 |
| docker run | 建立並執行容器 |
| docker ps | 查看容器 |

**docker run 重要參數**

| 參數 | 功能 |
|-----|-----|
| -d | 背景執行 |
| -it | 互動模式 |
| --name | 指定名稱 |
| --rm | 自動刪除 |
| -e | 環境變數 |
| -p | Port Mapping |
| -v | Volume 掛載 |

下一堂課學停止、刪除、日誌、進入容器。

---

## 板書 / PPT 建議

1. docker pull 流程圖（包含 Layer 下載）
2. docker run 參數表
3. docker run 執行流程圖
4. docker ps 輸出欄位說明


---

## 📺 補充教材：狂神說 Docker

> 以下內容來自【狂神說Java】Docker 教程系列，作為本節的補充參考資料。

### P09：鏡像的基本命令

我们来继续讲了解完了Docker的一些基本安装和基本的一些概念之后大家肯定现在是模能两可的我们来把这些所有的命令一学大家干嘛头脑瞬间就清醒起来了

然后我们再做一些作业大家干嘛就懂了那我们来看一下首先把这个帮助命令说一下这是一个万难的就是我们以后不会东西干嘛

就去这边查就可以了来我们写到这里还是一样的比如说第1个Docker version这个我们刚才已经看了对吧那除了这个之外Docker infer可以看Docker的信息

还有一个就是Docker命令刚刚Helps这是我们的一个万难命令我们之后的话都会通过这个命令啊这是一个万难命令万难命令

可以都通过它来看我们来看一下首先Docker version这个已经给大家讲解过了就是答应咱们当前Docker的一些基本的信息对不对那就是显示Docker的版本信息写到这

显示Docker的版本信息我的强迫症让我对齐来然后再往下Docker infer什么意思呢我们来看一下这个infer也是信息的意思对不对

那这个就会显示Docker的一些更加详细的信息啊点开那在这里面就可以看到Docker infer当前的客户端它的一些模式服务器它这里面运行了什么你看看当前运行的是零咱们停止的对吧这是跟容器相关的

然后当前这里面有多少个金像然后它的一个服务器的版本地址然后它的一些插件信息所以这个信息干嘛会更加详细的一点来写到这个地方显示

Docker的什么系统信息也就是System级别的包括咱们的金像和容器那我们刚才可以看到金像和容器的数量ok那再往下往Docker help

这就是一个帮助命令帮助命令就是以后我们不知道怎么用了比如说我想看Docker有哪些命令那这个时候我就可以干嘛说Docker刚刚help

那在这里面的话就可以看到Docker的所有命令是非常非常多的你可以看到从上到下的话命令非常非常多我们后面的话这些命令干嘛几乎都要学到然后这是第一个点

第二个点我们需要知道帮助文档的地址就是你们要学会以后我们的命令不会就查Docker刚刚help然后再不会的话可以去官网看这个官网的话在这边的话也是有对不对

那我们来看一下这个金像是哪里的你肯定是来晚的没有听我刚才讲的那在这边的话往下翻在这个地址的话

我们去找一下它的一个Docker文档还是从Docker官网翻到下面的一个Dalks到了Dalks这个文档之后这侧面是不是有一个咱们的get a star还有一些我们的帮助

然后上面的话有一个咱们的reference我们可以去找一下来 reference在这里 reference那在reference里面的话就可以找到我们的所有命令了

在这侧边第一个command line命令行那些你可以看到这边就非常多命令看到了吗看到同学可以我们之后的话就可以在这边查询

所以说要找到官方文档的位置在Dalks下面的reference命令行这边就有嘛所有跟Docker客户端相关的命令这个太多了你们可以看一下

每一个命令都可以干嘛都可以在这查到然后除了这个之外呢它的一些帮助文档也可以在这边看那我们现在的话就打开这个client

说一个命令就可以来这边看一下教大家会了之后大家之后干嘛就可以来这边进行查询就可以使用了OK

那把这个diss的文档也发给大家放到这里OK那现在的话我们就已经做好了准备对吧就可以学一下咱们的

第一个关于Docker人最重要的就是进向的命令进向的话是一个然后第二个我们要学他的容器命令这两个是基础的

我们要把这两个高度很多人会Docker觉得自己会了就会了这两个这两个连入门都不算我们说了我们整个课程的话

学到我们的idl基成其实只占这个课程的大概四分之一一点五这个样子吧四分之一点五这个样子来我们来把这个地方

来说一下进向命令进向命令的话我还是这样子给大家进行分组的说首先一个一个来比如说DockerImages

我们来看一下刚才我们用过这个命令是不是查看我们的所有进向对吧查看所有本地主机上的进向我们给大家下了个Hello World

那这个时候你们就可以去官方文档上来看跟着我在思路学习方法这个很重要在这边的话下面是不是有个DockerImages

看到同学可以那你点击DockerImages就可以了找到对应的命令那等他夹载过来那当然的话这个地方是比较慢的

你要等待他把这边夹载出来这个网站是国外的你们也可以在这边直接换他这个应该就是直接显示的这国外东西就是慢没办法

到了这个DockerImages然后这个地方告诉了他的使用方式然后他的一个介绍对不对他这个地方就可以干嘛

ViuShowOutopLevelImages就可以看到所有的一些进向地址包括仓库和他的一些Target标签对不对你可以通过一些参数

比如说通过GunA可以看到所有的进向比如说GunFelt可以通过来过滤对不对那除了这个之外

你也可以去做一些他的输出配置还有一些直显示ID看嘛这个我们经常用我们都来一个一个测测

来知道官方文档的地址就好了那第二个我不会用DockerImages刚刚Helps来

在这个地方的话也可以告诉我们当前常用命令是不是A、F、Q看到同学扣音

这就是学习的方式那现在知道这两个查询命令的方式我们就可以玩一项首先第一个DockerImages是不是可以查询出来

当前这个HelperWord的一个帮助信息对不对那现在的话我们来就把这个命令给大家详细的解释在这里

首先一个一个来说解释那这个地方第一个叫Repository什么意思

它的一个仓库圆就是这个进向来进向的仓库圆就是它的名字

对不对来仓库还是写仓库会比较好一点圆就是你们去下载的时候

就通过这个名字来下载运行的时候就通过它来运行Tog就是一个版本标签信息对不对进向的标签

标签那除了这个之外还有就是ImageID那这个就是咱们进向的ID对吧

那除了这个之外还有个Create就是咱们的一个进向的创建时间创建时间然后再往下走

就是咱们的一个Size这个进向的大小你看这HelperWord才13K是不是非常小来

咱们的一个进向的大小进向的大小X Shell怎么复制粘贴直接右键复制就可以了大小

那再往下走的话就是咱们再来往下说然后在这里面有一些可选项可选项的话大概是这么三个

第一个Gang A,Gang F,Gang Q是吧我们把这几个说一下在这个地方复制过来就可以了

来可选项然后Gang A的话就是显示全部的嘛来

列出所有的进向然后再往下走这个的话它是格式化我们不管

这个Filt是一个过滤我们也可以不管FoMat这几个干嘛想要就可以要不想要的话

可以去到我们没有这么经常用的话就不用它然后再往下走的话就是直显示ID

这个我们会经常用来直显示进向的ID这个地方的话我们以后删除的话

就可以把所有的进向干嘛列出来删掉等会给大家讲一些骚操作大家就可以玩起来现在的话

我可以来试一下这两个命令比如说Gang A那就是显示所有

还是Gang Images来ImagesGang A这样的话

可以看到Docker的一些详细信息跟我们看它差不多对不对你们也可以去试一下这里面的命令

我不是然后Gang Q的话只是显示这个ID这个命令我们之后会经常用Gang AQ把它联合起来

AQ的话其实就是显示所有的进向OK那这是咱们的一个Docker Images命令

这个比较简单那除了这个之外我们经常Docker要去搜索好比我们现在要去Gateup上搜索一个东西怎么样搜索对不对

那Docker的搜索是怎么来的呢那在这个地方我们都知道刚才给大家看了Docker它是有一个Docker Hub的

对不对我们来把Docker Hub打开来Docker Hub里面就是一个商店我们可以在这里面搜索很多东西

比如说我要搜索一个Tomcat或者我要搜一个MySQL这个地方的话你们就可以看到MySQL的服务是不是就在这个地方有啊

你可以看一下MySQL那你点开之后这边MySQL就这个进向这是网页版的搜索我们正常搜索的话

我们也可以直接在这边用Docker Search命令搜索进向OK那这个进向的话我们先聊一下

默认的话你们在这个官网上是不是就可以看到这个相关的一些MySQL进向的一些说明首先

它这边有点描述然后它当前支持的一些版本比如说我们一般喜欢使用5.7的或者这边8.0的也是支持的然后再往下走

它会介绍什么是MySQL然后再往下走How to use the image它这个地方就告诉你如何使用了

这边是不是就告诉你它要去下载这个MySQL这个的话我们当时讲到容器的时候再给大家去说

那这是咱们的一些基本信息现在的话我们就通过它来搜索来搜索命令的话

也比较简单比如说我们现在来搜索一下来清空一下来

刀柯 色尺我们要搜索一下咱们的MySQL那这个地方的搜索跟我们刚才在网页上搜索是一样的我们在网页上去搜索的时候

是不是就搜MySQL它就出来一个MySQL的列表啊看到了吗然后跟Glithub一样这边有很多信心

比如说Stars 9.5K对吧这就非常多那我们现在的话这边搜索你看

也搜索出来了来刀柯 色尺看到同学扣1比如说第一个MySQL

这边搜出来9494那这个MySQL就是对应我们这边官方文档的MySQL地址

对不对那我们把它干嘛给大家复制下来我就复制这么一点吧

来取过来这个搜索经向的话还是比较简单的我认为

来在下面的地方写上一个可选项来我们来这个地方我们不知道刀柯 色尺怎么用之后

还是一样命令可以通过两种方式讲思想啊第一种就是我们可以在这边去找这个刀柯 色尺的命令你往下翻的话

可以找到刀柯 色尺你可以在这边去搜刀柯 色尺在这刀柯 色尺它这里面就会告诉你可选项

这是第一种方式第二种的话你觉得官方文档慢我说了不需要官方文档

SERC是刚刚害普这里面就显示了比如说你可以去过滤一些跟死大相关的对不对

你可以让它去判断那在这官网的话其实干嘛也是一样的你要去搜索

到官网卡死了我们以后就用这个害普去看也是一样的那现在的话我想搜索就是

比如说在9000以上的这个收藏对不对可以来通过收藏跟那个Gate HUB一样通过收藏来过滤

对不对过滤那这个时候我们就可以通过一个命令在这边也显示着

比如说有一个刚刚Felter那我们把它取过来刚刚Felter那比如说Zert

那后面就是你要过滤的一个条件比如说我们这个地方有个Stars那也就是对应的这个Stars我们这边不是有个收藏Stars

那比如说来比如说等于3000那他就会去找这个只要收藏数不小于3000就是会去

比如说写个3000那么就是搜索出来的进一项就是咱们的Stars大于3000OK

那这个命令我们干嘛有二也会去用的当然不是一个必要的命令可以把它加上我们来测试一下

还是一样的我们还是来搜买搜口为例刚才我们Dock Search买搜口之后在后面追加参数

对不对那回车然后看到了现在是不是就只收出来3000以上的如果你要搜索咱们更高的

比如说5000以上的那这个时候就只剩下我们想需要的这个买搜口了对不对来理解这个命令的同学

扣1比较简单我把这两个给大家解到笔记里面来

那这是咱们的两个然后Dock Search完了之后我们还有了Dock POO

我们要把这个我们现在能搜索了就要下载对不对所以说这里不下载

进行那把它下的下来怎么下载还是一样的来

我们现在已经搜到了这个进行叫买搜口对不对我们就可以去下载我们可以通过Docker POO

来首先看一下Docker POO命令的还有POO还是一样的看一下帮助信息

刚刚还有POO来这里面的话命令比较少你看下载的时候可以通过A跟Q

对不对那我们不用它我们直接来搜Docker POO就跟拉去一样

我们要拉去买搜口这个时候它就会去下载买搜口你看它现在就在使用这个地方有个使用默认的Tag

就是Lust什么意思就是默认使用最新的版本我们也可以指定版本的下载我们都来下载一下

然后这个下载的话它是需要一些时间的因为它要去搜索我们把它写到这下载经向

下载经向Docker POO命令还是比较简单的下载新项

那这是一种方式还有一种就是我们可以通过这个地方是咱们的一个进向名字

那后面还可以跟上一个咱们的Tag这个是一个可选的但是很多情况下我们是会加上这个版本的因为不然的话

它都用最新版了来这后面可以加版本然后这边可以发现它默认使用最新版

然后现在我们就下载完了下载完了之后我们把这个干嘛全部给它拿过来这里面的门道大得去了很多人觉得这个命令

没什么研究的告诉你这里面藏到Docker的金水来看一下这个地方

第一个如果如果不写Tag如果不写咱们的一个Tag

就是不写咱们这个版本那默认就是咱们的Latest最后一个然后再往下走的话

它要去下载对不对那这一段它是怎么下载来它是不是一层一层下载了

这个叫分层下载也叫Layer分层下载这是Docker进向的核心来

DockerImage的核心就是这个分层下载是怎么做的呢我们还要后面要说到一个联合文件系统文件系统

这个的话我们后面给大家去解释一下它底层原理的时候再说然后在这边的话有它的一个签名信息

就是防卫标志签名只要签名是这个的话就不吃防卫的对吧

那下面这个DockerIO就是它的一个真实地址真实地址真实地址你看

短短一个DockerPool里面就包含了这么多信息对不对那这个地方的话DockerPoolMySocope

那就等价于DockerPool来相当于我们写的这个它就是真实地址是这样的

这两个命令是等价的能理解我意思理解了同学科一样来等价于它

等价于它OK就是这么一回事比较简单那如果我们要指定其他版本

我们要来测试一下指定来版本下载指定版本下载

那我们来下载一下往这边走比如说DockerPoolMySocope5.7对不对

那这个版本一定要干嘛一定要在我们这个官方文档有就这个MySocope点进来这个瀏览器老卡处

我们点进这个MySocope这下面的话它会有一些支持的版本这个版本一定要来源于这个地方你不要瞎写

你瞎写这个版本肯定不存在的我的5.7在这地方是有的来我们来把它下载一下回车

那现在的话你可以看到看这个区别还有它下载的层级这个分层是一个非常高明的现象

这是Docker高级里面的但是我都会给大家讲到了一点一点来循序见尽先理解

先会使用命令再理解命令这是一个过程你看看嘛上面是不是下载这些镜像的时候

它说已经存在了看到这个同学扣1这个分层这样的好处它可以共用比如说上面的8跟这边的镜像

前面是一样的它可以不用下载了这一块就共用了可能甚至几十个几百个镜像它前面东西都是共用的

它不需要重复下载极大的节省内存这个地方只下载了下面四个是不是非常明显它只需要更新新的东西好了

以前跟其他东西冲突的层干嘛它就不要了这个是Linux里面的一个非常核心的联合文件系统

就说的下载完之后这边干嘛就使用的是咱们的5.7%这就叫指定版本下载通过Tag

写到这再往这边走的话我们就可以看一下Docker ImagesM-A-G-E-S

Docker Images里面这边的话就可以看到我们的版本信息了比如说刚才下的5.7%的这边Tag就是5.7%没有标版本的

就是最新版的这也是我们的8%然后这边显着它的一个大小你可以看一下这个大小还是比较大的

我们还可以自己做一个金像这个官方的金像是比较大的我就喜欢有时候用这个官方的不爽我就自己做一个

这自己做一个的话也是一些门道除了这个之外我们金像里面我们现在会下载了

然后又会搜索了最后的话我们再把它删除对运行的话就是咱们容器的事情来

Remove金像删除金像删除金像删除金像里面的话也可以玩一些骚操作

来我们来看一下比如说cleardark

imagesdarkremove这个rm就是Linux像删除对不对

那rmi这个i就代表Imagermi就是删除金像所以说i非常好理解这一下就记住了

对不对那gf就是全部删掉那删除的话可以通过金像的id来删比如说image id

也可以通过金像名称来删那我们这地方比如说通过id来删了吧一般通过id删了方便一点

复制删贴来回去了来

现在的话他就干嘛他说delete已经把它删完了这个地方删除的层数你看一下并没有把那个8的删除

对不对我们刚才下载了很多层他这份只删除了一点那我们再来比如说darkimages

那现在的话我们刚才5.7这个就被删掉了那除了这个之外呢我们想批量删除全部的可以怎么操作

来玩下Linux的命令比较简单rmi删除金像

gf删除所有的我们来符合下去嘛我们可以干嘛来可以直接刀了4号来

这个就是把这个作为参数删进去那这个地方的参数我们可以比如说darkimages这是我们刚才讲的

来更qa或者aq这是不是就是查出所有金像的ID啊然后他就会干嘛

一个一个的地规删除回车那两个都删掉了我们现在再来

darkimages来回车发现干嘛是不是就没有了

看到的同学可以所以说这几个命令啊也非常的简单我们把它干嘛

取过来删除命令来操作所多得很我会一点一点

给你们去讲那这是咱们的一个删除指定的容器指定的容器那这个地方的话

就是咱们的容器ID那除了这个之外我们也可以删除多个容器来掉通过空格来

删除多个多个容器来多个容器那一般情况下我们要测试

我们要删除全部容器那就可以使用这样的方式来把这个取过来这是删除全部的容器

全部的容器OK搞定这三个命令会了我们之后干嘛

去使用它的话就相对来说会比较轻松一点来那到目前为止的话

我们的进向命令到这个地方就够了然后我们等会再说那进向命令选完之后呢

我们要去研究容器命令那我们来回顾一下进向刚才说的几个第一个

都可以卖几次互用了同学扣1第二个会搜索的同学

扣1第三个我们会去下载它会下载下来之后我们就可以把它用了

用起来就是容器的事情所以说我们到容器说然后我们要会把它删除那所以说增三改成

数百就这么合适对不对那这是关于咱们进向的这几个命令干嘛

一定要下去吸收理解那讲完进向我们来讲一下

容器的命令停一下停一下

### P10：容器的基本命令

我们来继续啊刚才这个地方的话我们把这个镜像写成容器了是吧这个地方是我们的Image ID就是镜像ID

这是我的手物那给大家改过来了然后我们来看一下容器命令然后先在这个地方先说明一下我们干嘛有了镜像才可以创建容器

这是一个前提所以说我们要干嘛先下载镜像我们都学了Linux所以说这个地方我们来下载一个怎么的Santos OS镜像来测试学习

我们就虚拟一个干嘛我们在咱们的Docker里面再装一个容器就好比咱们的一个虚拟器这个就比较方便了那现在我们所有说要做一个准备工作

来去下载一下还是一样的Docker PuDocker Pu命令把它下载下载Santos OS我们就用最新版本的

它应该下载下载都是8版本的了来下载现在的话我们就是用Docker去跑了一个Santos OS系统对不对

这个比你们那个虚拟机干嘛快多了那等它启动起来之后下载完之后我们就可以启动了所以说我们来还写到命令第一个我们要研究新建容器

并启动OK我这里面的粒子大量会藏大量的按键大家干嘛一定要跟着我走

我们看一下这个容器有没有下载完毕那这边的话它还正在去Pu这个地方的话可能会比较慢当然下载的时候会比较快

因为我们配置了镜像只要找到了你可以发现它就非常快那这个地方是一个Santos OS它是一个基本镜像

你可以发现它是不是就一层啊非常小它很快就下完了下完之后就可以使用了那看下载完毕了

下载完毕之后呢我们就可以通过DocRom命令来启动那我们还是看一下RomHelp那你看到Rom命令能跟着参数多的要死看到同学扣一样

就是我们Rom你可以看到比如说Gun AGun D然后还有常用的比如说Gun P

然后Gun H这些都是我们经常用的Gun Warlum挂在一些外卷这些东西都是我们常用的

我们来一个一个的看不要着急那还是一样的我把这个命令写到这里DocRom命令

对不对那DocRom命令的话它是怎么样的一个策略这是我们的一些可选可选参数

然后后面的话就是咱们的一个Image我们要去启动一个镜像对不对那就是这么一个意思

来DocRom启动一个镜像就跟我们的白画文是一样的那我们来说一下参数的说明来参数说明

说明那我们来看一下我们在这个地方的话经常会用的一个参数刚刚内幕

就是我们要给这个容器起个名字它跑起来了我们得知道它叫什么对不对那这个地方会有一个内幕

这个地方是咱们的容器名字就是这个容器跑起来了我们得给它们一个名比如说

我们跑起来的他们开讨肯定要有个他们开讨01他们开讨02对不对类似于这样的一个名字

用来区分人用来区分容器区分容器这是第一个

那除了这个之外还有一个GunDGunD代表我们以后台方式运行我们经常启动一些应用

比如说GunGa你们这种启动是前台的我们成金是不是要用NoHoop其实这是大家用的NoHoop

那除了这个之外我们现在的话去用Ga去用Docker运行就是GunD就可以了

加一个参数机架比较简单互台运行这个都是我们常用的然后就是GunI

还有一个GunT那GunI-T呢我就把它写到一块吧GunI-T那这个就是使用

咱们的一个交互方式运行比如说我们要等进入容器查看内容我们一般干嘛可以使用到GunI-T

当中干嘛都会用到然后还有就是GunP那GunP就是指定端口指定容器的端口

比如说我们这个GunP来我们要启动一个8080端口你肯定得把它8080打开对不对那除了这个之外

它之外有复杂的应用比如说它可以跟咱们的主机硬设起来那就可以在两个地方访问我们等会会再说

先过一个演书然后更大批跟小批是有区别的这个是随机指定端口来随机指定端口OK

那这就是一些常用的命令干嘛我们都会给大家讲到那在这个地方的话指定容器端口这个地方

大有门道我们干嘛等会都给大家说在这个地方给大家列出来四种方式

就是GunP有四种使用方式第一种的话就是干嘛主机端口然后硬设到我们的一个容器端口来

容器端口那除了这个之外呢我们GunP还经常可以干嘛直接就写容器端口那这个就是不往外面走了

对不对那还有甚至你不想写的话直接写一个容器端口这个嘛这个也是可以实现的

那我们最常使用的话还是这种方式这种这种常用的

一定要记住常用的先把这个命令搞明白刀壳的命令是最多的但是学起来非常非常快

还有一种方式呢就是你可以加上主机的名字这个主机端口你可以通过IP去指定所以说这地方还能带一个IP

然后主机端口容器端口我们最常使用的是第二种来现在的话了解了一个刀壳ROM命令然后我们就可以嘛

我们来先体验一下对不对我们来体验一步测试一下来测试

测试还是一样的我们来这边去写刀壳Images看一下这个现在我们下载了一个

散头OS金像刚刚下载的对不对那下载完之后这个金像不大

所以说我们用起来比较方便那现在我叫运行比如说刀壳ROM是不是运行这个

那现在我要给这个取一个名字或者我要进这个容器里面我就要干嘛呢我就

首先的话刚IT我们说了这样的话就可以进入容器交互运行对不对然后我们要启动一个金像

比如说现在我要启动散头OS那现在的话就把散头OS启动起来了然后我要去交互运行你要给我一个控制台吧那Linux里面一般控制台

都在并下面对不对那除了咱们的可能有的人经常用的是Bass命令还要用的是SH命令这都是一样的

我们这边默认的话是Bass命令来走你来看到区别的同学扣1现在我们已经进入了容器这个地方就是启动并进入容器

写到这我不这么写来我写到这你们看这个Root旁边的东西就明白了

来这个地方是启动并进入容器来来并进入容器那这个地方的话首先注意我们的一个区别

Linux后面这个跟的是主机名称对不对那现在我们启动了散头OS之后这个主机名是不是变成里面了那现在的话我们来看一下

这里面的话它这个主机名其实就是这个镜像名看到了吗这边的主机名就是我们的镜像ID

那现在的话我可以在这边Ls看一下你会发现这个容器里面这个跟我们容器外面这个

是不是一样的因为这个就是我们下载的散头OS它就是一个小型的服务器来这就是我们下载的

因为发现这个容器内它其实就是一个自己的服务器环境我这个怎么打不开了来写到这里

就是我们这个地方查看查看容器内的散头OS那现在你要去理解我这话我们说

内部的这个散头OS跟外部现在有关系吗没有半毛钱的关系能理解我这是不是没有半毛钱的关系

OK而且这个内部的有一些基础版本就是这个基础镜像它很多命令都是不完善的

甚至我们后面的话就可以看到然后现在我进来了这个容器我想退出去怎么进来

来exit退出去那现在就退出去又回到我们的狂声

我们再来Ls看一下你看着镜像里面的东西跟镜像外面的最大区别镜像外面咱们是服务器有基本的显示

这边的话是没有的对不对那这是我们镜像外面的我东西比较多的多了那比如狂声目录上面没有

OK那这就是咱们的一个退出命令退出容器的命令就是以后你们进入了容器怎么让退出来

就是这么一个命令从容器中退到主机从容器中退回咱们的一个主机OK

剩下命令干嘛我们还会继续再说那现在的话我已经退出来了我还想去查看一下

有哪些容器正在运行对不对那就列出所有的什么运行中的运行的容器就是我想看一下

我这个怎么运行的因为我这张启动起来我不知道它在运行对不对我clair一下

我哪知道我这张有个docker在运行所以说命令来了wnex命令下ps那这边的话就是dockerps回车

你就可以看到当前是在运行的一个容器这边没有运行因为我们干嘛已经一个Z次退出了

那现在的话怎么看一下我们运行过的呢那我们来看一下dockerps放A那就是查看曾经运行过的

你可以看一下我们刚才运行的东西就在这边干嘛全部都有比如说sandOS

我们之前的那把它干嘛取过来dockerps命令这些都是非常常用的命令

取过来取过来dockerps命令那这边的话我们也有些常用参数

我都写在这个地方第一个是干A就是列出当前正在运行这地方有个字

正在运行如果停了它就没了运行的容器那但是的话这是默认的容器

你一旦加了AA就代表A的意思对不对那就会它会顺带

带出什么历史运行过的容器而如果你直接一个dockerps裸的

空的那我经常就流空空的就代表是当前在运行的一些东西对不对

那除了这个之外还有就是咱们的一些比如说干N等于这个地方可以通过一个参数来判断

来就是显示最近创建的容器来显示最近创建的容器那这个地方可以显示各数对不对

的容器那我们这地方拿过来比如说现在我来ps跟a我现在只想显示一个你可以N等于1

对吧那这时候它就会只显示这个最近的这一个散头S就可以查出来这个经常不用

那还有的话我们只想显示这个容器的编号来container ID说我们容器编号对不对

那这个地方怎么样去操控我们也要写在这的把它缩下去来这个地方有个名字

经常会使用也是GunQ刚才我们说只显示进下是不是aq

容器也是一样的但是前面是一个是Docker PS一个是Docker imagine这些区别一定要多去写

这个Linux的命令唯一的方式就是熟能生巧你不练改学对不对那这个地方一定要写

这就是咱们直显示容器的编号那我们现在的话就可以玩起来了比如说Docker PS-aq显示所有当前容器的编号

对不对这个容器运行了一般都会存在如果你想拿不要了干嘛可以把它删掉

那现在的话我们要干嘛要想到一些删除容器那写到这儿删除容器来删除容器

那讲这个删除容器之前呢我们刚才是不是说过一个退出容器刚刚我在看到直播间有小伙伴老师刚才那个XS退出就停止了

能不能退出的时候不停止呢所以说把这个退出容器给大家去详细的说明一下就是我们刚才用了一种方式做XS直接把它退出了

对不对这是我们的一种方式这是干嘛直接容器停止并退出那还有种方式

我要容器不停止过退出可以通过快捷键Ctrl来加p加q

三个键啊来这是咱们来容器不停止退出我们也可以来测试一下容器不停止来退出

那我们来看一下测试一下比如说现在我们Docker PS看一下当前应该是没有存在运行的是不是空档没有存在运行的

那我现在的话去启动一下来Docker RunGang IT交互模式运行这样才能进得去否则进不去

我们先布后台后台的话等会说来我们通过Bass Mini进去那现在进去了之后我Ctrl加p加q

你看到它是不是直接从当前运行退回到狂神了那我现在在Docker PS来可以看到这个容器正在运行看到了同学扣一样

这就是咱们的第二种退出方式就是大家如果想我退出之后容器不停止那这个时候就通过Ctrl加p加q

能理解我的意思吧OK再往下走删除容器的话也是比较简单的我们刚才干嘛也给大家用过

那这边的话就是Docker Remove我们Remove i就是移除进向Remove就是指的容器所以整个Docker就是在围绕着进向跟容器干嘛

来打交道把他们两个理解清楚了这非常快那容器ID还有就是删除全部的容器是吧

那根据我们刚才命令的话也是可以拿过来去测试来Docker Remove我们要去把它全部移除那这个地方的话

可以直接Rm-F通过我们刚才的方式可以玩参数传递对不对那我们来一个参数传递

参数传递的话就是我们要查出来所有的容器那我们刚才讲过了Docker PS-AQ查出来所有容器的ID

然后把它干嘛Gun FD规删除是不是就可以了来这个地方写上

这就是我们的删除删除咱们的一个指定的容器指定的容器然后第二个就是咱们的删除所有的容器

那现在的话我们可以去测一下我们一个一个现在我们有两个镜像我们可以把这个镜像

没事它正在运行我们也不管它我们来Docker PS-A那现在我们这方面有好几个镜像

我们干嘛可以把它清一下我们来一个一个一个Docker RemoveRemove是直接一镜像我们来比如说

移除一个散头OS来点击粘贴你看这个可以移动成功

但是还有个在运行的我们来看一下这个运行的能不能移动成功比如这个EC的好像是在运行的

我们来删除你会发现这种运行是不能删除的对不对如果要强制删除

就是RM-F是不是那这个地方的话就是来不能

不能删除正在运行的容器运行的容器那如果要强制删除那就是RM-F

这是我们Linux就学过的那删除所有容器干嘛也可以通过它直接删除那我们来试一下现在的话

我们Docker来 PS-AQ可以看到当前我们有两个容器对不对

我先把它干掉来DockerRemoDocker-F

这是移除全部对不对那现在的话我们还可以通过Docker-F查询出来

所有的刚才我们查的PS-AQ那把他们的这个容器ID作为参数

然后地规删除是不是两个都没了来 回车那现在就没了

现在在Docker PS-AQ就空了对不对那所以说这是一种

干嘛全部删除的路径看懂了同学可以跟我们刚才逻辑是一样

这种操作是我们经常用的那除了这个之外再给大家扩展一个Linux命令我们除了这么删除之外咱们还可以干嘛

通过管道府这也咱们经常用的比如说刚一刚Q查出来所有的我们通过管道府

把这个查出来所有ID然后干嘛过滤到咱们这边Docker进行Remove有个叫什么

Chax来ARGS来这个命令的话

也是进行一个一个的删除对不对把它最删除Docker-Remove这个命令也可以删除

所有的容器只要你Linux玩得好你怎么玩都不会错来删除所有的容器

这是两种方式那到了这个位置的话我们在容器里面那些基本操作可以了对吧

然后就是咱们来说一些启动和停止容器的操作启动和停止容器的操作来我们把这个地方写完

还是一样的Docker-Start就是我们想把一个这个停止掉的容器通过这个容器ID

可以通过Start方这个嘛启动起来容器ID那除了这个之外我们要重启一个容器

Rista就跟Linux命令是一样的那现在的话我们要停止一个容器正在运行的

我们可以通过Stop来停止那如果要杀掉一个容器就是你可以暴力的删掉通过Kill来删这几个命令呢

都是Linux的命令所以说大家听起来非常轻松对不对那这咱们呢是启动容器来

然后再往下走的话这是咱们的一个重启容器然后再往下的话是一个停止当前正在运行的容器来

当前正在运行的容器然后还有一个就是DockerKill我们把它杀掉那写到这个地方写到这里

强制停止当前容器如果你直接DockerStop停止包错了DockerKill把它干掉就这么简单跟Linux里面的操作是一样的

对不对OK那我们来试验几个就好了比如说我现在的话运行一下

来我们现在启动咱们的一个SandOS那现在我进去了之后我把它Exist退出了那现在的话

我们来看一下DockerPS那这边的话是没有正在运行对不对DockerPS跟Li历史运行过的

在这的那现在我们把它启动DockerStartOK启动当前这个容器的IDcontainerID

把它扎过来回车那现在的话这个容器它就启动成功了我们来试一下DockerPS

那这边的话你可以看到正在运行对不对现在我们这个停止DockerStop

跟我们的Linux面临是一样的那我要把这个容器停了那停了之后的话我DockerPS应该看不到它正在运行了也是一样的

这是咱们的几个基本的命令OK到目前为止的话容器命令的话先了解这些

那了解完这些之后我们再往下走这些都是基本命令我把它称数人这是你要玩一个Docker

这些命令是你闭着眼睛干嘛都得玩会的那这些命令完了之后我们肯定还有一些进阶命令

对不对就是我们常用的命令常用的其他命令那这个命令的话是很重要的

这个地方其实是非常重要的我们来先停一下把这个基本的容器命令给大家停止

### P13：常用命令小結

我们来看一下刚才的话已经把这个基本的命令都讲完了给大家来画了一幅这幅图是官方的我们来给他小结一下在这里面的话就讲了我们的所有的命令你可以看到我们学的命令主要是跟容器金像相关的

对不对那除了这个之外还有跟咱们远程仓库的一些东西来所以说命令是非常非常多的但是不要着急啊不难我们这边的话现在是把容器这个地方的命令还有

continu是学完了之后呢我们要去学习dogfairrepository还有咱们生存的一些文件那这还有一些命令那在这个图里面就把所有东西显摔了比如说第一个

我们想从容器内考卫到咱们的linux上是不是用Copy命令啊看到的同学可以啊我们一个一个来点个和我们讲过的就是把容器内部的文件考卫到主机上

那就是通过Copy命令我们要把一个images启动成一个容器通过wrong命令干嘛就可以进行启动了对不对那这个时候我们运行啊我们要停止怎么办呢

我们可以通过q和stop进行停止如果要启动了可以启动通过start进行启动对不对这个没有什么问题你也可以通过pass进行暂停啊那除了这个之外

在里面有一些常问的命令比如说logs查看日子inspect查看有缘数据对不对attach也是进入咱们的一个命令然后再往下的话GunPitGunQ这是直接干嘛

咱们就是运行的方式退出然后这个地方是可以就是控制它的一个端口GunPitps看里面的所有的竞争信息m移除xq的也是进入执行对不对

那这边写的非常明显啊这个UseQ的是一个命令行中端而这边attach是不是就是直接进入啊对不对这是两个小投标

这两个命令非常重要平时容易有人搞混了ok那top的话就是查看一些精神的心意在这边的话

我们还都已经讲了那除了这些命令你看着这命令是不是只是容器跟这样相关的剩下命令还多着了我们要干嘛

一点一点的学习而docker容器的一些命令是不是在这边显示的比如说dockerversion,dockerinfo,dockerinvents你们在这边都可以看

ok那这是我们的一个命令的小节具体的命令的话是十分多的还是给大家讲那句话

没写一个命令干嘛就把它自己寄到博客里面否则有些命令隔世忘了你还经常比如说都已经学到很下面的

你还想回到上面来看一个命令这样是不是非常愚蠢啊你一定要写一个字的一种比如说像我下面那种小节这样的

那这就是我们的一个帮助文档以后之后我们要看什么命令直接来这个手则上面一查是不是一眼就看清楚了来

这边的话他现在的话已经用咱们的abcd就是咱们的英文字母给大家排序好了以后你要查什么

在这边直接查看就可以了非常非常的方便那所以说在这个地方记住Docker的命令是十分多的是十分多的

十分多的那上面我们学习的那些都是最常用的一些容器和镜像的命令那之后我们还会学习很多命令很多命令

很多命令比如说我们自己写了一个镜像怎么发布这边的话大家看一下你看

是不是我们还有logging命令我们可以登录看到了吗你们可以登录上docker app或者登录上阿里云

然后你可以把你的镜像干嘛通过push传上去跟getup是一样的你也可以通过docker fail来制作一个镜像

你看到没有这是一个单向线我们可以通过docker fail这个技术这个文件直接生成一个什么

镜像bude这个东西非常高级我们之后学的一个核心你把docker fail彻底理解了你就知道docker的所有运气了

当然我们在此之前的话还要去把docker的镜像认真练一下还要我们要做一些练习所以说非常难那这个地方接下来的话

接下来接下来就是一堆的练习一堆的练习那我带大家练习几个剩下的话

大家干嘛可以自行下去练习我们来练一些来玩一玩让大家来体验一下用docker的好处

比如说我们来部署一些真实的环境来测试一下那我们来几个作业练习OK 那我们来给大家停一下这个小街

