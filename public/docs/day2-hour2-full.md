# Day 2 第二小時：Docker 架構與工作原理

---

## 一、前情提要（3 分鐘）

上一個小時我們講了為什麼需要容器技術，以及 Docker 的基本概念。

我們知道了：
- 容器解決環境一致性問題
- Docker 有三個核心元素：Image、Container、Registry
- 容器比虛擬機輕量、快速

這個小時，我們要深入了解 Docker 的架構。知道它底層怎麼運作，之後使用起來會更有信心，出問題也知道怎麼排查。

---

## 二、Docker 架構總覽（10 分鐘）

### 2.1 Client-Server 架構

Docker 採用 Client-Server 架構，有三個主要元件：

1. **Docker Client**：使用者介面，接收你的命令
2. **Docker Daemon**：背景服務，實際執行工作
3. **Docker Registry**：遠端倉庫，存放 Image

當你在終端機輸入 `docker run nginx`，發生了什麼事？

```
你 → Docker Client → Docker Daemon → (Registry) → Container
```

1. Docker Client 接收你的命令
2. Client 透過 REST API 把命令送給 Daemon
3. Daemon 檢查本機有沒有 nginx Image
4. 如果沒有，Daemon 從 Registry 下載
5. Daemon 用 Image 建立並啟動 Container

### 2.2 為什麼要分開？

你可能會問：為什麼要分成 Client 和 Daemon？直接一個程式不就好了？

分開有幾個好處：

**遠端操作**

Client 和 Daemon 可以在不同機器上。你可以在自己的筆電上操作遠端伺服器上的 Docker。

```bash
# 連接遠端 Docker
docker -H tcp://192.168.1.100:2375 ps
```

**權限分離**

Daemon 需要 root 權限才能操作容器。Client 可以以普通使用者身份運行，透過 socket 跟 Daemon 溝通。

**服務化**

Daemon 以背景服務運行，開機自動啟動。不管你有沒有開終端機，Docker 服務都在。

---

## 三、Docker Client 詳解（8 分鐘）

### 3.1 Docker CLI

Docker Client 最常見的形式就是命令列工具 `docker`。

```bash
docker version    # 查看版本
docker info       # 查看系統資訊
docker ps         # 列出容器
docker images     # 列出映像檔
docker run        # 執行容器
```

這些命令都是 Docker Client 提供的。

### 3.2 與 Daemon 的溝通方式

Docker Client 透過以下方式與 Daemon 溝通：

**Unix Socket（預設，Linux/Mac）**

```
/var/run/docker.sock
```

這是一個本機的 socket 檔案。Client 和 Daemon 在同一台機器上時，透過這個 socket 溝通。

**TCP**

```
tcp://localhost:2375    # 未加密
tcp://localhost:2376    # TLS 加密
```

用於遠端連線。生產環境務必使用 2376 + TLS。

**環境變數設定**

```bash
export DOCKER_HOST=tcp://192.168.1.100:2376
export DOCKER_TLS_VERIFY=1
export DOCKER_CERT_PATH=~/.docker/certs
```

設定後，所有 docker 命令都會送到遠端。

### 3.3 Docker API

Docker Client 底層是透過 REST API 跟 Daemon 溝通。你也可以直接呼叫 API：

```bash
# 透過 socket 呼叫 API
curl --unix-socket /var/run/docker.sock http://localhost/containers/json

# 透過 TCP 呼叫 API
curl http://localhost:2375/containers/json
```

這代表你可以用任何程式語言寫程式來操作 Docker，只要能發 HTTP 請求就行。

Python、Go、Java、Node.js 都有官方或社群維護的 Docker SDK。

---

## 四、Docker Daemon 詳解（15 分鐘）

### 4.1 dockerd

Docker Daemon 的執行檔叫做 `dockerd`。它在背景持續運行，等待 Client 的請求。

在 Linux 上，dockerd 通常由 systemd 管理：

```bash
# 查看 Docker 服務狀態
systemctl status docker

# 啟動 Docker 服務
systemctl start docker

# 停止 Docker 服務
systemctl stop docker

# 設定開機自動啟動
systemctl enable docker
```

### 4.2 Daemon 的職責

Docker Daemon 負責所有實際的工作：

**Image 管理**
- 從 Registry 下載 Image
- 在本機儲存和管理 Image
- 建立新的 Image

**Container 管理**
- 建立、啟動、停止、刪除 Container
- 監控 Container 狀態
- 收集 Container 日誌

**Network 管理**
- 建立和管理 Docker 網路
- 處理容器之間的網路連接
- 處理 Port Mapping

**Volume 管理**
- 建立和管理資料卷
- 處理資料持久化

**安全**
- 驗證 API 請求
- 管理容器的隔離和權限

### 4.3 containerd

Docker Daemon 不是直接管理容器，它把容器相關的工作委託給 containerd。

containerd 是一個獨立的容器執行環境（Container Runtime），負責：
- 容器的生命週期管理
- Image 的傳輸和儲存
- 容器的執行和監督

架構是這樣的：

```
Docker Client
     ↓
Docker Daemon (dockerd)
     ↓
containerd
     ↓
runc（實際執行容器）
```

**為什麼要這樣分層？**

歷史原因。早期 Docker 是一個單體程式，後來為了標準化，把容器執行的部分拆出來變成 containerd，再後來把最底層的執行部分拆出來變成 runc。

runc 是 OCI（Open Container Initiative）標準的參考實作。這個標準定義了容器應該怎麼執行，任何符合 OCI 標準的執行器都可以替換 runc。

**這對你有什麼影響？**

大部分時候沒影響，你只要跟 Docker Daemon 打交道就好。但了解這個架構有助於排查問題，也有助於理解為什麼 Kubernetes 可以不用 Docker。

### 4.4 Daemon 設定

Docker Daemon 的設定檔在：

```
/etc/docker/daemon.json
```

常見的設定：

```json
{
  "storage-driver": "overlay2",
  "log-driver": "json-file",
  "log-opts": {
    "max-size": "10m",
    "max-file": "3"
  },
  "registry-mirrors": [
    "https://mirror.example.com"
  ],
  "insecure-registries": [
    "192.168.1.100:5000"
  ],
  "default-address-pools": [
    {"base": "172.17.0.0/16", "size": 24}
  ]
}
```

**storage-driver**：儲存驅動，overlay2 是目前最推薦的。

**log-driver**：日誌驅動，控制容器日誌怎麼儲存。

**log-opts**：日誌選項，設定日誌檔案大小上限，避免日誌把硬碟塞爆。

**registry-mirrors**：Registry 映射站，加速 Image 下載。

**insecure-registries**：允許使用 HTTP（而非 HTTPS）連接的 Registry，用於測試環境。

修改設定後要重啟 Docker：

```bash
systemctl restart docker
```

---

## 五、Docker Registry 詳解（10 分鐘）

### 5.1 什麼是 Registry

Registry 是存放 Image 的倉庫。

你可以把 Registry 想像成 GitHub：
- GitHub 存放程式碼
- Registry 存放 Docker Image

就像 GitHub 有公開和私有 repo，Registry 也有公開和私有的 Image。

### 5.2 Docker Hub

Docker Hub 是最大的公開 Registry，網址是 hub.docker.com。

**官方映像（Official Images）**

Docker Hub 上有很多官方維護的 Image，這些 Image 經過官方審核，品質有保證：

- `nginx`：Nginx 官方映像
- `mysql`：MySQL 官方映像
- `redis`：Redis 官方映像
- `python`：Python 官方映像
- `node`：Node.js 官方映像
- `ubuntu`：Ubuntu 官方映像

官方映像的名稱沒有斜線，直接就是 `nginx`、`mysql`。

**社群映像**

其他使用者上傳的 Image，名稱格式是 `使用者名稱/映像名稱`：

- `bitnami/nginx`
- `linuxserver/nginx`

使用社群映像要注意來源可靠性。

**私有映像**

Docker Hub 也可以存放私有 Image，但免費帳號只能有一個私有 repo。

### 5.3 Image 的命名規則

完整的 Image 名稱格式：

```
[registry-host/][namespace/]repository[:tag]
```

**範例：**

```bash
nginx                              # 官方映像，預設 tag 是 latest
nginx:1.25                         # 指定版本
nginx:1.25-alpine                  # Alpine 版本（更小）

mysql:8.0                          # MySQL 8.0
python:3.11-slim                   # Python 3.11 精簡版

bitnami/nginx                      # 社群映像
bitnami/nginx:1.25                 # 社群映像指定版本

gcr.io/google-containers/nginx     # Google Container Registry 的映像
192.168.1.100:5000/myapp           # 私有 Registry 的映像
```

**Tag 的重要性**

`latest` 是預設的 tag，但它不代表最新版本——它只是一個名字叫 latest 的 tag。

**永遠不要在生產環境使用 latest tag。** 因為你不知道它實際指向哪個版本，而且可能會變。

應該使用具體的版本號：

```bash
# 不好
docker pull nginx:latest

# 好
docker pull nginx:1.25.3
```

### 5.4 私有 Registry

企業通常會架設私有 Registry，原因：

1. **安全性**：不想把公司的 Image 放在公開平台
2. **速度**：內網下載比較快
3. **合規**：某些產業法規要求資料不能離開公司

常見的私有 Registry 方案：

**Docker Registry**

Docker 官方提供的開源 Registry，最簡單：

```bash
docker run -d -p 5000:5000 --name registry registry:2
```

**Harbor**

VMware 開源的企業級 Registry，功能豐富：
- 漏洞掃描
- 存取控制
- 映像簽名
- 多租戶

**其他雲端服務**

- AWS ECR（Elastic Container Registry）
- GCP GCR（Google Container Registry）
- Azure ACR（Azure Container Registry）
- 阿里雲 ACR

### 5.5 Image 的拉取和推送

**拉取 Image**

```bash
# 從 Docker Hub 拉取
docker pull nginx:1.25

# 從私有 Registry 拉取
docker pull 192.168.1.100:5000/myapp:v1
```

**登入 Registry**

```bash
# 登入 Docker Hub
docker login

# 登入私有 Registry
docker login 192.168.1.100:5000
```

**推送 Image**

```bash
# 先用 docker tag 設定目標名稱
docker tag myapp:v1 192.168.1.100:5000/myapp:v1

# 推送
docker push 192.168.1.100:5000/myapp:v1
```

---

## 六、Image 的分層結構（12 分鐘）

### 6.1 Layer 的概念

Docker Image 不是一個單一的檔案，它是由多個層（Layer）疊加而成的。

每一層代表一組檔案系統的變更：
- 第一層可能是 Ubuntu 基礎系統
- 第二層可能是安裝 Python
- 第三層可能是安裝你的套件
- 第四層可能是複製你的程式碼

```
┌─────────────────────────┐
│   你的應用程式程式碼      │  Layer 4
├─────────────────────────┤
│   pip install 套件       │  Layer 3
├─────────────────────────┤
│   安裝 Python            │  Layer 2
├─────────────────────────┤
│   Ubuntu 基礎系統        │  Layer 1
└─────────────────────────┘
```

### 6.2 為什麼要分層？

**節省空間**

假設你有 10 個應用程式，都是用 Python 3.11 + Ubuntu 為基礎。

如果不分層，每個 Image 都要包含完整的 Ubuntu + Python，10 個 Image 就要 10 份重複的資料。

有了分層，Ubuntu 和 Python 的 Layer 只需要存一份，10 個 Image 共用這些 Layer，只有應用程式自己的部分是獨立的。

**加速下載**

當你拉取一個 Image，Docker 會檢查本機已經有哪些 Layer。已經有的就不用再下載了。

```bash
$ docker pull python:3.11

3.11: Pulling from library/python
a2abf6c4d29d: Already exists      # 本機已有
a9edb18cadd1: Already exists      # 本機已有
589b7251471a: Already exists      # 本機已有
186b1aaa4aa6: Pull complete       # 需要下載
7c55dd8f39fa: Pull complete       # 需要下載
```

**加速建構**

當你建構 Image 時，如果某一層沒有變化，Docker 會使用快取，不需要重新建構。

這就是為什麼 Dockerfile 的順序很重要——把不常變動的放前面，常變動的放後面。我們之後講 Dockerfile 時會詳細說明。

### 6.3 查看 Image 的 Layer

```bash
# 查看 Image 的層次
docker history nginx:1.25
```

輸出類似：

```
IMAGE          CREATED       CREATED BY                                      SIZE
a6bd71f48f68   2 weeks ago   /bin/sh -c #(nop)  CMD ["nginx" "-g" "daemon…   0B
<missing>      2 weeks ago   /bin/sh -c #(nop)  STOPSIGNAL SIGQUIT           0B
<missing>      2 weeks ago   /bin/sh -c #(nop)  EXPOSE 80                    0B
<missing>      2 weeks ago   /bin/sh -c #(nop)  ENTRYPOINT ["/docker-entr…   0B
<missing>      2 weeks ago   /bin/sh -c set -x     && groupadd --system -…   112MB
<missing>      2 weeks ago   /bin/sh -c #(nop)  ENV PKG_RELEASE=1~bookworm   0B
<missing>      2 weeks ago   /bin/sh -c #(nop)  ENV NJS_VERSION=0.8.2        0B
<missing>      2 weeks ago   /bin/sh -c #(nop)  ENV NGINX_VERSION=1.25.3     0B
<missing>      2 weeks ago   /bin/sh -c #(nop)  LABEL maintainer=NGINX Do…   0B
<missing>      2 weeks ago   /bin/sh -c #(nop)  CMD ["bash"]                 0B
<missing>      2 weeks ago   /bin/sh -c #(nop) ADD file:…                    74.8MB
```

每一行就是一個 Layer。`CREATED BY` 告訴你這一層是怎麼來的。

### 6.4 唯讀層與可寫層

Image 的所有 Layer 都是唯讀的。

當你用 Image 啟動一個 Container 時，Docker 會在最上面加一個可寫層（Writable Layer），也叫做 Container Layer。

```
┌─────────────────────────┐
│   Container Layer       │  可寫（Container 專屬）
├─────────────────────────┤
│   Layer 4               │  唯讀
├─────────────────────────┤
│   Layer 3               │  唯讀
├─────────────────────────┤
│   Layer 2               │  唯讀
├─────────────────────────┤
│   Layer 1               │  唯讀
└─────────────────────────┘
```

Container 裡的所有檔案操作——建立檔案、修改檔案、刪除檔案——都發生在這個可寫層。Image 本身不會被改變。

這就是為什麼同一個 Image 可以啟動多個 Container，而且互不影響——每個 Container 都有自己獨立的可寫層。

### 6.5 Copy-on-Write

如果 Container 想修改一個來自 Image Layer 的檔案，怎麼辦？

Docker 使用 Copy-on-Write（寫時複製）策略：

1. Container 想修改 /etc/nginx/nginx.conf
2. 這個檔案原本在 Layer 3（唯讀）
3. Docker 把這個檔案複製到 Container Layer
4. 修改發生在 Container Layer 的副本上
5. 原本的 Layer 3 不受影響

這個機制讓 Image 可以被多個 Container 共用，同時每個 Container 又可以有自己的修改。

---

## 七、Storage Driver（8 分鐘）

### 7.1 什麼是 Storage Driver

Storage Driver 決定了 Docker 怎麼在硬碟上儲存 Image 和 Container 的資料。

不同的 Storage Driver 有不同的特性和效能。

### 7.2 常見的 Storage Driver

**overlay2**（推薦）

目前最推薦的選擇。
- Linux 核心 4.0+ 原生支援
- 效能好、穩定
- 同時支援 ext4 和 xfs 檔案系統

**devicemapper**

Red Hat 系（RHEL、CentOS 7）早期的預設選擇。
- 現在已不推薦，應該用 overlay2

**btrfs / zfs**

這兩個需要特定的檔案系統支援。
- 功能強大，支援快照
- 但設定複雜，不常用

**vfs**

最簡單的實作，不支援 Copy-on-Write。
- 每個 Layer 都完整複製
- 效能差、浪費空間
- 只用於測試或相容性問題排查

### 7.3 查看和設定 Storage Driver

```bash
# 查看目前使用的 Storage Driver
docker info | grep "Storage Driver"
```

設定 Storage Driver（在 /etc/docker/daemon.json）：

```json
{
  "storage-driver": "overlay2"
}
```

**注意**：改變 Storage Driver 會讓既有的 Image 和 Container 無法使用（因為儲存格式不同）。通常只在初次安裝時設定，不要在有資料的系統上隨便改。

---

## 八、本堂課小結（4 分鐘）

這個小時我們深入了解了 Docker 的架構：

**Client-Server 架構**
- Docker Client：使用者介面，接收命令
- Docker Daemon：背景服務，實際執行工作
- 透過 Unix Socket 或 TCP 溝通

**Docker Daemon**
- 管理 Image、Container、Network、Volume
- 委託 containerd 執行容器
- 設定檔在 /etc/docker/daemon.json

**Docker Registry**
- 存放 Image 的倉庫
- Docker Hub 是最大的公開 Registry
- 企業通常架設私有 Registry

**Image 分層結構**
- Image 由多個唯讀 Layer 組成
- Container 有一個額外的可寫層
- Layer 共用節省空間、加速下載
- Copy-on-Write 機制

下一個小時，我們要開始動手安裝 Docker。

有問題嗎？

---

## 板書 / PPT 建議

1. Docker 架構圖（Client → Daemon → Registry）
2. Docker Daemon 元件圖（dockerd → containerd → runc）
3. Image 完整命名格式
4. Layer 分層示意圖
5. Container Layer（可寫）vs Image Layer（唯讀）
6. Copy-on-Write 流程圖


---

## 📺 補充教材：狂神說 Docker

> 以下內容來自【狂神說Java】Docker 教程系列，作為本節的補充參考資料。

### P05：Docker 中的名詞概念

我们来继续了解了Docker能干嘛之后我们就要干嘛就要安装安装之前我们来了解一下Docker的基本组成我们跟他说了什么镜像什么容器

我们得干嘛先对这些印象有一些客观的一些概念之后我们干嘛再往下去学习会好一点就是一个基本的名词

那我们来看一下我们可以去百度来看一下在这边的话有非常多图对不对那就是百度百颗这边

那我们翻到最上面在这个地方的话他有一个我们去搜一个Docker的架构图

那在这个地方的话大家就可以看到来Docker的架构图在这边的话比如说大家打开这个百度图片

这是我经常用的一个技巧在这里面可以看到很多东西来看比如说这个地方就换了Docker的一个架构图我们可以把这个图给大家取过来

那在这个图上面就形象的展现了Docker要用的所有东西大家如果是第一次接触Docker看到这个图就蒙了但是的话

如果在我们学完之后你再回过图来干嘛看这个图就非常清晰我们来看一下Docker它是怎么组成的

我们这个地方有几部分呢第一部分这个地方有个client客户端还有一个Docker的主机我们就把它想象成一个服务就好了Docker的一个服务

那还有一个就是我们的远程仓库对不对那现在的话来给大家去理解这分为了三部分也就是我们的

可以把它叫客户端还有一个服务器服务器然后还有一个就是咱们的一个仓库来 仓库

我们之前学过一个东西叫做Reduce还记得吗来我们学过Reduce它有一个Reduce Server

也是在本地就可以启动起来还有叫Reduce Client对不对那比如说我通过Reduce Server把它启动起来

然后我们通过Reduce Client的访问那这边的话我们把Docker启起来之后它也会存在这两个东西我们可以通过Docker的一些命令来进行访问

比如说Docker Run运行一个容器Docker Pool去拉取一个容器Docker Build的构建一个容器那构建完之后就要运行这个真正的运行

是在Docker的服务上去运行的它这个地方是一个Docker守护进程那守护进程里面要去运行它首先的话要通过进向运行这个进向大家就好比

比如说你把它想成java的一个java类对不对或者就是一个类模板class这个class是不是全局唯一对于java来说那它真正运行起来的时候

它是不是可以产生很多很多的对象那这每一个容器我们就把它叫做对象就好了你们可以这么去理解比如说这个进向

现在我是一个Tomcat的进向那这个Tomcat的运行唯一运行起来我可以要部署一个的话那可能这方就来个Tomcat一容器那现在我要再启动一个Tomcat

我还可以利用这个进向干嘛再启动一个把它叫做Tomcat2那这两个的话我们就可以把它搭建成一个局群

但是它本质还是通过这个进向来进行运行的然后最终呢这些容器还有些进向来自于哪了可能来自于我们的一个远程仓库对吧

那这个地方有个Docker Hub它的地址那这边都是有的那我们来先把这几个名词先听一下第一个就是咱们的进向刚才说的

第二个就是咱们的容器第三个就是咱们的仓库我们来先把这三个进向干嘛给大家一个一个说第一个就是咱们的Image

那第二个容器的单词呢比较简单container然后咱们的一个仓库的单词Repository对了

这几个单词的话我们就是学到现在的话大家应该完全是能够把它记住了那来Repository那什么就是进向来

哎呦来什么是进向来我们来说一下进向的话你们就把先Docker进向

进向就好比是一个模板我们说了加了里面是不是通过这个模板可以通过这个模板来创建咱们的一个容器服务

比如说我现在有个Tomcat进向那我现在要去把这个Tomcat启动起来这个进向是不能启动的要把这个进向运行起来才能启动对不对

那所有时候我要把它运行起来RawRaw明明就是运行运行起来之后呢它就会变成一个真正启动的一个服务

那这个服务就是一个容器嘛那比如说就是Tomcat01那这是咱们的一个具体的容器那它是来干嘛提供服务的那它除了通过这个进向可以干嘛

可以New很多通过这个进向可以创建多个容器那这个容器之间呢你也可以进行具体操作而这个容器就是我们这边了

你可以把它想象一个载体最终的一个服务运行那比如说最终咱们的一个服务运行或者项目运行就是在容器中的这个容器我们总算是

刚才说了是不是互相隔离的那我们来看一下这个容器的一个概念来这个容器的一个概念

容器的概念的话其实也不难我们这些说过的吧那我们来写了Docker利用咱们的一个容器技术干嘛

它可以做到咱们的独立运行一个或者是嘛来一组运用它可以把很多运放在一个容器里面去运行

完全是没问题的它是通过进向来创建的我们还说来创建的现在我说的这些名词

等于我们一去写代码或者就是一测试大家干嘛就立马明白了那它有一些基本的操作容器可以干嘛

可以启动停止对不对删除那这都是它的基本命令所以说等于我们要去干嘛

去学习这些命令也比较简单那再往下你可以把这个容器你看成是一个Linux你就可以把

来目前大家最开始理解的时候就可以把把这个容器理解为

就是是嘛一个简易的Linux系统Linux系统就是一个非常非常轻巧的Linux系统

它里面的话就是一些基本的东西比如说一些基本的命令我们的项目就放在这个特别微型的Linux系统上面

干嘛把它放不起来了所以说还比较简单的OK然后再往上

就是一个仓库仓库顾名思义是放东西的对不对那就是仓库就是来存放进向的地方

来进向的地方我们这么多进向肯定要拿个地方来放嘛来地方对不对

就给你们的开完项目肯定要放到Dock Hub上那我们这边也是要那这个仓库的话我们就要去算

仓库分为咱们的一个公有仓库和私有仓库那这个就跟咱们的Docker跟咱们的Git的概念是一样的公用的

就是所有人可以访问而私有的干嘛就是只有自己能够访问对不对那国内最大的一些

比如说咱们的一些Docker Hub对不对那官方的话就是Docker Hub那国内的话其实也有一些

包括阿里云的或者咱们的一些网易云华为云的那我这边就充电

也阿里云来讲他们都有咱们的容器服务来都有容器服务包括我们国内的话是非常慢的

就是这个仓库默认是国外的那我们在国内的话我们一般要去干嘛配置进向加速

我们的Maven是不是就这么干的来配置进向加速不然的话我们从国外去下载这个东西干嘛死慢死慢的

肯定是不行的所以说要把这几个概念理解一下我们等会的话通过代码运行

你一下就知道他是干嘛的了然后把这幅图我先放在这大家大脑里干嘛

产生一个基本的印象我们就是这么散布我们能够把刀壳安装完之后我们要用客户端来去干嘛去启动刀壳里面的一个容器

这个容器他要去干嘛去服务清理上去下载那具体的流程我们就来看一下我们是怎么做的

那我们来这个地方来安装咱们的一个刀壳了来安装刀壳OK来给大家挺一下

### P08：Run 的流程和 Docker 原理

我们再来看啊这个Hello World到底是怎么启动的呢我们这个地方刚才结了一个图我们把这个图复制过来复制到这

在这个地方啊我们Docker Run Hello World之后它是怎么一步一步的走到我们要把这个意思干嘛给它全部理懂啊我们这个地方继续给大家画图

比如说我这么来给大家画个图大家一下就懂了首先我们Docker Run就这一方就比如说我们把它叫做开始运行开始运行启动

对吧那首先开始运行了他第一步他说了我们干嘛来看一下我们这个图他说我们没有找到这个景象他说on to find没有找到那现在的话他肯定第一步要去寻找景象对吧就跟我们去经常面试的提问你这个地方敲下回车之后发生了什么很多人就懵了

这个地方敲下回车怎么去做的浏览器那现在的话就问同样问题Docker你们RUN了之后他做了哪些事情我们就把这个事情高动开始之后呢他首先Docker会在干嘛本机

寻找景象寻找景象这是他要做的什么第一步事情那假设在Docker的本机寻找到了之后他就会去做一个事情比如说来判断一下

判断本机是否有这个景象OK那这个时候无非就会产生两种情况嘛要么有要么没有对不对那假设有这个景象那如果这个地方是yes那结果他是有有的话就是什么使用这个景象

运行这个景象运行OK那如果没有这个景象呢那肯定就要去下载这个景象对不对那这个地方就是no我写一个那这就是咱们的一个去Docker Hub上下载

上下载那现在我们用了RUN的加速他就去RUN上下载对不对下载那就会比较快点Docker Hub比较慢那现在去下载完之后就能用了吗当然是不行的Docker Hub上也会产生两种情况

第一种就是他判断对吧咱们在Docker Hub就是这个仓库啊这Docker Hub就是一个仓库就跟Gate Hub是一样的是否可以找到这个景象那又会产生两种情况

一种是找到一种是找不到嘛对不对那我们来看一下假设如果找不到的情况下就是no找不到的话他就干嘛了就是返回错误那比如说找不到景象OK那如果能够找到呢那他肯定就是OK的对不对 yes那就是可以来下载

这个进行到本地来进行到本地那到了本地之后就跟这个地方是不是连起来了然后他现在发现本地有这个进行干嘛

就可以使用这个进行进行运行了那所以说就回到了这一步那这就是他的一个什么Docker Run的一个运行原理图那这个地方想Round运行流程分析图流程图

OK那之后我们的所有东西干嘛就是基于这个内容进行的那现在的话大家是不是就理解了刚才他这个地方为什么要打印这些日志啊什么Round之后为什么要去他说找不到找不到又去铺去下载下来对不对那这里面具体的所有东西到底是怎么执行的呢我们现在要开始干嘛一步一步的去研究啊这个Halloween的一个流程非常非常重要大家希望跟着我这个思路理解的时候再去找

那比如说现在我们来搜一个找不到的那你比如说Docker Run一个那狂神我绝对没有这个进向吧这个进向不可能存在的没人这么无聊回车你看他就说找不到这个进向他现在就尝试去远程下载他会发现下载下载不到他就会报一个错对不对你不可能去这么玩的那这就是咱们Round的一个基本流程啊那其实到了目前为止你看这被卡住了这卡住的话他是去找找不到

那我们在这个地方你看着从Docker组成到AndronDocker我们现在就把Docker安装这一块的所有命令去说了一下对不对但这些命令只是一个入门的命令我们再要往下学的话需要把底层的一些命令干嘛全部了解之前命令才是我们最重要的了解所有命令之前我们把这个底层原理在这个地方干嘛也给大家说一下那这个地方的话我看一下视频三分钟那我就不停了就直接一字印讲Docker聊一个问题是怎么工作的

就是我们要去聊一下这个Docker到底是怎么工作的对不对你看一下这边是不是报错了他说DockerAero Response没有找到这个狂神什么66666这个仓库对不对然后来Docker是怎么工作的呢他这个地方要记住那我们刚才说过Docker是一个什么CS的结构Client和Sower来Sower结构的系统

这个地方一定需要了解对不对那Docker是咱们守护进城Docker的守护进城就是他的服务就用Windows上的服务一样来守护进城好比你们安装了一个买择口他在后台运行对不对Docker也是他这个服务一在后台运行运行在我们的一个主机上

这个主机有时候要把它称为数组继啊他都是干嘛都是一个词那这个时候他们通过咱们的一个Socket来通过Socket从咱们的一个客户端

访问访问就好比你们去访问买择口干嘛是一样的那这个时候呢咱们的一个Docker服务进城也就是这个DockerSower我们就把它理解为DockerSower来DockerSower

那他就干嘛接收到我们的DockerClient你们就把它抢成客户端来客户端到指令ok就好比你们给买择口发了一个指令干嘛他就要进行执行对不对那就会就会执行这个命令那这个命令的话说白了就是跟容其跟进向相关的我们等会再去了解他是怎么做的那再给大家画幅图理解一下

守护进城打错了是吧这个字的话经常会打错这是这个书法的问题守护进城给你们改过来吧进城来我们来看一下在这个地方继续画这个图就是非常简单说白了什么意思了我们这个地方可能会存在多个客户端去连接就是比如说Docker的一些命令对吧

那我们把这地方画一些Docker的客户端然后他在后台呢其实他是一个整个大的一个服务啊那比如说这是咱们的一个后台的守护进城我写到这这咱们的一个客户机客户端客户端那这边的话可能也是我们的一个客户端那都可以连接客户端那这个整个大的呢这个就是我们的一个Lennix服务器

那我把它写到这个地方这就是咱们的一个Lennix服务器那我们在这个服务器上通过客户端去连接他对不对那他就去连接这些所谓的服务那我们把他画过来那这就是咱们的一个后台的守护进城

后台的守护进城ok拿到就连到这个后台守护进城之后守护进城连到这个之后呢他就会启动一些容器对不对那这个地方就是我们Docker容器这些东西都在咱们的服务之内

Docker容器ok那通过这个守护进城去操作容器的资源比如说我想获取这个资源是什么样的对不对那你们现在就发现啊这个容器跟我们的虚拟机是不互相隔离的说明一个意思这个容器就好比是一个小的Lennix

你就可以把它想象这好比来好比是一个什么小的Lennix来来来Ux虚拟机

那在这里面的话我有相对于Lennix独立的比如说我有自己的一些端口号能力就有意思啊比如说我在这里面我比如说运行了一个服务Local Hose他们开的8080

那这个8080外界是访问不到的它是在这个容器内的那比如说现在我又启动了一个容器我可能就跑了一个MySQL来比如说我跑了一个MySQL

那现在的话我这个地方来一个3306这个就是互相隔离的它这里面就好比容器里面也有个小的synic Linuxs训绩机它也在这里面能跑

这个的占用的进程啊或者内存资源小号是非常小的我们外面这个大的Linux跟这里面是干嘛相互隔离的那如果要把它这个端口

我们要去访问容器里面的我们还需要干嘛在Linux服务器上跟这个容器里面做一个连通这个的话我们之后再来讲

先把这个基本的干嘛概念搞懂来把这个地方写到这个地方那现在的话大家就知道Docker到底是怎么工作对吧我们通过客户端的命令

然后最后到这个守护进程在这里面的话放了很多容器启动了那这个容器默认是一个都没有启动的我们启动之后它才干嘛

才会产生啊比较简单然后第二个就是我们要了解Docker为什么比咱们的虚拟机快这个的话其实很多地方也都有讲讲最简单的话就是Docker有着什么

比咱们虚拟机更少的抽象层这个地方一定是了解的我们可以去来百度一下这个有很多那种比较好的帖子在这边干嘛都有去进行写

先抽象的理解概念因为的话很多命令大家还没学如果你不学那些命令的话那到后面的话你就不能理解他的意思现在的话就按照这种思想先去理解

比如说这个地方有一个图片我们把这个图片拿过来我觉得画得很好当他就讲了一个虚拟机跟Docker的一个区别把它放到这

这个图片没有下载下来可能是我的网络问题我们把它解个图在这个图里面的话他就讲了我们的一个

这首歌怎么把这个换一首歌这个是台观的歌第一个就是你看着

我们Docker的话他是不是直接跟咱们引擎相关走的而我们这边的话首先虚拟机安装了我们这个硬件之后比如说这是咱们的hostOS

上面还安一层虚拟再往上我们要去虚拟一个镜像出来我们要把它们的guestOS这个什么这个硬件干嘛

给它装起来装起来之后在这个硬件上就跑服务比如说这台电脑上我们要跑两个Santo OS那它叫干嘛

叫虚拟两个虚拟机出来这个就非常非常慢而我们的Docker呢没有这一层的概念它是直接干嘛

在咱们的主机上安装Docker这个服务那所有的东西干嘛是运行在Docker这个服务里面的就是容器所以说Docker它有着干嘛

比虚拟机更少的抽象成那这样下就Docker利用的是咱们的主机的就是宿主机咱们的一个内存内核

它用的是一个内核而我们这个虚拟机就是咱们这边的VMVM的话就非常麻烦是吧它是需要咱们的一个guestOS

就是我们需要去再搭建一个这样的环境但是跑不起来的就非常非常的慢所以说当我们去新建一个咱们的容器的时候

Docker它就不需要像虚拟机这样去运行对不对那写到这所以说咱们新建一个咱们的容器的时候那Docker不需要

像虚拟机一样重新加载一个操作系统内核这个是非常慢的

启动操作系统内核引导性的操作对不对比如说你们去装性玩都会引导操作非常非常慢那这个时候的话

虚拟机是加载咱们的一个guestOS那我们的话咱们是分钟级别的分钟

级别的分钟级别的就非常非常的慢而咱们的Docker来写到这儿

而Docker是利用咱们数组机是我们当前的电脑主机的操作系统

这个的话它就不需要再去虚拟一个出来了对不对就十分十分的快省略了

来省略写到这儿省略了这个复杂的过程OK

复杂的过程所以说相对来说就会比较快对不对那它是一个秒级的启动的话就是一个秒级的

十分的快你们干嘛可以直接在这个层面上就能把它理解了那再往这个地方的话

我们再来看一下这边有一些看一下有没有那种时间的图我们来看一下

这边的话这个地方有一个时间的图我们来把这种时间图给大家解一下这个干净

我们就用这个干净的话其实也够用我们把它解到这儿就可以了签一下那个就不看

在这个地方的话跟我刚才上面的总结是一样的比如说我们的虚拟化技术以及我们的

Docker容器那这边的话主要是我们的OS虚拟化它利用的是咱们的是吧

虚拟机性的我们是要根据容器来进行隔离对不对然后再往下

它的安全性的话还是可以的虚拟机安全性一定是最强的因为它是完全虚拟台电脑而我们这边

只是一个容器对不对而我们这边的话一般是全部是Linux的如果是Docker

现在的话是都支持了这个可能比较老了现在的话是都可以的了它速度比较快而且它在一个虚拟机上

可以干嘛可以安装很多OK那这是关于咱们的一个让大家了解一下

Docker它的一个运行原理说了一下Halloween的一个流程那现在大家知道我们要去启动一个应用那怎么样去找的一个过程

那现在就知道了对不对那这个底层原理的话先作为了解就是等大家把这个

所有的命令学回来之后我们再回过头来看我给大家说的这一段能理解我的意思这是学习方式

就是我先给大家去把这个概念说到这个地方原是让大家大脑有个抽象影响当中看命令的时候

你就会有一种思维那之后学习完所有的命令学习完毕所有的命令你们再回过头来

看这一张头脑会非常的清晰来 命令那再回过头来来 看这段

什么理论就会很清晰来 为什么这个地方现在大家不清晰的原因就是

因为理论干嘛还没有时间所以说接下来我们就干嘛我们就要去实践了

剩下的时间都是我们实践的那我们来看一下Docker的常用命令常用命令

命令OKDocker现在是支持全平台岛这个图的原因这个图我就把它删了吧

对我们来说也没有什么咋的作用OK那我们来看一下

Docker的常用命令就非常非常的多了OK给大家来停一下这段原理的部分

### P18：鏡像原理之聯合文件系統

我们来继续 刚才的话 闲聊了一会儿给大家然后这边的话 可实话 这个刚才因为有人捣乱的原因大家正常进入这个面板之后 你就能看懂了这边都是正常单词 容器 镜像 眷跟咱们的网络那再往下走的话 就是Docker镜像的一个具体

来 这个地方是一个探究的过程 它并不难我们来看一下Docker镜像 它到底是怎么做的 对不对那刚才我们已经说了 镜像什么它就是一个独立的软件包 包含了咱们的什么代码 运行的一些环境的库 还有一些环境辩量和配置文件

它全部都带了 就是我们未来的所有的应用所有的应用 就不需要咱们的服务器再去部署了 对不对所有的应用 干嘛 直接打包 来 直接打包部署这是咱们公司现在一个流行所以你们未来如果还不会Docker 马上就被淘汰了

就这么简单 这是一个非常现实的话那直接打包 成为一个Docker镜像那就可以干嘛 直接跑起来就是不需要运为干嘛 再去部署一些环境了跑起来 这就是非常非常的快

而且方便咱们的扩展跟部署ok 那镜像就是一个独立的软件包你们可以理解为 我们可以把它下载下来 对不对它可以放在我们的仓库 那如何得到镜像呢得到镜像 得到镜像呢

它有这么几种方式 第一种方式就是咱们的从咱们的远层仓库下载 下载 对吧第二种呢 是让朋友给你 朋友拷贝给你给你 然后第三种呢 就是自己制作一个镜像一个镜像 那这个之后我们还是会讲到

当然还有点时间 刀科Fail这个是我们后面要讲的ok 这得到镜像的一些方式只要你能把这个包拿过来 你干嘛 就可以直接运行那镜像加载什么原理呢

刚才我们看到它是不是分层下载的我们之前来看一下我们之前的笔记我们在刀科回顾Hallower的的时候就说过这边的所有东西 它是一层一层这样去下载过来的 对不对那这是怎么样去理解的呢

我们来把这个分层干嘛 理解一下在这里面呢 Linux里面有个概念叫做联合文件系统 也是UFS 大家可以去查一下那它就是可以嘛 将我们所有的东西干嘛进行分层管理 说白了

我们以前是不是学过Gate它可以干嘛 控制版本能理解为止 就是有一个记录我们每操作一上有个记录比如说 我第一步安装SantosOS

这是一个基本的环境架设那么这个地方就是第一层 就是一个记录那我就在上面安装了Balker那现在的话 这就第二层 我再安装了GDK那现在的话就第三层

那它每一层的话 它都会把它记录下来就是随时可以看到联合文件系统呢 就这么一层概念它支持对文件系统的修改干嘛一次一次的作为提交

来一层一层的叠加那我们就可以看到下载的时候那我们下载的 下载的时候看到的 一层一层的来 就是这个

来 理解的同学 扣1这是一个基本的概念就叫联合文件系统那我们就可以通过这种方式来记录它每一次修改 对不对

那每一次修改我们就可以看是否是新增呢还是叠加呀 假设啊我现在有两个进向比如说我的Tomcat

还有咱们的一个比如说MySQL他们底下都要用到咱们的一个基本的Linux的内核那假设我就写个

基本的 假设都要用到SantaOS那现在我的Tomcat下载了SantaOS那我的MySQL就不需要下载了能理解我这个同学扣1他们这一个联合文件

这一层是共用的它这边要用 直接干嘛从这边拿过去就可以了就是非常极大的节省的内存和空间

那假设上面是不一样的这是Tomcat那就按TomcatMySQL就按MySQL的嘛但是这一层是一样的

也可以方便咱们的内存ok 这就是咱们一个联合文件系统的一个什么基本的概念那咱们最开始的话

了解说概念之后接下来就是我们要了解它是怎么进行加载的刚才我们也说过了 对不对最开始的时候 看这幅图

这下面是不是一个boot 就是一个内核这是咱们的一个内核这个内核里面什么都没有现在我操作了一步

比如说你看第一步FromDiBand我就是下了一个DiBand 对吧然后在这边我去安装了一个咱们的EMACS

那这个时候就在这个内核上面干嘛加了一层镜像就是往上面加了一层能看到这是一个层的概念吗

能看到同学可以啊教你们看这些图很多人看不懂内核然后加了一层

EMACS一层对不对然后现在我又执行了一步来我现在又执行了一步

比如说我installed了一个Apache对不对那现在的话又加了一层EMACS上面是不是又多了一层啊能看懂同学可以

就是咱们的内核现在加上一个EMACS那这个地方的话是咱们的第一个然后还有咱们的第二个

再加个EMACS就变成一个越来越高的层了而这底下的这个层呢一般是功用的那这个地方来了解一下

那这地方就说了咱们说了Docker的镜像实际上就是一层一层的文件系统构成的

而这种层级的文件系统就是咱们的UFS叫做联合文件系统这样的话是不是一下子就有一点感觉了

对不对而我们在这里面呢分为两个名词一个叫bootfs一个叫rootfs

什么意思呢boot就是我们启动的时候我们任何一个系统系统来

启动需要干嘛引导加载对吧引导加载你们去用

装过机的人就知道引导加载那这个引导的话我们一般就是一个boot嘛那这个地方的话

最开始的时候这个加载那个系统的这一块东西是几乎不变的那些意思

就是我们启动的时候我们需要一个内核加上我们一个加载器来这个bootlater

那这个地方一个加载器跟一个内核那这个把这个内核加载成功之后我们的系统就运行起来了

对不对那运行起来之后那这个根就不要了对不对别说咱们这个加载就没了

那就是代表我们从咱们的一个黑屏就是我们用电脑来说黑屏到我们的开机开机进入咱们的系统

那这个之间的过程我们就把它称之为加载那假设这个加载它没有了对不对

加载完之后这个系统已经开起了这个加载是不是就可以卸载掉了那这一部分

我想问一下大家是不是功用的就是无论是什么进向它这部分是不是功用的那就有意思

因为进来之后这个系统就不用变了这是一个点第二个就是我们这边有个RootFS那就是我们真正这个系统启动之后

就会变成我们的一个基本的Linux系统就我们开机了那开机之后这里面就是一个典型的什么Linux的文件系统在这里面就是一个小的Linux

这就是我们说了为什么我们启动了之后一个容器就是一个小的什么虚拟机环境它就是一个完整的

虚拟机环境比如散头OS对不对那我们现在的话这个RootFS说白了就上面的是什么

不同的发行版比如说你们有散头OS有版图只是在这一层干嘛有了一些些许的区别

那我们平时安装的散头OS我们之前安装都是几个G对不对我给大家看一下我们这边的一些压缩包比如说我们这边有一个

录屏来我们拿过来啊在这边有一个文件呃我之前的话

安装包我都一般喜欢把它打包啊在这边你看这是我散头OS近相这是1.5个G这个一个压缩近相都这么大

但是我们这边的散头OS你看一下才多大呢Dalk images那我们在这边下载的你看一下

才237兆乖乖一个系统就这么点所以说它肯定是干嘛经纪检检过了

它没有那么多内核对不对那所以说我们这个地方得出一个命令对于一些比较精简的内核呢

就是OS操作系统那我们的Root FS可以很小就是一些文件嘛对不对基本的命令啊

和一些程序工具库就好了比如说我们的一些基本命令LS命令对不对CD命令

然后MAC命令你们刚才看我们连这些压缩命令的命比如LL什么VM它都没有啊这一些被压缩过的

然后这就是因为它的上面它这个Botus Trap这些很大的引导没了它只需要放一些文件跟命令就可以了

所以说就变得非常小这些指令机很小的对不对那这个时候它的底层呢

还是使用我们主机的一个内核这个时候容器内部就是命令真正的内核还是用的咱们的主机的所以说可以实现咱们效率的最大化

我们真正的启动虚拟机就是要去启动这个底下的这个跟这个内核引导非常慢那真正的话去运行这个命令

那就是分分钟的事情这也就是我们说的是吗咱们的虚拟机现在是不是就有感觉了

是秒击启动对虚拟机是咱们的分钟级分钟级别的而我们的容器

分钟级别而咱们的容器是咱们的什么秒击的那就是来源于这个底层的一个改变非常简单

那这个时候我们要使用一些新的系统我们是不是就干嘛去用一些新的系统就好了比如说你要文件的话你要用咱们Linux就用

要用咱们的SandOS用SandOS要用Ubuntu要用Ubuntu要用红帽就用红帽

这个上面的可能会有一些区别但是底层的引导是不变的那所以说我们根本就不要它一层我们只用底层的一个内核是不是

可以搞定OK理解了这个Docker进行加载的一个原理跟咱们的一个联合文件系统之后

我们就可以来理解一下它分层的一个原理了来给大家提一下

### P19：鏡像原理之分層理解

我们来看一下它是怎么分层的我们之前下载一个软件的时候它这边都会有一层一层的概念对不对比如说现在我们还是按照这个来下载

我来给大家下载一下我们在这边去下载一个radius来Docker2Radius它会去拉取radius

它下载这个版本的时候你可以看它有非常多层这里面的层有可能是跟我们之前有冲突的就是已经下载过了它就不会再下载了

你看是不是这一层是不是就被下载过了它就不用再下载了它只用下载根于radius相关的一些新的东西就可以了

其他东西它不用动这就是一层一层的概念每一个这个就称为一个layer就可以看到它这样一个层然后再往下走的话

我们刚才学过一个命令叫做inspect对不对我们来可以来看一下我们来看一下我们这个

刚才下载的这个radius inspect进向的一些操作回车在这个地方往上走这是一个完整的上面的话就是它的一个进向ID

不说往下找它的一个分层在这个地方有个叫layer这是它的一个官方名词有这个layer在这个地方你可以看到它的所有的层

对不对这里面的每一层其实就说白了对应的每一个步骤每一个步骤

比如说第一步安装sandOS刚才有人做过所以说他不做了alreadyexcess就已经安装完了

而剩下安装radius需要的步骤干嘛在一步一步的安装每安装一步它就是一个记录说不来就是一个文件级的记录

那它是怎么记录的呢我们来看一下在这地方给大家去说过一个图我们往下翻所有的进向

最开始的时候都有一个基础进向那我们在上面去修改的时候它就会创建一个层比如说我们这个地方有一个第一层

就是我们的一个操作系统对不对比如说地方用的是Ubuntu为例那现在我要装的一个passer环境它立马就生成了第二层

这个层它就是只有一个passer那我如果把上面这个层删了我也可以只留这两个的话它就是一个Ubuntu加passer那我每操作一个环境

比如说我再加一层我现在要装一个GDK我现在要装GDK那装完GDK之后那这边就干嘛

它就会产生一个进向这就是咱们的一个第四层能力接维斯就是你每装一个策略它就干嘛

就会加一层那这个就好比什么Windows里面的一个安全补丁是一样的我们每操作一步

它都会加一层那现在的话比如说我现在这个地方有两个进向了看到了吗

假设我们这个地方来了一些其他进向假设这个东西存在的话比如说举这个例子那在第一层里面

来然后第一层里面存在三个进向比如说文件1文件2文件3

那现在第二层里面我来了个文件4文件5文件6这两个是没有冲突的

看到了吗这两个是没有冲突的那把它打包成一个进向的时候它里面就会有六个文件也就相当于有什么

有六层能力界尾寸把这两个最后你看它是打包成了一个总体的进向那它会把这个两个大层干嘛再压缩

这一个大层里面有三个小层那所以说这边最终把它拼下来的话这个地方就是六个文件也就是说形成了下面这样一个区别

那但是现在我突然又来了我有一个心虚球我要在这文件上再加一层比如说我要加上这是我们的一个基本环境

假设我给大家举个例子这底下三层是我们最基本的比如说我这边有个环境三头S可能还有一个咱们的买色口

可能还有一个咱们的Tomcat那有了这个之后第二层我们用加了一些东西比如说我这个地方加了一个Reduce那比如说还加了一个文件6

我就把它叫做ES然后这个时候这个文件5原来是我的App 1.0能解为啥原来的话最早的话

基本的环境在这个地方我们把它压过来是可以用的现在我把这个应用升级了来个App 2.0那现在我一旦升级之后

它会发现这个是版本的升级它就会干嘛用文件7把这个文件5替换掉所以说我们还是6层对不对

这样的话就可以实现我们的一些文件复用那假设现在又来一个应用那这个应用的话还是三层那还是三层那这个应用的话

底层用的还是咱们的三头S买色口以及Tomcat的这一层干嘛直接可以原封不动的拿过来进行使用OK吧

那假设第二层这个Reduce跟这个ES不变它也可以干嘛直接进行复用那如果只是文件变了

我们干嘛操作文件就好了它这个就可以干嘛就可以使用复用那在外部看来的话我们可能有六个进向

六个Layer我们刚才看了那个单词叫做Layer就是一个层级嘛对不对那现在的话

我们其实这里面的话是有七个因为我们的5是嘛被替换掉了那这些都不重要这是它的一个底层原理大家重点要理解一个概念是

我们刚才说的什么联合文件系统它的所有进向它是一步一步的然后干嘛进行了分层那我们了解这个分层之后

我们再来看一下它最终呢把这个进向打包之后也就是会生成这么一个样子对不对

有了咱们基础的文件还有它那我们真正下载的时候下载的时间就下载了它那现在假如我们去下载这个进向

你会看到它这边有六个Layer就是我们这边下载我们来往上假如Reduce的话你给反推一下

它这边来看一下来这是几步啊一二三四五六这是不就是六步啊

对不对这个基本进向肯定是一个系统的话我们这边已经有了所以说它不用下载了

而剩下的五个可能是Reduce相关的一些配置命令那所以说它是单独的这边可以考过来然后这每一个呢

再往下面是不是就是六个层级啊对不对那这就对我们的六个层对于外来展示的话

就是这么一个效果但是你要知道它它里面比如说你这么Docker铺一个Reduce这个Reduce就直接一信下载下来

它是分层下载的分层下载的好处就是假设有些应用它的层是相同的我们干嘛就可以直接附用

所以说这个地方要记住那Docker进向的话默认都是指读的容器启动的时候我们就加了一个新的经向

来给大家解释这段话什么意思呢比如说我现在这个地方已经去启动了一个Tomcat我们从Docker官方去铺了一个Tomcat

把它下载下载我一旦把它运行起来之后它就会变成这个样子这个本来的Tomcat你是没有权限去动它的

这个地方的Tomcat就是原来的就是启动的这个经向它是一个指读的那现在你对它的操作其实也是一个层级操作

在上面加了一层你的所有操作都在这里你的所有操作都在这里那其实会发现这个地方是新的一层了

能理解回事这就是新的一层那之后我们可以把我们新的这一层加上原来的干嘛

再打包成一个大的经向发布我们等会就可以来做个这么多测试来打包大的经向发布那我们就可以把它当作一个新的经向而这个经向

里面这个操作是不是就被我们改过的了而这底下的是我们不会变的所以说你每次下过来的新东西它里面的你是不会变的而你操作说白就在这个层级上干嘛

去新加操作在这里面就有个概念那里面的叫咱们的一个进向层进向层是无法改变的就是你咱们从远程铺过来的

哎哟 这怎么了就是你们从远程下过来的这个东西是无法改变的但是你们通过这边一旦Continue绕起来的这个东西

它就已经新加了一层了所以说我把这个图给大家总结到这个地方你们这个地方去下载的是一个基本的层级那比如说这个地方是Continue

TombCat假设这个地方有6个来一二6个层级OK 那这个地方的6个层级跟你无关你把它铺下来之后

那这个TombCat里面它会去干嘛呢它会去下载比如说这是第一层这是第二层这是第三层

第四层第五层第六层它把它下载下来之后那这是一个完整的TombCat

对不对你把它下载过来这是TombCat然后你要去把它运行起来的时候那这个地方写上

这是咱们的一个PoMing令来PoMing令那最终我们要把它运行起来那就要再执行一个RomMing令RomMing令

这个理解很重要思想上的理解来你Rom起来之后呢就变成了两层

就会现在的话有一个基本的TombCat那这就是我们的进向运起来的我们把它叫做进向层也就是我们原来的进原本的进向

进向层进向进进哎呦

进向这个书法的问题进向层那这个进向层是一个固定的就是我们的TombCat

就是你下载过来的东西你的所有操作的来把它运起来这边你把它把这个地方理解为

这是一个容器层OK这是你自己操作一旦运起来就变成这个样子你的所有操作都是基于容器层的

来没关系你的所有操作都是基于什么的容器层的OK

那最终你把这个容器写完之后部署完之后你想用这个容器发布比如说我现在

我是狂神我想发布给张三那张三我怎么样拿到我这个来我就需要把我操作完这个之后

通过它原来的进向层加上它新改变的这个进向再次打包成为一个层级所以它是这么一个过程

那接下来的话我们就来研究一下如何咱们的提交一个自己的进向那提交一个自己的进向

也就是扛密套咱们的一个进向那把这个地方给大家停一下理解了这个分层再来理解这个扛密套

你瞬间就懂了来停一下停一下

